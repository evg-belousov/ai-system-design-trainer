import type { Question } from '@/data/types';

export const databasesQuestions: Question[] = [
  {
    id: 'sa-databases-001',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is the first normal form (1NF) in relational databases?',
    options: [
      'Every non-key attribute is functionally dependent on the entire primary key',
      'All attributes are atomic (indivisible), and the table contains no repeating groups',
      'There are no transitive dependencies of non-key attributes',
      'All foreign keys reference existing records in the related tables',
    ],
    correctIndex: 1,
    explanation:
      'The first normal form (1NF) requires that all attribute values be atomic — each table cell contains a single indivisible value rather than a list or a nested structure. There must also be no repeating groups of columns (for example, phone1, phone2, phone3 is a 1NF violation). The option about functional dependency on the entire primary key describes the second normal form (2NF). The absence of transitive dependencies is the third normal form (3NF). Referential integrity of foreign keys is not a normal form but an integrity constraint (referential integrity).',
  },
  {
    id: 'sa-databases-002',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'junior',
    type: 'quiz',
    question: 'Which type of SQL JOIN returns only the rows that have a match in both tables?',
    options: [
      'LEFT JOIN',
      'RIGHT JOIN',
      'INNER JOIN',
      'FULL OUTER JOIN',
    ],
    correctIndex: 2,
    explanation:
      'INNER JOIN returns only the rows that have matching values in both joined tables. If a row from one table has no match in the other, it will not appear in the result. LEFT JOIN returns all rows from the left table, supplementing them with data from the right table (or NULL when there is no match). RIGHT JOIN works the same way but for the right table. FULL OUTER JOIN returns all rows from both tables, substituting NULL where there is no match. The choice of JOIN type depends on business requirements: for example, if you need a list of all customers, including those who have not bought anything yet, you use a LEFT JOIN from customers to orders.',
  },
  {
    id: 'sa-databases-003',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is the key difference between the UNION and UNION ALL operations in SQL?',
    options: [
      'UNION combines tables horizontally, while UNION ALL combines them vertically',
      'UNION removes duplicates from the result, while UNION ALL returns all rows, including duplicates',
      'UNION ALL works only with identical tables, while UNION works with any tables',
      'UNION ALL sorts the result automatically, while UNION does not',
    ],
    correctIndex: 1,
    explanation:
      'UNION and UNION ALL combine the results of two or more SELECT queries vertically (by adding rows). The key difference: UNION performs deduplication — it removes duplicate rows from the result, which requires additional sorting and comparison (reducing performance). UNION ALL returns all rows from all queries, including full duplicates, and works faster. In practice, you should use UNION ALL when you are sure there are no duplicates, or when duplicates are acceptable — this is significantly more efficient. Both operators require the same number of columns with compatible data types across all combined queries.',
  },
  {
    id: 'sa-databases-004',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'middle',
    type: 'open',
    question: 'Explain what the third normal form (3NF) is and give an example of a table that is in 2NF but violates 3NF. Show how to bring it to 3NF.',
    sampleAnswer:
      'The third normal form (3NF) requires that the table be in 2NF and contain no transitive dependencies: each non-key attribute must depend only on the primary key and not on any other non-key attribute. Example of a violation: an "Employees" table with the columns (employee_id PK, name, department_id, department_name, department_location). Here department_name and department_location depend on department_id, not directly on employee_id — this is a transitive dependency. To bring it to 3NF, you need to move the department attributes into a separate table: "Employees" (employee_id PK, name, department_id FK) and "Departments" (department_id PK, department_name, department_location). This eliminates update anomalies (when renaming a department, you change one record rather than all employee rows) and deletion anomalies (when the last employee of a department is dismissed, information about the department is not lost).',
    explanation:
      'Normalization is the process of eliminating data redundancy. 1NF: atomicity. 2NF: no partial dependencies (relevant for composite keys). 3NF: no transitive dependencies. Denormalization (deliberately violating normal forms) is sometimes applied to improve read performance, but the analyst must understand which anomalies arise as a result and how to control them (for example, via triggers or application-level logic).',
  },
  {
    id: 'sa-databases-005',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'senior',
    type: 'open',
    question: 'Compare relational (SQL) and non-relational (NoSQL) databases. In what cases should a systems analyst recommend a NoSQL solution? What trade-offs does this involve?',
    sampleAnswer:
      'Relational databases (PostgreSQL, Oracle, MS SQL) provide ACID transactions, a strict data schema, a powerful query language (SQL), and normalized storage. They are optimal for systems with a clear data structure, complex relationships between entities, and consistency requirements (financial systems, ERP, CRM). NoSQL databases fall into categories: document stores (MongoDB) — flexible schema, nested documents; key-value (Redis) — maximum speed for simple operations; columnar (Cassandra, ClickHouse) — analytics and writing large volumes; graph (Neo4j) — complex relationships and graph traversals. NoSQL is recommended when: (1) the data schema is unstable and changes frequently; (2) horizontal write scaling is required; (3) the data is non-relational by nature (graphs, documents, time series); (4) low latency is needed under high load with simple access patterns. Trade-offs (CAP theorem): most NoSQL systems sacrifice strong consistency for availability and partition tolerance (eventual consistency). Also: the absence of JOINs leads to data duplication, limited ad-hoc query capabilities, and the need to design the data model around specific query patterns (query-driven design).',
    explanation:
      'Choosing the database type is an architectural decision with long-term consequences. The analyst must evaluate: the volume and growth rate of data, read/write patterns, consistency requirements (strong vs eventual), the need for complex queries and JOINs, and scaling requirements. Often the optimal choice is polyglot persistence: using different types of databases for different tasks within a single system (for example, PostgreSQL for transactions + Redis for caching + ClickHouse for analytics).',
  },
  {
    id: 'sa-databases-006',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'senior',
    type: 'quiz',
    question: 'When designing an ER model for an online store, the relationship between the "Order" and "Product" entities is "many-to-many". How should this relationship be correctly implemented in a relational database?',
    options: [
      'Add an array of product IDs to the orders table',
      'Create an intermediate (junction) table with foreign keys to both entities',
      'Add an order_id field to the products table',
      'Use a JSON field to store the list of products in an order',
    ],
    correctIndex: 1,
    explanation:
      'A "many-to-many" relationship in the relational model is implemented through an intermediate (associative, junction/bridge) table that contains foreign keys to both related entities. For example: an order_items table (order_id FK, product_id FK, quantity, price). The primary key is composite (order_id, product_id) or surrogate. The intermediate table can contain additional relationship attributes (quantity, price at the time of the order). An array of IDs (option 1) and a JSON field (option 4) violate 1NF, complicate writing queries, and do not allow the use of referential integrity constraints. Adding order_id to the products table (option 3) would turn the relationship into "one-to-many", which does not meet the requirements.',
  },
  {
    id: 'sa-databases-007',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'middle',
    type: 'quiz',
    question: 'Which of the listed anomalies is eliminated when moving from an unnormalized table to the first normal form?',
    options: [
      'Transitive dependency',
      'Storing multiple values in a single cell (violation of atomicity)',
      'Partial dependency on a composite key',
      'Cyclic dependency between tables',
    ],
    correctIndex: 1,
    explanation:
      'The first normal form (1NF) eliminates the violation of atomicity: the situation where multiple values are stored in a single cell (for example, a comma-separated list of phone numbers or a nested table). After bringing the data to 1NF, each cell contains exactly one atomic value. Transitive dependency is eliminated when moving to 3NF. Partial dependency on a composite key is eliminated when moving to 2NF. A cyclic dependency between tables is not a normalization problem but relates to schema design as a whole.',
  },
  {
    id: 'sa-databases-008',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'junior',
    type: 'quiz',
    question: 'Which transaction isolation level in SQL is used by default in most DBMSs and prevents reading uncommitted data?',
    options: [
      'Read Uncommitted',
      'Read Committed',
      'Repeatable Read',
      'Serializable',
    ],
    correctIndex: 1,
    explanation:
      'Read Committed is the isolation level at which a transaction sees only the data committed by other transactions. This prevents the "dirty read", where a transaction reads the uncommitted changes of another transaction that may be rolled back. Read Committed is the default level in PostgreSQL, Oracle, and SQL Server. However, it does not protect against the "non-repeatable read" — a repeated SELECT may return different data if a concurrent transaction managed to commit its changes. Read Uncommitted is the minimal isolation that allows dirty reads. Repeatable Read guarantees the stability of the rows that have been read. Serializable is the maximum isolation, emulating sequential execution.',
  },
  {
    id: 'sa-databases-009',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is an index (INDEX) used for in a relational database?',
    options: [
      'To encrypt the data in a table',
      'To speed up searching for and retrieving data by certain columns',
      'To automatically back up the table',
      'To restrict access to specific rows',
    ],
    correctIndex: 1,
    explanation:
      'An index is a data structure that speeds up search and retrieval operations (SELECT, WHERE, JOIN, ORDER BY) on certain columns of a table. Without an index, the DBMS performs a full table scan, checking every row. An index works like the subject index in a book — it allows you to quickly find the needed data without scanning everything. Trade-off: indexes speed up reads but slow down writes (INSERT, UPDATE, DELETE), since the index must also be updated on every data change. Indexes also take up additional disk space. Therefore, indexes are created only for columns frequently used in queries.',
  },
  {
    id: 'sa-databases-010',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'junior',
    type: 'quiz',
    question: 'Which type of NoSQL database stores data as nodes and edges, making it ideally suited for modeling social networks and recommendation systems?',
    options: [
      'Document Store',
      'Column-Family Store',
      'Key-Value Store',
      'Graph Database',
    ],
    correctIndex: 3,
    explanation:
      'Graph databases (Neo4j, Amazon Neptune, JanusGraph) store data as a graph: nodes represent entities, edges represent the relationships between them, and both can have properties. Graph databases are optimal for queries related to graph traversal: "friends of friends", "shortest path", "recommendations based on relationships", fraud detection, and knowledge management. Query languages: Cypher (Neo4j), Gremlin, SPARQL. Document databases (MongoDB) store JSON/BSON documents. Columnar databases (Cassandra, HBase) are optimized for writes and analytical queries. Key-value stores (Redis, DynamoDB) are the simplest model, used for caching and sessions.',
  },
  {
    id: 'sa-databases-011',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'junior',
    type: 'open',
    question: 'What is MVCC (Multi-Version Concurrency Control) and why is this mechanism used in databases?',
    sampleAnswer:
      'MVCC (Multi-Version Concurrency Control) is a concurrency control mechanism in which the DBMS keeps multiple versions of the same data row. When a transaction updates a row, a new version is created while the old one is retained as long as it is needed by other active transactions. Reading data never blocks writing, and vice versa — each transaction sees a consistent "snapshot" of the data at a specific point in time. This radically improves performance when many users work concurrently. MVCC is used in PostgreSQL, Oracle, and MySQL (InnoDB). For example, in PostgreSQL each row has the attributes xmin (the ID of the transaction that created it) and xmax (the ID of the transaction that deleted/updated it), which makes it possible to determine the visibility of a version for a specific transaction. A drawback of MVCC is the accumulation of old versions (bloat), which requires periodic cleanup (VACUUM in PostgreSQL).',
    explanation:
      'MVCC solves the fundamental "readers block writers" problem of the traditional locking approach. Without MVCC, a SELECT could block an UPDATE and vice versa, which is critical for high-load systems. The analyst must understand MVCC in order to properly design transactional logic and understand the specifics of the different isolation levels.',
  },
  {
    id: 'sa-databases-012',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'middle',
    type: 'quiz',
    question: 'Which index type is the most versatile and is used by default in most relational DBMSs?',
    options: [
      'Hash index',
      'B-tree (balanced tree)',
      'Bitmap index',
      'GIN index (Generalized Inverted Index)',
    ],
    correctIndex: 1,
    explanation:
      'B-tree (balanced tree) is the most versatile index type, used by default in PostgreSQL, MySQL, Oracle, and SQL Server. A B-tree supports equality operations (=), range queries (<, >, BETWEEN), sorting (ORDER BY), and prefix search (LIKE \'abc%\'). Data in a B-tree is stored in sorted order, which provides logarithmic search complexity O(log n). A Hash index is optimal only for exact equality (=) and does not support ranges. A Bitmap index is efficient for columns with a small number of unique values (gender, status) in OLAP systems. A GIN index is used for full-text search and indexing arrays/JSONB.',
  },
  {
    id: 'sa-databases-013',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'middle',
    type: 'quiz',
    question: 'Which anomaly during concurrent transaction execution occurs when a transaction re-reads the same data and gets a different result because another transaction committed changes?',
    options: [
      'Dirty Read',
      'Non-Repeatable Read',
      'Phantom Read',
      'Lost Update',
    ],
    correctIndex: 1,
    explanation:
      'Non-Repeatable Read is an anomaly in which a transaction reads the same row twice and gets different values, because between the reads another transaction updated and committed that row. Example: transaction A reads balance = 1000, transaction B changes it to 500 and commits, transaction A re-reads balance = 500. A dirty read is reading uncommitted data. A phantom read is the appearance or disappearance of rows in the result of a repeated query (an INSERT/DELETE by another transaction). A lost update is when two transactions read the same data and overwrite each other. The Repeatable Read level prevents non-repeatable reads.',
  },
  {
    id: 'sa-databases-014',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'middle',
    type: 'open',
    question: 'What is polyglot persistence and in what cases is this approach justified? Give an example of a system that uses several types of databases.',
    sampleAnswer:
      'Polyglot persistence is an approach in which different types of databases are used within a single system, each of which is optimal for its task. Instead of using a single universal DBMS for all needs, different system components work with the storage that best matches their data access patterns. Example of an online store: PostgreSQL — the main relational database for orders, users, and the product catalog (ACID transactions, complex queries); Redis — caching user sessions, shopping carts, and frequently requested data (low latency, TTL); Elasticsearch — full-text search over the product catalog (faceted search, ranking); ClickHouse — analytics and reporting (fast aggregating queries over large data volumes); MongoDB — storing logs and untyped user data (flexible schema). The approach is justified when a system has heterogeneous requirements for storing and processing data. Risks: increased operational complexity (you need to administer several DBMSs), the difficulty of ensuring consistency across the stores.',
    explanation:
      'Polyglot persistence is a consequence of the "right tool for the job" principle. In a microservices architecture, each service can choose the optimal DBMS. The analyst must justify the choice of each store through an analysis of access patterns, data volumes, and non-functional requirements (latency, throughput, consistency).',
  },
  {
    id: 'sa-databases-015',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'middle',
    type: 'quiz',
    question: 'Which approach to database schema migration involves applying sequential change scripts (versioned migrations)?',
    options: [
      'Shadow Database',
      'Versioned Migrations (Flyway, Liquibase)',
      'Database Dump & Restore',
      'Manual DDL Execution',
    ],
    correctIndex: 1,
    explanation:
      'Versioned Migrations is an approach in which each database schema change is described by a separate script (migration) with a sequence number or timestamp. Migrations are applied sequentially, and a special table (for example, flyway_schema_history) tracks which migrations have already been applied. Tools: Flyway, Liquibase, Alembic (Python), Knex (Node.js). Advantages: versioning the schema together with the code (in Git), reproducibility (any developer or CI/CD pipeline can bring the database to its current state), an audit trail of changes, and support for rollback scripts. A Shadow Database is a copy of the database for testing migrations. Dump & Restore is a one-time operation, not suitable for iterative development. Manual DDL is the manual execution of SQL, which is error-prone.',
  },
  {
    id: 'sa-databases-016',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'senior',
    type: 'quiz',
    question: 'Which transaction isolation level in SQL fully prevents all concurrency anomalies but can significantly reduce performance?',
    options: [
      'Read Committed',
      'Repeatable Read',
      'Serializable',
      'Snapshot Isolation',
    ],
    correctIndex: 2,
    explanation:
      'Serializable is the strictest isolation level, guaranteeing that the result of concurrent transaction execution is equivalent to some sequential execution. It prevents all anomalies: dirty read, non-repeatable read, phantom read, and the serialization anomaly (write skew). The implementation depends on the DBMS: in PostgreSQL, Serializable is implemented via SSI (Serializable Snapshot Isolation) — an optimistic approach that rolls back a transaction when a serialization conflict is detected. In MySQL, it is implemented via gap locks. Trade-off: maximum correctness at the cost of reduced throughput and an increased number of rollbacks. Snapshot Isolation is an intermediate level (not part of the SQL standard) that prevents most anomalies but allows write skew.',
  },
  {
    id: 'sa-databases-017',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'senior',
    type: 'open',
    question: 'Describe strategies for optimizing slow SQL queries. What tools and approaches are used to diagnose performance problems?',
    sampleAnswer:
      'SQL query optimization strategies: (1) Analyzing the execution plan: EXPLAIN ANALYZE (PostgreSQL), EXPLAIN FORMAT=JSON (MySQL) show how the DBMS executes the query — table scans, used indexes, cardinality estimates, operation costs. The key is to look for Seq Scan on large tables, Nested Loop on large result sets, Sort on non-indexed columns. (2) Indexing: creating indexes on columns in WHERE, JOIN, ORDER BY. Composite indexes — consider the column order (leftmost prefix rule). Covering indexes (covering index / INCLUDE) — if all the needed columns are in the index, the table is not read. (3) Rewriting queries: replacing subqueries with JOINs, using EXISTS instead of IN for correlated subqueries, avoiding functions on indexed columns (WHERE UPPER(name) — the index is not used). (4) Partitioning: splitting large tables by range (date), list, or hash — the DBMS scans only the needed partitions (partition pruning). (5) Denormalization: materialized views, precomputed aggregates for frequent analytical queries. (6) Tools: pg_stat_statements (PostgreSQL), Performance Schema (MySQL), slow query log, APM systems (Datadog, New Relic).',
    explanation:
      'Query optimization is an iterative process: measure → analyze the execution plan → form a hypothesis → make a change → re-measure. The analyst must be able to read an execution plan and formulate recommendations for the developers. Common mistakes: creating indexes "on everything" (slows down writes), ignoring table statistics (ANALYZE), optimizing queries without understanding the business context.',
  },
  {
    id: 'sa-databases-018',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'senior',
    type: 'quiz',
    question: 'Which type of database is specifically optimized for storing and analyzing timestamped data (metrics, IoT sensors, logs)?',
    options: [
      'Document database',
      'Graph database',
      'Time-Series Database',
      'General-purpose columnar store',
    ],
    correctIndex: 2,
    explanation:
      'A Time-Series Database (TSDB) is a specialized type of database for storing and analyzing time-series data: sequences of data points indexed by time. Examples: InfluxDB, TimescaleDB (a PostgreSQL extension), Prometheus, VictoriaMetrics. TSDB optimizations: efficient compression (adjacent points are often close in value), fast writes (append-only), efficient aggregating queries over time windows (average temperature per hour), automatic deletion of old data (retention policy), downsampling (aggregating old data to save space). Applications: infrastructure monitoring, IoT sensors, financial quotes, application telemetry.',
  },
  {
    id: 'sa-databases-019',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'senior',
    type: 'open',
    question: 'Compare database migration strategies: Big Bang migration and incremental migration. What risks does each approach carry, and how can they be minimized?',
    sampleAnswer:
      'Big Bang migration is a one-time transfer of all data from the old system to the new one, followed by switching all users over. Advantages: simplicity of planning (a single switchover moment), no need to maintain synchronization between the old and new systems. Risks: prolonged downtime, high risk — rolling back on failure is difficult, all errors are discovered simultaneously. Minimization: thorough testing on a copy of production data, rehearsal migrations, a clear rollback plan. Incremental migration is when data and users are transferred in stages, with the old and new systems running in parallel. Approaches: parallel run (dual write/read), the Strangler Fig pattern (gradual replacement of components), migration by user groups / modules. Advantages: minimal downtime, the ability to detect problems at early stages, easy rollback of an individual stage. Risks: the complexity of data synchronization between systems (CDC, dual write), a prolonged period of maintaining two systems, data consistency. Minimization: a clear synchronization strategy, monitoring of discrepancies, automated integrity checks.',
    explanation:
      'The choice of migration strategy depends on: the acceptable downtime, data volume, schema complexity, the number of dependent systems, and the business tolerance for risk. The analyst plays a key role in migration planning: mapping fields between schemas, data transformation rules, handling invalid data, migration success criteria.',
  },
  {
    id: 'sa-databases-020',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'middle',
    type: 'quiz',
    question: 'Which index type in PostgreSQL is optimal for columns with a small number of unique values (for example, gender, order status) in OLAP scenarios?',
    options: [
      'B-tree index',
      'Hash index',
      'BRIN index (Block Range Index)',
      'Bitmap index (via a bitmap scan when combining indexes)',
    ],
    correctIndex: 3,
    explanation:
      'Bitmap indexing is efficient for columns with low cardinality (a small number of unique values). PostgreSQL does not have an explicit Bitmap index as a separate type (unlike Oracle), but the optimizer automatically uses a Bitmap Index Scan — it converts the results of several B-tree or other indexes into bitmaps, combines them (AND/OR), and then accesses the table. This is especially efficient when combining conditions on several low-cardinality columns. A B-tree is versatile but excessive for two or three values. A Hash index is only for exact equality. A BRIN index is efficient for physically ordered data (for example, timestamps in append-only tables).',
  },
  {
    id: 'sa-databases-021',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is a foreign key (Foreign Key) in a relational database?',
    options: [
      'A unique identifier of a record in a table',
      'A column or set of columns that references the primary key of another table',
      'A key for encrypting data',
      'An index to speed up searches',
    ],
    correctIndex: 1,
    explanation:
      'A foreign key (FK) is a column or combination of columns whose values reference the primary key (or a unique key) of another table. An FK enforces referential integrity: you cannot insert an FK value that does not exist in the related table. The ON DELETE/ON UPDATE rules define the behavior when the parent record is deleted/changed: CASCADE (cascading delete), SET NULL, RESTRICT (prohibit), NO ACTION. The FK is the foundation for implementing relationships between entities in the relational model.',
  },
  {
    id: 'sa-databases-022',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'junior',
    type: 'quiz',
    question: 'Which SQL command is used to delete a table along with all its data and structure?',
    options: [
      'DELETE FROM table_name',
      'TRUNCATE TABLE table_name',
      'DROP TABLE table_name',
      'REMOVE TABLE table_name',
    ],
    correctIndex: 2,
    explanation:
      'DROP TABLE is a DDL command that deletes a table completely: its structure, data, indexes, constraints, and triggers. The operation is irreversible without a backup. DELETE FROM removes rows but keeps the table structure; it records each deletion in the transaction log and can be undone (ROLLBACK). TRUNCATE quickly removes all rows while keeping the structure; it resets auto-increment counters and usually does not log row by row. REMOVE TABLE does not exist in standard SQL.',
  },
  {
    id: 'sa-databases-023',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What does the letter "A" (Atomicity) guarantee in the properties of ACID transactions?',
    options: [
      'The data always remains in a correct state',
      'A transaction is executed completely or not at all',
      'Concurrent transactions are isolated from each other',
      'The result of a committed transaction is preserved permanently',
    ],
    correctIndex: 1,
    explanation:
      'Atomicity guarantees that a transaction is executed as a single whole: either all the operations of the transaction complete successfully, or none of them are applied. On a failure at any stage, all changes are rolled back. Example: a money transfer — the debit and credit must be performed together or not at all. Consistency — the data remains in a correct state. Isolation — the isolation of concurrent transactions. Durability — the preservation of the result.',
  },
  {
    id: 'sa-databases-024',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'junior',
    type: 'open',
    question: 'Explain the difference between a primary key (Primary Key) and a unique key (Unique Key). When should each be used?',
    sampleAnswer:
      'A Primary Key (PK) is a column or set of columns that uniquely identifies each record in a table. Constraints: only one PK per table, does not allow NULL, automatically creates a unique index. A Unique Key (UK) is a uniqueness constraint for a column or set of columns. Differences: there can be multiple UKs per table, it allows one NULL (in most DBMSs), and it also creates a unique index. When to use a PK: for the main record identifier (id, a surrogate key). When to use a UK: for business identifiers that must be unique but are not the main key (a user\'s email, an organization\'s tax ID, a product SKU). Example: a users table has a PK on id (a surrogate key) and a UK on email (a natural business identifier).',
    explanation:
      'The PK defines the identity of a record in the relational model; the FKs of other tables reference the PK. The UK ensures the uniqueness of business attributes. Practice: a surrogate PK (id) + a UK on natural business identifiers is the standard approach, combining the advantages of both.',
  },
  {
    id: 'sa-databases-025',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is a two-phase commit (Two-Phase Commit, 2PC)?',
    options: [
      'A mechanism for replicating data between servers',
      'A protocol for coordinating distributed transactions that ensures the atomicity of operations across several nodes',
      'A method for backing up a database',
      'An algorithm for balancing load between replicas',
    ],
    correctIndex: 1,
    explanation:
      'Two-Phase Commit (2PC) is a protocol for coordinating distributed transactions. Phase 1 (Prepare): the coordinator sends a prepare request to all participants; the participants perform the operations, write to the log, and respond with their readiness to commit or abort. Phase 2 (Commit/Abort): if all participants are ready, the coordinator sends commit; if even one is not ready, it sends abort. 2PC guarantees the atomicity of a distributed transaction but has drawbacks: it is a blocking protocol (participants wait for the coordinator\'s decision), there is a single point of failure (the coordinator), and it scales poorly. Alternatives: the Saga Pattern, eventual consistency.',
  },
  {
    id: 'sa-databases-026',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is Change Data Capture (CDC) in the context of databases?',
    options: [
      'Backing up data',
      'Tracking and capturing changes in a database in order to deliver them to other systems',
      'Encrypting data in transit',
      'Compressing data to reduce storage volume',
    ],
    correctIndex: 1,
    explanation:
      'Change Data Capture (CDC) is a technology for tracking changes (INSERT, UPDATE, DELETE) in a database and delivering them to other systems in real time. Implementation methods: reading the transaction log (the most efficient — Debezium), database triggers, timestamp-based polling (less reliable). Applications: data replication, synchronization between systems, building analytical warehouses, implementing event-driven architecture on top of a legacy database, updating search indexes, invalidating caches. CDC is a key tool for the Transactional Outbox pattern.',
  },
  {
    id: 'sa-databases-027',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'middle',
    type: 'quiz',
    question: 'Which database design pattern involves storing related data together to optimize reads in NoSQL databases?',
    options: [
      'Normalization to 3NF',
      'Denormalization and embedding',
      'Hash sharding',
      'Range partitioning',
    ],
    correctIndex: 1,
    explanation:
      'In document-oriented NoSQL databases (MongoDB, Couchbase), embedding is often applied — storing related data inside a single document instead of normalizing it into separate collections. Example: an order contains an embedded array of order line items. Advantages: a single query instead of a JOIN, atomic update of the document. Disadvantages: data duplication, increased document size, the difficulty of updating embedded data when it changes in the source. Rule: embed if the data is read together and rarely changes independently; reference if the data is updated independently or the document becomes too large.',
  },
  {
    id: 'sa-databases-028',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'middle',
    type: 'open',
    question: 'Describe strategies for migrating a database schema in production without downtime (zero-downtime migration).',
    sampleAnswer:
      'Zero-downtime migration strategies: (1) Expand-Contract Pattern (Parallel Change): Expand — add a new column/table, the application writes to both places (old and new); Migrate — a background process migrates the historical data; Contract — after the full migration and updating all clients, remove the old structure. (2) Backward-Compatible Changes: adding nullable columns and creating new tables do not break the old code. (3) Blue-Green Database: two schema versions, switching the traffic. Difficult for stateful systems. (4) Shadow Writes: writing to both schema versions simultaneously for validation. (5) Feature Flags: the new code uses the new schema only after the flag is enabled. Practices: migrations must be backward-compatible (the old code works with the new schema); separate the deployment of code and the database migration; small incremental migrations instead of large ones; test migrations on a copy of production data; have a rollback plan. Tools: Flyway, Liquibase, gh-ost (GitHub) for online schema changes in MySQL.',
    explanation:
      'Zero-downtime migrations are critical for high-availability systems. The key principle: splitting changes into backward-compatible steps. Removing a column is three steps: 1) stop using it in the code, 2) deploy, 3) drop the column. Adding a NOT NULL column: add it as nullable → fill it with values → make it NOT NULL.',
  },
  {
    id: 'sa-databases-029',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is a deadlock in a database?',
    options: [
      'A situation where a transaction waits for too long',
      'A mutual lock of two or more transactions, each of which waits for a resource locked by the other',
      'A transaction log overflow',
      'A failure when writing to disk',
    ],
    correctIndex: 1,
    explanation:
      'A deadlock (mutual lock) is a situation where two or more transactions mutually block each other: T1 holds a lock on resource A and waits for resource B; T2 holds a lock on B and waits for A. Neither transaction can proceed. The DBMS detects the deadlock and automatically rolls back one of the transactions (the victim) so that the others can continue. Prevention: a consistent order of accessing resources, short transactions, minimizing locks (optimistic locking), timeouts.',
  },
  {
    id: 'sa-databases-030',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'senior',
    type: 'quiz',
    question: 'Which pattern ensures consistency between writing to the database and sending a message to a broker?',
    options: [
      'Saga Pattern',
      'Two-Phase Commit',
      'Transactional Outbox',
      'Event Sourcing',
    ],
    correctIndex: 2,
    explanation:
      'Transactional Outbox solves the dual write problem: writing to the database and sending to the broker are two different operations that cannot be combined into a single transaction. Solution: the message is written to a special outbox table in the same transaction as the business data. A separate process (the message relay) reads the outbox and publishes to the broker. Implementation options: a polling publisher (periodic polling), CDC-based (Debezium reads the transaction log). The pattern guarantees that the message will be sent if and only if the transaction is committed. 2PC is a heavy protocol that scales poorly. Saga is for distributed transactions.',
  },
  {
    id: 'sa-databases-031',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is table partitioning, and what problem does it solve?',
    options: [
      'Splitting a table across servers for distributed processing',
      'Splitting a large table into logical parts (partitions) by a given key to improve performance and manageability',
      'Creating copies of a table for redundancy',
      'Encrypting parts of a table with different keys',
    ],
    correctIndex: 1,
    explanation:
      'Partitioning is the splitting of a large table into smaller parts (partitions) by the value of a partitioning key. Types: Range (by range, for example, dates), List (by a list of values), Hash (by the hash of the key). Advantages: partition pruning (the DBMS scans only the needed partitions), parallel processing of partitions, convenient archiving (DROP PARTITION instead of DELETE), faster maintenance operations. Applications: large tables with time-based data (logs, transactions), data with a natural division (by region, by customer). Do not confuse it with sharding — the distribution of data across different servers.',
  },
  {
    id: 'sa-databases-032',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'senior',
    type: 'open',
    question: 'Compare optimistic and pessimistic locking. When should each be used?',
    sampleAnswer:
      'Pessimistic locking: a transaction acquires a lock on the data before reading/modifying it and holds it until completion. Implementation: SELECT ... FOR UPDATE. Advantages: guaranteed integrity, simple logic. Disadvantages: reduced concurrency, deadlock risk, blocking during long operations. When to use: a high probability of conflicts, critical financial operations, short transactions. Optimistic locking: a transaction reads the data with a version (version, updated_at), and when writing it checks that the version has not changed. Implementation: UPDATE ... WHERE id = ? AND version = ?; if 0 rows were updated, there is a conflict. Advantages: high concurrency, no physical locks. Disadvantages: the need to handle conflicts (retry, notifying the user), more complex logic. When to use: a low probability of conflicts, long operations (editing a document), web applications with forms. Practice: most web applications use optimistic locking as the default.',
    explanation:
      'The choice of locking type depends on data access patterns. Optimistic locking is the standard for web applications where a user edits a form for several minutes. Pessimistic locking is for critical operations with high contention (debiting an account). ORM frameworks (Hibernate, ActiveRecord) support both approaches through annotations or methods.',
  },
  {
    id: 'sa-databases-033',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is Write-Ahead Logging (WAL), and why is it needed?',
    options: [
      'Logging queries for auditing',
      'A mechanism for writing changes to a log before applying them to the main data, to ensure durability and recovery',
      'Caching writes to speed them up',
      'Replicating data between servers',
    ],
    correctIndex: 1,
    explanation:
      'Write-Ahead Logging (WAL) is a mechanism in which changes are first written to a log, and only then applied to the main data pages. Guarantees: durability (on a failure, the data is recovered from the WAL), atomicity (incomplete transactions are rolled back). It is used in PostgreSQL (pg_wal), MySQL InnoDB (redo log), and Oracle (redo log). WAL is also used for replication (streaming replication in PostgreSQL), point-in-time recovery, and CDC. Trade-off: additional writes (write amplification), but critical protection against data loss.',
  },
  {
    id: 'sa-databases-034',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'middle',
    type: 'quiz',
    question: 'Which type of replication assumes that writes happen on a single server (master) while reads can be performed from several replicas?',
    options: [
      'Multi-Master Replication',
      'Master-Slave (Primary-Replica) Replication',
      'Peer-to-Peer Replication',
      'Synchronous Replication Only',
    ],
    correctIndex: 1,
    explanation:
      'Master-Slave (Primary-Replica) Replication is a model where one server (master/primary) accepts all writes, and the changes are replicated to one or more replica servers (slaves/replicas), from which reads are performed. Advantages: read scaling, fault tolerance (promotion of a replica when the master fails). Disadvantages: replication lag, potential data loss with asynchronous replication. Multi-Master allows writes to any node but makes conflict resolution harder. Application: most production systems with a read > write load.',
  },
  {
    id: 'sa-databases-035',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is NULL in SQL?',
    options: [
      'The value "zero"',
      'An empty string',
      'The absence of a value (unknown/missing)',
      'An error in the data',
    ],
    correctIndex: 2,
    explanation:
      'NULL in SQL means the absence of a value — the data is unknown, not applicable, or not filled in. NULL is not equal to zero (0), an empty string (\'\'), or another NULL. A comparison with NULL always yields UNKNOWN: WHERE column = NULL does not work, you need WHERE column IS NULL. Aggregate functions (SUM, AVG) usually ignore NULL. To handle NULL, the functions COALESCE (returns the first non-NULL value), NULLIF, IS NULL, and IS NOT NULL are used. A column with a NOT NULL constraint cannot contain NULL.',
  },
  {
    id: 'sa-databases-036',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is a composite index (Composite Index), and how does the order of columns affect its efficiency?',
    options: [
      'An index that compresses data',
      'An index on several columns, efficient for queries that use these columns in the order in which they are defined in the index',
      'An index automatically created when the table is created',
      'An index for full-text search',
    ],
    correctIndex: 1,
    explanation:
      'A composite index is an index on several columns. Leftmost Prefix Rule: an index on (A, B, C) is efficient for queries by A; A and B; A, B, and C. It is NOT efficient for queries by B or C alone without A. The order matters: an index (status, created_at) is different from (created_at, status). The first column should be the most selective or the one most frequently used in WHERE. A Covering Index is a composite index containing all the columns of the query (including those in SELECT), allowing the query to be executed using only the index, without accessing the table.',
  },
  {
    id: 'sa-databases-037',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'middle',
    type: 'open',
    question: 'What is the N+1 problem when working with a database? How can it be avoided?',
    sampleAnswer:
      'The N+1 problem is an anti-pattern where, to fetch data, 1 query is executed for a list of parent records and N additional queries are executed for the related data of each parent record. Example: fetch a list of orders (1 query), then for each order fetch the customer (N queries). With 1000 orders, that is 1001 queries instead of 2. Solutions: (1) JOIN — combine the data in a single query: SELECT * FROM orders JOIN customers ON orders.customer_id = customers.id. (2) Eager Loading (ORM) — specify the relationships to preload: Order.includes(:customer).all (Rails), @ManyToOne(fetch = FetchType.EAGER) or join fetch (Hibernate). (3) Batch Loading — loading the related data in batches: WHERE customer_id IN (1, 2, 3, ...). (4) DataLoader pattern (GraphQL) — automatic batching of queries. Detection: monitoring the number of SQL queries per HTTP request, ORM logging.',
    explanation:
      'N+1 is one of the most common causes of performance problems in applications that use an ORM. Lazy loading is convenient for development but dangerous in production. Rule: for lists, always use eager loading or explicit JOINs. Monitoring the SQL queries for each endpoint is a mandatory practice.',
  },
  {
    id: 'sa-databases-038',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is database sharding?',
    options: [
      'Splitting a table into partitions within a single server',
      'Horizontally distributing data across several independent database servers',
      'Replicating data between servers',
      'Caching frequently used queries',
    ],
    correctIndex: 1,
    explanation:
      'Sharding is the horizontal scaling of a database by distributing data across several independent servers (shards). Each shard contains a portion of the data, determined by the shard key. Methods: hash sharding (a hash of the key), range sharding (ranges of values), directory sharding (a routing table). Advantages: scaling writes and storage, distributing the load. Challenges: cross-shard queries and transactions, rebalancing when adding shards, the choice of shard key. Do not confuse it with partitioning (one database, several partitions) or replication (copies of the same data).',
  },
  {
    id: 'sa-databases-039',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'senior',
    type: 'open',
    question: 'Describe approaches to PostgreSQL performance tuning. Which parameters and metrics are analyzed?',
    sampleAnswer:
      'PostgreSQL performance tuning: (1) Query analysis: EXPLAIN ANALYZE for the execution plan; pg_stat_statements for the top slow queries; log_min_duration_statement for logging slow queries. Metrics: Seq Scan vs Index Scan, rows estimated vs actual, time. (2) Indexing: creating indexes on columns in WHERE, JOIN, ORDER BY; analyzing pg_stat_user_indexes (unused indexes); partial indexes (CREATE INDEX ... WHERE), expression indexes. (3) Memory parameters: shared_buffers (25-40% of RAM), effective_cache_size (50-75% of RAM), work_mem (for sorts and hash tables, careful — it is multiplied by the number of connections), maintenance_work_mem (for VACUUM, CREATE INDEX). (4) Parallelism: max_parallel_workers_per_gather, parallel_tuple_cost, parallel_setup_cost. (5) Checkpoints: checkpoint_timeout, checkpoint_completion_target (to spread out the I/O). (6) Autovacuum: tuning the frequency and aggressiveness to prevent bloat. (7) Monitoring: pg_stat_activity (active sessions), pg_stat_user_tables (seq_scan, idx_scan, dead tuples), pg_stat_bgwriter (checkpoint stats). (8) Connection Pooling: PgBouncer/PgPool to reduce the overhead of creating connections.',
    explanation:
      'PostgreSQL tuning is an iterative process: measure → hypothesis → change → measure. Start with query analysis (pg_stat_statements) — often 90% of problems are solved with the right indexes. Memory parameters depend on the nature of the workload (OLTP vs OLAP). Production monitoring is mandatory (pgAdmin, Datadog, Prometheus + postgres_exporter).',
  },
  {
    id: 'sa-databases-040',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'junior',
    type: 'quiz',
    question: 'Which SQL operator is used to group rows by a column value?',
    options: [
      'ORDER BY',
      'GROUP BY',
      'PARTITION BY',
      'CLUSTER BY',
    ],
    correctIndex: 1,
    explanation:
      'GROUP BY groups rows with the same values of the specified columns into summary rows. It is typically used with aggregate functions: COUNT, SUM, AVG, MAX, MIN. Example: SELECT department, COUNT(*) FROM employees GROUP BY department — the number of employees per department. HAVING filters groups (like WHERE for rows). ORDER BY sorts the result. PARTITION BY is used in window functions to divide rows within a query without affecting the grouping of the result.',
  },
  {
    id: 'sa-databases-041',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'middle',
    type: 'quiz',
    question: 'How does synchronous replication differ from asynchronous replication?',
    options: [
      'Synchronous uses TCP, asynchronous uses UDP',
      'With synchronous replication the transaction on the master waits for confirmation from the replica; with asynchronous it does not wait',
      'Synchronous works only within a single data center',
      'Asynchronous does not support failover',
    ],
    correctIndex: 1,
    explanation:
      'Synchronous replication: a transaction on the primary is considered complete only after the write is confirmed on at least one replica. Guarantee: the data will not be lost if the primary fails. Disadvantage: increased latency, dependence on the availability of the replica. Asynchronous replication: the primary confirms the transaction immediately, and replication happens in the background. Advantage: low latency. Disadvantage: replication lag, possible data loss if the primary fails before replication. The choice depends on the requirements for durability vs latency.',
  },
  {
    id: 'sa-databases-042',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is a materialized view?',
    options: [
      'A virtual table computed on every query',
      'A view whose data is physically stored and periodically refreshed',
      'A temporary table for the current session',
      'A backup copy of a table',
    ],
    correctIndex: 1,
    explanation:
      'A Materialized View is a view whose result is physically stored in the database as a table. Unlike a regular VIEW (which is computed on every query), a materialized view stores precomputed data. Advantages: fast access to complex aggregates, reduced load on the source. Disadvantages: the data may be stale, it requires refreshing (REFRESH). Applications: analytical reports, caching the results of heavy JOINs, denormalization for reads. PostgreSQL supports REFRESH MATERIALIZED VIEW (a full refresh) and REFRESH MATERIALIZED VIEW CONCURRENTLY (without blocking reads).',
  },
  {
    id: 'sa-databases-043',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What does the CAP theorem state about distributed systems?',
    options: [
      'A distributed system can provide any three properties simultaneously',
      'Of the three properties — Consistency, Availability, Partition Tolerance — only two can be provided simultaneously',
      'Distributed systems are always faster than centralized ones',
      'Partitioning always improves performance',
    ],
    correctIndex: 1,
    explanation:
      'The CAP theorem (Eric Brewer) states that a distributed system cannot simultaneously provide all three properties: Consistency (all nodes see the same data at the same time), Availability (every request receives a response), Partition Tolerance (the system operates when the connection between nodes is severed). Since partition tolerance is mandatory for distributed systems, the choice is effectively between CP (consistency during a partition — refusing unavailable nodes) and AP (availability during a partition — eventual consistency). Traditional RDBMSs are CA within a single node. NoSQL systems often choose AP (Cassandra, DynamoDB) or CP (MongoDB, HBase).',
  },
  {
    id: 'sa-databases-044',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'senior',
    type: 'open',
    question: 'Describe the Saga pattern for managing distributed transactions. What kinds of Saga exist, and how are they implemented?',
    sampleAnswer:
      'A Saga is a pattern for managing distributed transactions through a sequence of local transactions, each of which has a compensating action. Instead of an ACID transaction via 2PC, there is eventual consistency with a guaranteed rollback on failure. Kinds of Saga: (1) Choreography-based Saga: each service knows which event to publish after its local transaction; other services subscribe to events and react. Pros: decentralization, loose coupling. Cons: it is hard to trace the full process, there is a risk of cyclic dependencies. (2) Orchestration-based Saga: a central orchestrator (the Saga Coordinator) manages the sequence of steps, calling services and handling responses. Pros: the process logic is in one place, easier monitoring. Cons: the orchestrator is a single point of failure, tighter coupling. Implementing compensations: each step has a compensating action (canceling a reservation, refunding money). On a failure, the Saga performs the compensations in reverse order. Tools: Camunda, Temporal, Axon Framework, or a custom one built on Kafka.',
    explanation:
      'The Saga is the standard approach to transactions in microservices, replacing the heavy 2PC. Key requirements: the idempotency of the steps and compensations, observability (the Saga status), handling concurrent conflicts (semantic lock). The analyst must document all the steps of a Saga and their compensations for each business process.',
  },
  {
    id: 'sa-databases-045',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'junior',
    type: 'quiz',
    question: 'Which SQL operator is used to modify the structure of an existing table?',
    options: [
      'UPDATE',
      'MODIFY',
      'ALTER TABLE',
      'CHANGE TABLE',
    ],
    correctIndex: 2,
    explanation:
      'ALTER TABLE is a DDL command for modifying the structure of an existing table: adding/removing/changing columns, adding/removing constraints, changing data types, renaming columns and the table. Examples: ALTER TABLE users ADD COLUMN age INT; ALTER TABLE users DROP COLUMN temp; ALTER TABLE users ADD CONSTRAINT uk_email UNIQUE (email). UPDATE is a DML command for changing data in rows, not the structure.',
  },
  {
    id: 'sa-databases-046',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What are window functions (Window Functions) in SQL?',
    options: [
      'Functions for working with dates and times',
      'Functions that perform calculations over a set of rows related to the current row, without grouping',
      'Data encryption functions',
      'Functions for creating temporary tables',
    ],
    correctIndex: 1,
    explanation:
      'Window functions (the OVER clause) perform calculations over a set of rows (a window) related to the current row, while keeping all rows in the result (unlike GROUP BY). Syntax: function() OVER (PARTITION BY ... ORDER BY ... ROWS/RANGE ...). Examples: ROW_NUMBER() — numbering rows; RANK(), DENSE_RANK() — ranking; LAG(), LEAD() — accessing the previous/next row; SUM() OVER, AVG() OVER — cumulative aggregates. Applications: numbering within groups, moving averages, comparing with a previous value, top-N within categories.',
  },
  {
    id: 'sa-databases-047',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is connection pooling, and why is it needed?',
    options: [
      'A mechanism for encrypting connections',
      'Reusing a pool of pre-created database connections to reduce overhead',
      'Load balancing between database servers',
      'Caching query results',
    ],
    correctIndex: 1,
    explanation:
      'Connection Pooling is a mechanism for reusing database connections from a pre-created pool. Creating a connection is an expensive operation (TCP handshake, authentication, allocating resources on the server). The pool maintains N open connections; the application takes a connection from the pool, uses it, and returns it. Parameters: min/max connections, idle timeout, validation query. Implementations: at the application level (HikariCP, c3p0) or an external pooler (PgBouncer for PostgreSQL). Without pooling, a high-load application can exhaust the database connection limit.',
  },
  {
    id: 'sa-databases-048',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is a BRIN index in PostgreSQL, and when should it be used?',
    options: [
      'An index for full-text search',
      'A block-based index (Block Range Index), efficient for physically ordered data',
      'An index for JSON fields',
      'An index for geographic data',
    ],
    correctIndex: 1,
    explanation:
      'BRIN (Block Range Index) is a PostgreSQL index type that stores summary information (min/max) for ranges of physical table blocks. It is efficient when: the data is physically ordered by the indexed column (timestamps in append-only tables, monotonically increasing IDs). Advantages: very compact (1000 times smaller than a B-tree), fast to create, low write overhead. Disadvantages: it works only when there is physical correlation between the data and the order of values; it is less efficient for point queries. Applications: tables of logs, time series, and transactions with a timestamp.',
  },
  {
    id: 'sa-databases-049',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'senior',
    type: 'open',
    question: 'What strategies are used for scaling databases? Compare vertical and horizontal scaling.',
    sampleAnswer:
      'Vertical scaling (Scale Up): increasing the power of a single server — more CPU, RAM, faster disks (NVMe SSD). Advantages: simplicity, no changes to the architecture, ACID transactions work as is. Disadvantages: the physical limit of server power, the high cost of top-tier configurations, a single point of failure. Horizontal scaling (Scale Out): distributing the load across several servers. Strategies: (1) Read Replicas — replicas for reads, the master for writes; this scales reads, not writes. (2) Sharding — splitting data across servers by a key; this scales both reads and writes but complicates cross-shard operations. (3) Functional Partitioning — different data on different servers (users DB, orders DB); natural for microservices. (4) Caching Layer — Redis/Memcached to reduce the load on the database. Advantages: almost unlimited scaling. Disadvantages: architectural complexity, distributed transactions, eventual consistency. Recommendation: start with vertical scaling; move to horizontal scaling when the limit is reached or high availability is required.',
    explanation:
      'Scaling a database is often the most difficult part of scaling a system. Read replicas are the first step for most applications (a read-heavy workload). Sharding is a last resort that requires significant architectural changes. Caching often gives a 10x improvement with minimal changes.',
  },
  {
    id: 'sa-databases-050',
    block: 'sa',
    topic: 'databases',
    topicLabel: 'Databases',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is a CTE (Common Table Expression) in SQL?',
    options: [
      'A data type for storing temporary values',
      'A temporary named query result that can be used in the main query',
      'A data encryption mechanism',
      'A table integrity constraint',
    ],
    correctIndex: 1,
    explanation:
      'A CTE (Common Table Expression) is a temporary named result set defined using the WITH clause. It exists only within a single query. Syntax: WITH cte_name AS (SELECT ...) SELECT * FROM cte_name. Advantages: improving the readability of complex queries, reusing a subquery, recursive queries (WITH RECURSIVE) for hierarchical data. Example of a recursive CTE: traversing a category tree, finding all subordinates of an employee. A CTE is materialized (PostgreSQL) or inlined (MySQL) — depending on the optimizer.',
  },
];
