import type { Question } from '@/data/types';

export const capTheoremQuestions: Question[] = [
  {
    id: 'sd-cap-001',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What does the CAP theorem state?',
    options: [
      'A distributed system can simultaneously provide high speed, security, and scalability',
      'A distributed system can simultaneously guarantee only two of three properties: consistency, availability, and partition tolerance',
      'Any system must be designed considering C (Caching), A (API), and P (Performance)',
      'Data consistency is always more important than system availability',
    ],
    correctIndex: 1,
    explanation:
      "The CAP theorem (Brewer's theorem) states that a distributed system cannot simultaneously guarantee all three properties: Consistency (all nodes see the same data), Availability (every request receives a response), and Partition Tolerance (the system keeps working when the network link between nodes is broken). Since network partitions are inevitable in distributed systems, in practice the choice is between CP (consistency + partition tolerance) and AP (availability + partition tolerance).",
  },
  {
    id: 'sd-cap-002',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'middle',
    type: 'open',
    question: 'Give examples of CP and AP systems. Explain why they made that particular choice.',
    sampleAnswer:
      'CP systems (Consistency + Partition Tolerance): 1) Apache ZooKeeper -- a coordination service for distributed systems. It guarantees linearizable consistency: all clients see the same state. During a network partition, the minority of nodes stops serving requests (sacrificing availability). This is critical because ZooKeeper is used for leader election and distributed locks, where consistency is mandatory. 2) MongoDB (with majority write concern) -- provides strict consistency for writes acknowledged by a majority of replicas. AP systems (Availability + Partition Tolerance): 1) Cassandra -- during a network partition it keeps serving requests on all available nodes, even if data may be temporarily out of sync. This suits systems where availability matters more than accuracy (news feeds, counters). 2) DynamoDB -- by default provides eventual consistency with an option for strongly consistent reads. The AP choice is driven by Amazon’s requirement for 99.99% availability.',
    explanation:
      'It is important to understand that the CAP theorem describes system behavior only when a network partition is present. In normal operation a system can provide all three properties. Also, many modern systems let you tune the balance between C and A at the level of individual operations (tunable consistency). For example, Cassandra lets you set the consistency level (ONE, QUORUM, ALL) per request.',
  },
  {
    id: 'sd-cap-003',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'senior',
    type: 'open',
    question: 'What limitations and criticisms does the CAP theorem have? What is PACELC as an extension of it?',
    sampleAnswer:
      'Criticism of the CAP theorem: 1) Binary nature: CAP presents properties as binary (present/absent), but in practice there is a spectrum of consistency levels (strong, eventual, causal, read-your-writes) and availability (99.9%, 99.99%). 2) Rarity of partitions: network partitions happen rarely, and CAP says nothing about the trade-offs during normal operation. 3) It does not account for latency: even without a partition, strong consistency requires coordination between nodes, which increases latency. PACELC is an extension of CAP: if there is a Partition (P), choose between Availability (A) and Consistency (C); else (E), choose between Latency (L) and Consistency (C). For example: Cassandra -- PA/EL (during a partition it chooses availability, without a partition -- low latency). MongoDB -- PC/EC (during a partition it chooses consistency, and consistency without a partition too). DynamoDB -- PA/EL by default, PC/EC for consistent reads.',
    explanation:
      'PACELC, proposed by Daniel Abadi, gives a more precise model for describing the behavior of distributed systems. It explains why Cassandra and DynamoDB (both AP under CAP) behave differently in normal operation: Cassandra is optimized for low latency (EL), while systems like Spanner are optimized for strong consistency (EC) even without partitions, but at the cost of higher latency.',
  },
  {
    id: 'sd-cap-004',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is eventual consistency?',
    options: [
      'Data is always consistent across all nodes at any moment in time',
      'In the absence of new updates, all replicas will eventually converge to the same data state',
      'Data never becomes consistent in a distributed system',
      'Consistency is achieved only after restarting all nodes of the system',
    ],
    correctIndex: 1,
    explanation:
      'Eventual consistency is a consistency model that guarantees that if data updates stop, after some time all replicas will converge to the same state. Between an update and full synchronization, different nodes may return different versions of the data. This model is used in AP systems (DynamoDB, Cassandra, DNS). It provides high availability and low latency, but requires the application to be able to work with potentially stale data.',
  },
  {
    id: 'sd-cap-005',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What does the letter "P" (Partition Tolerance) mean in the CAP theorem?',
    options: [
      'The system can split data across tables (partitioning)',
      'The system keeps working even if the connection between some network nodes is broken',
      'The system supports parallel request processing',
      'The system protects data from unauthorized access',
    ],
    correctIndex: 1,
    explanation:
      'Partition Tolerance means the system keeps functioning even if the network connection between some nodes is lost (network partition). In distributed systems, network partitions are inevitable (a cable is cut, a switch fails, a data center becomes unreachable), so P is a mandatory property for any distributed system. In practice the question is: what to do during a partition -- sacrifice consistency (AP) or availability (CP)?',
  },
  {
    id: 'sd-cap-006',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'junior',
    type: 'quiz',
    question: 'Which system is an example of a CP system (Consistency + Partition Tolerance)?',
    options: [
      'Apache Cassandra',
      'DNS',
      'Apache ZooKeeper',
      'Amazon DynamoDB (in default mode)',
    ],
    correctIndex: 2,
    explanation:
      'Apache ZooKeeper is a classic example of a CP system. It is used for coordinating distributed systems (leader election, distributed locks, configuration storage), where consistency is critically important. During a network partition, nodes that end up in the minority stop serving requests (sacrificing availability) to guarantee that all clients see the same state. Cassandra and DynamoDB are examples of AP systems; DNS is also AP (different servers may return different records during an update).',
  },
  {
    id: 'sd-cap-007',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'junior',
    type: 'quiz',
    question: 'Why is it impossible to give up Partition Tolerance (P) in a distributed system?',
    options: [
      'Because it is the cheapest property to implement',
      'Because network failures are inevitable in distributed systems and the system must be able to cope with them',
      'Because Partition Tolerance improves performance',
      'Because without P it is impossible to implement data encryption',
    ],
    correctIndex: 1,
    explanation:
      'In the real world, network partitions are inevitable: hardware fails, cables get damaged, routers get overloaded, data centers lose connectivity. A system that is not partition-tolerant stops working on any network failure, which is unacceptable for distributed systems. Therefore, in practice the choice is not between C, A, and P, but between CP and AP: during a network partition, sacrifice consistency (keep serving requests with potentially stale data) or availability (refuse service until connectivity is restored).',
  },
  {
    id: 'sd-cap-008',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What are quorum reads/writes in distributed systems?',
    options: [
      'Reading and writing data only by the leader node',
      'A mechanism where an operation is considered successful when acknowledged by a majority of nodes (W + R > N)',
      'Reading data in parallel from all nodes simultaneously',
      'Writing data with encryption on each node',
    ],
    correctIndex: 1,
    explanation:
      'The quorum approach defines how many nodes must acknowledge an operation. With N nodes, W (write quorum) is the minimum number of acknowledgments for a write, and R (read quorum) is the minimum number of responses for a read. If W + R > N, then a read is guaranteed to overlap with at least one node holding the latest write, providing strong consistency. Examples: N=3, W=2, R=2 (strong consistency); N=3, W=1, R=1 (eventual consistency, maximum availability). Cassandra lets you tune W and R per request: QUORUM, ONE, ALL.',
  },
  {
    id: 'sd-cap-009',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'middle',
    type: 'open',
    question: 'What is linearizability and how does it differ from other consistency models?',
    sampleAnswer:
      'Linearizability is the strictest consistency model. It guarantees that all operations appear to execute instantaneously at some point between the start and completion of the operation, and that all clients see the same order of operations. This creates the illusion of a single copy of the data. Comparison with other models: 1) Sequential consistency: all clients see the same order of operations, but it may differ from the actual real-time order. 2) Causal consistency: guarantees order only for causally related operations (if A happened before B and B depends on A, everyone sees A before B). 3) Eventual consistency: guarantees only eventual convergence, with no ordering guarantees. Linearizability is expensive: it requires coordination between nodes (usually via consensus protocols like Raft/Paxos), which increases latency. It is provided by: ZooKeeper, etcd, Google Spanner (via TrueTime).',
    explanation:
      'Linearizability is necessary for: leader election (only one node should become leader), distributed locks, uniqueness constraints (two users must not claim the same username). For most applications (news feeds, product catalogs) weaker consistency models suffice, which improves performance and availability.',
  },
  {
    id: 'sd-cap-010',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is the "Read Your Writes" consistency model?',
    options: [
      'A model that guarantees all users see all writes in real time',
      'A model that guarantees a user always sees their own write on a subsequent read after writing data',
      'A model in which reads are always faster than writes',
      'A model in which data is written only after acknowledgment by all nodes',
    ],
    correctIndex: 1,
    explanation:
      'Read Your Writes (RYW) is a consistency model in which, after performing a write, a client is guaranteed to see the result of that write on subsequent reads. Without RYW, a user might update their profile, reload the page, and see old data (because the read hit a replica that has not yet received the update). Implementation: 1) Sticky sessions -- pinning a client to a single node. 2) Reading from the master after a write (within a short window). 3) Including the version/timestamp of the client’s last write in the read request. RYW is a compromise between strong consistency and eventual consistency, often sufficient for good UX.',
  },
  {
    id: 'sd-cap-011',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'senior',
    type: 'open',
    question: 'How do vector clocks work and why are they needed in distributed systems?',
    sampleAnswer:
      'Vector clocks are a mechanism for determining the causal ordering of events in distributed systems. Each node maintains a vector of N counters (one per node in the system). Rules: 1) On a local event, the node increments its own counter by 1. 2) When sending a message, the node increments its own counter and attaches the current vector to the message. 3) When receiving a message, the node updates each element of its vector to max(its own, received) and increments its own counter. Comparing vectors: if all elements of vector A are <= the corresponding elements of B and at least one is strictly smaller, then A happened before B (A -> B). If neither vector dominates, the events are concurrent (no causal relationship). Application in Dynamo-style systems: on a conflict (concurrent writes) the system keeps both versions and asks the client to resolve the conflict (as Amazon DynamoDB does) or uses automatic resolution (Last Writer Wins by timestamp).',
    explanation:
      'Vector clocks solve the problem of ordering events, which ordinary physical clocks cannot solve because of the impossibility of perfectly synchronizing clocks between nodes (a problem described by Leslie Lamport). In practice, full vector clocks are rarely used because of their size (they grow with the number of nodes). Alternatives: Lamport timestamps (simpler, but less information), Dotted Version Vectors (an optimization for stores like Riak).',
  },
  {
    id: 'sd-cap-012',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is the Raft algorithm and what problem does it solve?',
    options: [
      'A load balancing algorithm across servers',
      'A consensus algorithm that lets a group of nodes agree on the same value, even if some nodes have failed',
      'A data encryption algorithm in distributed systems',
      'A data compression algorithm to reduce network traffic',
    ],
    correctIndex: 1,
    explanation:
      'Raft is a distributed consensus algorithm designed as a more understandable alternative to Paxos. It lets a group of nodes (a cluster) reach agreement on a sequence of operations, even if some nodes have failed (up to (N-1)/2 for N nodes). Raft splits the problem into three subproblems: 1) Leader Election: one node is elected leader, and all writes go through it. 2) Log Replication: the leader replicates operations to follower nodes. 3) Safety: guarantees that all nodes apply the same operations in the same order. Raft is used in etcd (Kubernetes storage), CockroachDB, Consul, TiKV.',
  },
  {
    id: 'sd-cap-013',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'senior',
    type: 'open',
    question: 'What is the split-brain problem in distributed systems and how is it solved?',
    sampleAnswer:
      'Split-brain is a situation where a network partition divides the cluster into two (or more) parts, and each part considers itself the only working one and keeps accepting writes. This leads to data conflicts: two parts of the cluster independently modify the same data, and when connectivity is restored the data is out of sync. Solutions: 1) Quorum: only the part with a majority of nodes (> N/2) may continue operating. The minority goes read-only or stops serving requests. 2) Fencing tokens: the leader receives a monotonically increasing token. The store rejects requests with an old token, preventing the "zombie leader" problem. 3) STONITH (Shoot The Other Node In The Head): forcibly powering off minority nodes via IPMI/BMC. 4) Witness/Arbiter node: an additional lightweight node that stores no data but participates in voting, ensuring an odd number of votes. 5) Automatic conflict resolution: CRDTs (Conflict-free Replicated Data Types) -- data structures that mathematically guarantee convergence without coordination.',
    explanation:
      'Split-brain is one of the most dangerous situations in distributed systems, especially for CP systems (databases, coordination services). Incorrect handling of split-brain can lead to data loss or corruption. Kubernetes uses etcd with Raft consensus, which requires a quorum and automatically prevents split-brain.',
  },
  {
    id: 'sd-cap-014',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'middle',
    type: 'open',
    question: 'What is the PACELC theorem and how does it complement CAP?',
    sampleAnswer:
      'PACELC is an extension of the CAP theorem proposed by Daniel Abadi. The statement: if there is a network Partition (P), choose between Availability (A) and Consistency (C); else (E), in normal operation, choose between Latency (L) and Consistency (C). CAP says nothing about the behavior of a system without partitions, and PACELC fills this gap. Classification of systems: PA/EL -- during a partition it chooses availability, without a partition -- low latency. Examples: Cassandra, DynamoDB (by default). PC/EC -- during a partition it chooses consistency, and consistency without a partition too. Examples: MongoDB (with majority), Google Spanner. PA/EC -- during a partition it chooses availability, but guarantees consistency without a partition. Example: some MongoDB configurations. PC/EL -- during a partition it chooses consistency, but optimizes latency without a partition. Example: PNUTS (Yahoo).',
    explanation:
      'PACELC explains why systems classified the same under CAP behave differently. For example, Cassandra and VoltDB are both partition-tolerant, but Cassandra is optimized for latency (EL), while VoltDB is optimized for consistency (EC). PACELC helps architects choose a database more precisely, considering system behavior not only in failure conditions but also in normal conditions.',
  },
  {
    id: 'sd-cap-015',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'junior',
    type: 'open',
    question: 'What do the properties C (Consistency) and A (Availability) mean in the CAP theorem? Give simple examples.',
    sampleAnswer:
      'Consistency means that all nodes of the system return the same data at the same moment in time. If one node wrote the value "X=5", then any node receiving a read request for X must return 5 (or a newer value). Example: a bank account -- after funds are debited, any balance query must show the up-to-date amount, otherwise a double debit is possible. Availability means that every request to a working node of the system receives a response (not necessarily with the freshest data). The system does not refuse service even if some nodes are unavailable. Example: DNS -- even if a root server has updated a record, caching servers keep responding with old data instead of failing. A social network -- it is better to show the feed with a small update delay than to show nothing.',
    explanation:
      'In practice the choice between C and A depends on business requirements. Financial systems (banks, payments) usually choose CP, while social networks and content platforms choose AP. It is important to understand that CAP describes behavior only during a network partition; in normal operation a system can provide both C and A.',
  },
  {
    id: 'sd-cap-016',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is Causal Consistency?',
    options: [
      'A model in which all data is always consistent on all nodes',
      'A model in which causally related operations are seen by all nodes in the correct order, while concurrent operations may be seen in different orders',
      'A model in which data becomes consistent only after a system restart',
      'A model in which consistency is achieved by locking all nodes',
    ],
    correctIndex: 1,
    explanation:
      'Causal Consistency is a consistency model that guarantees that causally related operations are seen by all nodes in the same order. If operation B depends on the result of operation A (for example, a reply to a comment), then all nodes will see A before B. But if operations are independent (two independent comments), different nodes may see them in different orders. Causal Consistency is stronger than eventual consistency but weaker than linearizability. It provides a good balance between consistency and performance. It is implemented via: vector clocks, logical timestamps, dependency tracking. Example: MongoDB has supported causal consistency sessions since version 3.6.',
  },
  {
    id: 'sd-cap-017',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'senior',
    type: 'quiz',
    question: 'How does Google Spanner provide global strong consistency in a geographically distributed deployment?',
    options: [
      'It uses classic Two-Phase Commit without optimizations',
      'It uses the TrueTime API with atomic and GPS clocks to assign globally ordered timestamps, providing external consistency',
      'It gives up consistency in favor of availability',
      'It stores all data in a single data center',
    ],
    correctIndex: 1,
    explanation:
      'Google Spanner is a unique distributed database that provides global strong consistency (external consistency -- even stronger than linearizability). The key innovation is the TrueTime API: special servers with atomic clocks and GPS receivers provide global time with a known error bound (usually < 7ms). Spanner assigns transactions a timestamp from TrueTime and waits until the error bound has passed (commit wait), guaranteeing that the timestamp order matches the actual order of events. This makes it possible to provide strong consistency without expensive consensus for reads (read-only transactions do not require coordination). The cost: increased write latency due to commit wait.',
  },
  {
    id: 'sd-cap-018',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'senior',
    type: 'open',
    question: 'What is anti-entropy in distributed systems and what mechanisms are used to synchronize replicas?',
    sampleAnswer:
      'Anti-entropy refers to mechanisms for maintaining data consistency between replicas in distributed systems, working in the background. The main approaches: 1) Read Repair: when reading data from multiple replicas (a quorum read), if a discrepancy is detected, the latest version is written to the stale replicas. Pro: requires no separate process. Con: works only for data that is being read. 2) Anti-Entropy Protocol (Merkle Trees): a periodic process that compares data between replicas using Merkle trees. Nodes exchange subtree hashes, allowing fast discovery of discrepancies without comparing all data. Used in Cassandra, DynamoDB. 3) Hinted Handoff: if the target node is unavailable during a write, another node stores the write in temporary storage (a hint) and delivers it when the target node recovers. 4) Gossip Protocol: nodes periodically exchange information with randomly chosen neighbors, spreading updates across the entire cluster (an epidemic algorithm).',
    explanation:
      'Anti-entropy is critically important for AP systems, where data can become out of sync during network partitions or node failures. Cassandra uses all four mechanisms: gossip to exchange cluster metadata, hinted handoff to deliver missed writes, read repair on reads, and Merkle tree anti-entropy (nodetool repair) for full synchronization.',
  },
  {
    id: 'sd-cap-019',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'junior',
    type: 'quiz',
    question: 'Which of the following systems is an example of an AP system (Availability + Partition Tolerance)?',
    options: [
      'PostgreSQL with synchronous replication',
      'Apache ZooKeeper',
      'Apache Cassandra',
      'etcd',
    ],
    correctIndex: 2,
    explanation:
      'Apache Cassandra is a classic example of an AP system. During a network partition it keeps serving requests on all available nodes, providing availability at the cost of potential data desynchronization (eventual consistency). Cassandra uses tunable consistency: you can configure the consistency level per request (ONE, QUORUM, ALL), which lets you balance between availability and consistency. ZooKeeper and etcd are CP systems (they use consensus and sacrifice availability). PostgreSQL with synchronous replication is a CP system.',
  },
  {
    id: 'sd-cap-020',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What are CRDTs (Conflict-free Replicated Data Types) and how do they provide consistency without coordination?',
    options: [
      'Special types of databases with built-in replication',
      'Data structures that mathematically guarantee replica convergence under concurrent updates without the need for coordination between nodes',
      'A data transfer protocol for minimizing conflicts on the network',
      'A data encryption method for secure replication',
    ],
    correctIndex: 1,
    explanation:
      'CRDTs (Conflict-free Replicated Data Types) are special data structures designed so that concurrent updates on different replicas can always be merged without conflicts. They mathematically guarantee eventual consistency without coordination between nodes. Types: G-Counter (increment-only counter), PN-Counter (counter with increment and decrement), G-Set (set with additions), OR-Set (set with additions and removals), LWW-Register (register with Last Writer Wins). Applications: collaborative editing (Google Docs-like), shopping carts, online counters, chats. Used in Riak, Redis (CRDT types), Automerge, Yjs.',
  },
  {
    id: 'sd-cap-021',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'junior',
    type: 'open',
    question: 'Give a real-life example illustrating the choice between consistency and availability.',
    sampleAnswer:
      'Example: an ATM. During a network partition (the ATM has lost its connection to the central server): CP choice: the ATM refuses service ("Service temporarily unavailable") until the connection is restored. This guarantees that operations are consistent. AP choice: the ATM allows withdrawing a limited amount (for example, up to $200) based on local data. Availability is preserved, but an overdraft is possible, which will be corrected after the connection is restored. Most ATMs choose a compromise: for small operations -- AP (availability with a limit), for large operations -- CP (online confirmation is required).',
    explanation:
      'This example shows that the choice between C and A is a business decision, not a purely technical one. The cost of unavailability (losing customers) vs. the cost of inconsistency (overdraft, fraud) determines the choice.',
  },
  {
    id: 'sd-cap-022',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What happens to a CA system (Consistency + Availability) during a network partition?',
    options: [
      'It keeps working normally',
      'It ceases to exist as a distributed system, since it ignores the partition',
      'It automatically switches to CP mode',
      'It becomes faster',
    ],
    correctIndex: 1,
    explanation:
      'A CA system is a system that provides consistency and availability but is not tolerant of network partitions. In practice this means the system runs on a single node or in a network where partitions are impossible (which is unrealistic for distributed systems). When a partition occurs, a CA system will either stop working or be forced to choose between C and A. Classic RDBMS (PostgreSQL single node) is an example of CA before replication is added.',
  },
  {
    id: 'sd-cap-023',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is Tunable Consistency?',
    options: [
      'The ability to change the consistency level for individual operations',
      'Automatic consistency tuning by the system',
      'Consistency that improves over time',
      'Consistency only for certain data types',
    ],
    correctIndex: 0,
    explanation:
      'Tunable Consistency lets you set the consistency level for each operation (read/write) separately. In Cassandra: ONE (write/read on one node), QUORUM (majority), ALL (all nodes). The formula R + W > N guarantees strong consistency (where R is read consistency, W is write consistency, N is the replication factor). Example: N=3, W=2, R=2 (strong); N=3, W=1, R=1 (eventual, max availability). This lets you balance between consistency, availability, and latency for each use case.',
  },
  {
    id: 'sd-cap-024',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'middle',
    type: 'open',
    question: 'Explain the formula R + W > N for achieving strong consistency. Give example configurations.',
    sampleAnswer:
      'The formula R + W > N is used in systems with quorum replication. N is the number of replicas of the data. W is the number of nodes that must acknowledge a write (write quorum). R is the number of nodes to read from (read quorum). If R + W > N, then the sets of write and read nodes necessarily overlap, guaranteeing that the reader sees the latest write. Examples for N=3: 1) W=2, R=2 (R+W=4>3): strong consistency, tolerates 1 node failure for read and write. 2) W=3, R=1 (R+W=4>3): fast reads, slow writes, any node can serve reads. 3) W=1, R=3 (R+W=4>3): fast writes, slow reads. 4) W=1, R=1 (R+W=2<3): eventual consistency, max availability and speed.',
    explanation:
      'DynamoDB, Cassandra, and Riak use this model. The choice of W and R depends on the load pattern: read-heavy (low R), write-heavy (low W), balanced (W=R=quorum). With W=ALL or R=ALL the system is less fault-tolerant -- a single node failure blocks operations.',
  },
  {
    id: 'sd-cap-025',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is Monotonic Reads?',
    options: [
      'Reading data strictly in chronological order',
      'A guarantee that a client will not see an older version of the data after it has seen a newer one',
      'Reading from a single node only',
      'Reading data only in ascending order of ID',
    ],
    correctIndex: 1,
    explanation:
      'Monotonic Reads is a consistency model guaranteeing that if a client has read version X of the data, subsequent reads will return X or a newer version, never an older one. Without this guarantee, when reading from different replicas a client may observe "jumps backward in time". Example of a violation: a user sees a comment, refreshes the page, and the comment disappears (it hit a lagging replica). Implementation: sticky sessions (always read from one replica) or passing the version of the last read in the request.',
  },
  {
    id: 'sd-cap-026',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'senior',
    type: 'open',
    question: 'Describe the Paxos algorithm. What problem does it solve?',
    sampleAnswer:
      'Paxos is an algorithm for reaching consensus in a distributed system, proposed by Leslie Lamport. It lets a group of nodes agree on a single value, even if some nodes have failed. Roles: Proposers (propose values), Acceptors (vote), Learners (learn the result). Basic Paxos phases: 1) Prepare: a Proposer sends Prepare(n) with proposal number n. An Acceptor responds with Promise if n is greater than all previously seen numbers, and reports any previously accepted values. 2) Accept: the Proposer, having received a majority of Promises, sends Accept(n, v) with a chosen value v (either its own or one already accepted earlier). 3) An Acceptor accepts if it has not Promised a higher number. 4) A value is chosen when a majority of Acceptors have accepted it. Multi-Paxos optimizes for a sequence of values (a log). Paxos is hard to understand and implement, which is why Raft appeared.',
    explanation:
      'Paxos is provably correct, but its complexity has spawned many variations and bugs in implementations. Lamport wrote a paper "Paxos Made Simple", which is still considered complex. Raft was created as an understandable alternative. Google Chubby and Apache ZooKeeper use variants of Paxos.',
  },
  {
    id: 'sd-cap-027',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'senior',
    type: 'quiz',
    question: 'Which guarantee is violated when using Last Writer Wins (LWW) for conflict resolution?',
    options: [
      'Availability',
      'Causal consistency -- concurrent updates may be lost, even if they do not logically conflict',
      'Partition tolerance',
      'Durability',
    ],
    correctIndex: 1,
    explanation:
      'Last Writer Wins (LWW) resolves conflicts by choosing the value with the later timestamp. The problem: if two users simultaneously update different fields of the same document, one update will be lost even though they do not logically conflict. Example: User A sets name="Alice", User B sets email="bob@test.com" simultaneously. With LWW one of the updates will disappear. Alternatives: CRDTs (merge at the field level), application-level conflict resolution, vector clocks for detecting conflicts.',
  },
  {
    id: 'sd-cap-028',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'middle',
    type: 'open',
    question: 'What are Sloppy Quorum and Hinted Handoff? How do they affect availability?',
    sampleAnswer:
      'Sloppy Quorum is a quorum variation in which a write can be acknowledged by nodes that are not the "home" replicas for the key, if the home replicas are unavailable. This increases availability: even if all 3 home replicas are offline, the write can go to other nodes in the cluster. Hinted Handoff is a mechanism in which a node that accepted a "foreign" write (a hint) stores it with a marker indicating the target node. When the target node returns, the hint is passed to it (handoff). This provides eventually consistent delivery of data to the correct replicas after recovery. Dynamo (Amazon), Cassandra, and Riak use these techniques. The trade-off: increased availability at the cost of temporarily violating the R+W>N guarantees.',
    explanation:
      'Sloppy Quorum is criticized for creating an illusion of consistency: the client receives a write acknowledgment, but the data may be temporarily unreadable from the "correct" replicas. Riak with W=quorum + sloppy quorum can be less consistent than it seems.',
  },
  {
    id: 'sd-cap-029',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'junior',
    type: 'quiz',
    question: 'Which type of replication provides RPO=0 (zero data loss)?',
    options: [
      'Asynchronous replication',
      'Synchronous replication',
      'Lazy replication',
      'Periodic scheduled replication',
    ],
    correctIndex: 1,
    explanation:
      'Synchronous replication guarantees that data is written to a replica before acknowledging the client. RPO (Recovery Point Objective) = 0, since there is no window of data loss. The downside: increased write latency (waiting for the replica’s response) and reduced availability (if the replica is unavailable, writes are blocked). Asynchronous replication has RPO > 0: data can be lost between the moment of writing on the master and replication to the slave.',
  },
  {
    id: 'sd-cap-030',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'senior',
    type: 'open',
    question: 'How does the leader election algorithm in Raft work? Describe the process.',
    sampleAnswer:
      'Leader election in Raft: 1) All nodes start as Followers. 2) Each follower has an election timeout (random, 150-300ms). 3) If a follower does not receive a heartbeat from the leader before the timeout, it becomes a Candidate. 4) The Candidate increments its term (epoch), votes for itself, and sends RequestVote to all nodes. 5) A node votes for the candidate if: a) the candidate’s term >= its own, b) it has not yet voted in this term, c) the candidate’s log is at least as up to date. 6) A candidate that receives a majority of votes becomes the Leader. 7) The Leader sends heartbeats, preventing new elections. On a split vote (nobody got a majority) -- a new round with an incremented term and new random timeouts. Safety: at most one leader per term.',
    explanation:
      'Random election timeouts prevent persistent split votes. Raft guarantees that the leader has all committed entries, thanks to the log up-to-dateness requirement during voting. etcd, CockroachDB, and Consul use Raft.',
  },
  {
    id: 'sd-cap-031',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is Byzantine Fault Tolerance (BFT)?',
    options: [
      'Tolerance to physical damage of servers',
      'The ability of a system to operate correctly even in the presence of nodes behaving maliciously or unpredictably',
      'Protection against DDoS attacks',
      'Data encryption between nodes',
    ],
    correctIndex: 1,
    explanation:
      'Byzantine Fault Tolerance (BFT) is the ability of a system to reach consensus and operate correctly even if some nodes behave maliciously (send false data, contradictory messages to different nodes). It requires 3f+1 nodes to tolerate f Byzantine nodes. Examples: PBFT (Practical BFT), blockchain algorithms (PoW, PoS). Classic algorithms (Paxos, Raft) assume only crash-faults (a node either works correctly or is silent), not Byzantine faults.',
  },
  {
    id: 'sd-cap-032',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What mechanism does CockroachDB use to provide serializable isolation in a distributed system?',
    options: [
      'Two-Phase Locking (2PL)',
      'Hybrid Logical Clocks (HLC) + Raft consensus for each range',
      'Optimistic Concurrency Control only',
      'Single-master replication',
    ],
    correctIndex: 1,
    explanation:
      'CockroachDB uses a combination of: Hybrid Logical Clocks (HLC) for ordering events (a combination of physical and logical clocks), Raft consensus for replicating each range (a subset of the data), MVCC for transaction isolation, serializable snapshot isolation (SSI) for detecting write skew. This makes it possible to achieve serializable isolation in a geo-distributed system without TrueTime (as Spanner has). CockroachDB is a CP system, sacrificing availability during a partition.',
  },
  {
    id: 'sd-cap-033',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'middle',
    type: 'open',
    question: 'What are Lamport Timestamps and what problem do they solve?',
    sampleAnswer:
      'Lamport Timestamps are a logical clock mechanism proposed by Leslie Lamport for determining the order of events in a distributed system. The problem: physical clocks on different nodes cannot be perfectly synchronized, so wall-clock time cannot be relied on for ordering events. Rules: 1) Each process has a counter LC, initially 0. 2) Before each event: LC = LC + 1. 3) When sending a message: include LC in the message. 4) When receiving a message: LC = max(LC, received_LC) + 1. Guarantee: if event A happened before B (A -> B), then LC(A) < LC(B). Limitation: the converse is not true -- LC(A) < LC(B) does not mean A -> B (the events may be concurrent). To distinguish concurrent events, Vector Clocks are needed.',
    explanation:
      'Lamport Timestamps are sufficient for total ordering of events (assigning a unique order), but not for determining concurrency. Amazon DynamoDB used vector clocks but moved to a simpler scheme. Google Spanner uses TrueTime for real physical timestamps.',
  },
  {
    id: 'sd-cap-034',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'junior',
    type: 'quiz',
    question: 'Which consistency model is used in DNS?',
    options: [
      'Strong consistency',
      'Linearizability',
      'Eventual consistency',
      'Sequential consistency',
    ],
    correctIndex: 2,
    explanation:
      'DNS uses eventual consistency. When a DNS record is updated, the change gradually propagates through the hierarchy of DNS servers and caches. During the TTL (Time To Live), different users may get different values. DNS is a classic example of an AP system: it is always available (resolvers always respond), tolerant of partitions (works even if some servers fail), but does not guarantee immediate consistency (old records in caches).',
  },
  {
    id: 'sd-cap-035',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'senior',
    type: 'open',
    question: 'Describe the conflict resolution approach in Amazon DynamoDB. How are conflicting writes handled?',
    sampleAnswer:
      'DynamoDB originally (Dynamo paper, 2007) used vector clocks to detect conflicts and returned all conflicting versions to the client for resolution (semantic reconciliation). Example: a shopping cart -- on a conflict, merge items from both versions. Modern DynamoDB (AWS) has simplified the model: 1) Single-item operations by default use Last Writer Wins based on timestamp. 2) DynamoDB Transactions (added in 2018) provide serializable ACID for operations across multiple items. 3) Conditional writes (PutItem with ConditionExpression) let you implement optimistic locking. The choice: LWW for simplicity (if losing concurrent updates is acceptable), conditional writes for critical data, transactions for ACID requirements.',
    explanation:
      'The evolution of DynamoDB shows the trade-off between simplicity and flexibility. The original vector clocks + client-side resolution was too complex for most use cases. LWW is simpler but can lose data. Transactions solve the problem but are more expensive.',
  },
  {
    id: 'sd-cap-036',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is a Fencing Token and what problem does it solve?',
    options: [
      'A token for encrypting data between nodes',
      'A monotonically increasing identifier that prevents operations from a stale leader (zombie leader)',
      'A token for authenticating clients',
      'A network partition identifier',
    ],
    correctIndex: 1,
    explanation:
      'A Fencing Token is a monotonically increasing number issued when acquiring a lock or being elected leader. The problem: a node acquired a lock, then froze (GC pause, network delay), the lock expired, and another node acquired the lock. The first node "wakes up" and tries to write data, unaware that it lost the lock (zombie leader). The solution: every operation against the store includes a fencing token. The store rejects operations with an old token. Example: token=33 was issued to the old leader, token=34 to the new one. The store accepts only operations with token >= 34.',
  },
  {
    id: 'sd-cap-037',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'senior',
    type: 'open',
    question: 'How does Google Spanner achieve external consistency using TrueTime?',
    sampleAnswer:
      'External consistency (stronger than linearizability): if transaction T1 commits before T2 starts, then T1 must be visible to T2 on all nodes. The problem: it is impossible to determine "before" without synchronized clocks. The TrueTime API: TT.now() returns an interval [earliest, latest] with the guarantee that the real time lies within it. Implementation: atomic clocks and GPS receivers in each data center, with an error bound usually < 7ms. Commit wait: Spanner assigns a transaction a commit timestamp s and waits until TT.after(s) becomes true (the real time has provably passed s). This guarantees that transactions with an earlier timestamp are visible to all. The cost: additional write latency (~7ms). Read-only transactions require no coordination -- a safe timestamp is chosen and data is read from any replica.',
    explanation:
      'TrueTime is a unique Google innovation that requires special hardware. CockroachDB achieves similar guarantees with HLC, but with larger clock skew may require retries. Spanner showed that distributed serializable transactions are possible at global scale.',
  },
  {
    id: 'sd-cap-038',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is a Lease in distributed systems?',
    options: [
      'A permanent right of ownership over a resource',
      'A temporary right to exclusive access to a resource with automatic expiration',
      'A contract between services about a data format',
      'A data replication mechanism',
    ],
    correctIndex: 1,
    explanation:
      'A Lease is a temporary right to exclusive access to a resource that automatically expires after a set time. The advantage over an ordinary lock: if the lease holder freezes or crashes, the lease expires and the resource becomes available to others. The holder must periodically renew the lease. Applications: leader election (the leader holds a lease on the role), distributed locks (ZooKeeper, etcd use lease-based locks), cache invalidation. Important: the holder must verify that the lease is still valid before critical operations (or use fencing tokens).',
  },
  {
    id: 'sd-cap-039',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'junior',
    type: 'open',
    question: 'Give examples of systems with strong consistency and systems with eventual consistency.',
    sampleAnswer:
      'Strong consistency: 1) Google Spanner -- a globally distributed database with serializable transactions. 2) etcd -- a key-value store for Kubernetes, uses Raft. 3) Apache ZooKeeper -- a coordination service with linearizable operations. 4) CockroachDB -- distributed SQL with serializable isolation. 5) PostgreSQL with synchronous replication. Eventual consistency: 1) Apache Cassandra -- a distributed database optimized for availability and write throughput. 2) Amazon DynamoDB (by default) -- managed NoSQL with eventually consistent reads. 3) DNS -- records propagate gradually through caches. 4) Amazon S3 -- object storage with eventual consistency for some operations. 5) Riak -- a distributed key-value store.',
    explanation:
      'The choice of model depends on application requirements. For financial transactions, inventory management -- strong consistency. For social feeds, view counters, recommendations -- eventual consistency is often sufficient and gives better performance.',
  },
  {
    id: 'sd-cap-040',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is Jepsen and what is it used for?',
    options: [
      'A consensus algorithm',
      'A framework for testing distributed systems for correctness under network failures',
      'A database with a linearizability guarantee',
      'A replication protocol',
    ],
    correctIndex: 1,
    explanation:
      'Jepsen is a framework and a series of tests created by Kyle Kingsbury (aka Aphyr) to verify the consistency guarantees of distributed systems. Jepsen injects network partitions, kills nodes, induces delays, and checks whether the system upholds its claimed guarantees. Jepsen has uncovered bugs in MongoDB, Cassandra, Elasticsearch, CockroachDB, Redis, and many other systems. The tests are published at jepsen.io. Jepsen has become a standard for verifying production-ready distributed systems.',
  },
  {
    id: 'sd-cap-041',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'middle',
    type: 'open',
    question: 'Explain the difference between strong consistency, sequential consistency, and eventual consistency.',
    sampleAnswer:
      '1) Strong consistency (linearizability): all operations appear to execute instantaneously at some point between their start and completion, and all clients see the same order. As if there were a single copy of the data. The strictest model. 2) Sequential consistency: all clients see the same order of operations, but this order may differ from the actual real-time order. Each client’s operations preserve their relative order (program order). Weaker than linearizability. 3) Eventual consistency: if updates stop, all replicas eventually converge to the same state. No guarantees about order or convergence time. The weakest model. Between them are intermediate models: causal consistency, read-your-writes, monotonic reads.',
    explanation:
      'In practice, most applications can work with eventual consistency, provided the UI/UX handles cases of desynchronization (indicators like "saving...", optimistic updates with rollback on error).',
  },
  {
    id: 'sd-cap-042',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What approach does Cassandra use to achieve consistency on reads?',
    options: [
      'Reads only from a single master node',
      'Read Repair: when reading from multiple replicas it detects discrepancies and asynchronously fixes stale replicas',
      'It uses two-phase commit for each read',
      'It caches all data in memory',
    ],
    correctIndex: 1,
    explanation:
      'Read Repair in Cassandra: when reading with a consistency level > ONE (for example, QUORUM), the coordinator requests data from multiple replicas. If versions diverge, the coordinator: 1) Returns the freshest version (by timestamp) to the client. 2) Asynchronously sends this version to the stale replicas to correct them. This is a "lazy" anti-entropy mechanism that works only for data being read. Additionally, Cassandra uses Anti-Entropy Repair (nodetool repair) for full synchronization via Merkle trees.',
  },
  {
    id: 'sd-cap-043',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is the Gossip Protocol and where is it used?',
    options: [
      'A protocol for encrypting messages',
      'An epidemic protocol for spreading information between nodes via random exchange',
      'A leader election protocol',
      'A synchronous replication protocol',
    ],
    correctIndex: 1,
    explanation:
      'The Gossip Protocol (epidemic protocol) is a method for spreading information in a distributed system. Each node periodically picks a random neighbor and exchanges information with it. Information spreads exponentially fast (like rumors or an epidemic). Properties: eventually consistent, scalable, fault-tolerant. Applications: membership detection (which nodes are alive), spreading cluster metadata, failure detection. Cassandra uses gossip to exchange information about the cluster topology and node states.',
  },
  {
    id: 'sd-cap-044',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'senior',
    type: 'open',
    question: 'How do you implement a distributed lock using Redis? What problems can arise?',
    sampleAnswer:
      'The simplest implementation: SET key value NX PX timeout (NX -- only if not exists, PX -- expiry in ms). Release: check the value (so as not to delete someone else’s lock) and DEL. Problems: 1) Clock drift: if the node holding the lock freezes longer than the timeout, the lock expires, another node acquires it, and the first one "wakes up" and keeps working, believing it still holds the lock. Solution: a fencing token. 2) Single Redis: if Redis fails, the lock is lost. 3) Redis Sentinel failover: a lock on the master may not be replicated to the slave before promotion -- two clients acquire the lock. Redlock: Martin Kleppmann’s algorithm for a Redis cluster -- acquiring the lock on a majority of independent masters. Criticism: Redlock is complex and does not guarantee correctness under certain failure modes. Recommendation: for critical locks use ZooKeeper/etcd with Raft.',
    explanation:
      'Distributed locks are one of the hardest problems. Martin Kleppmann (author of "Designing Data-Intensive Applications") and Salvatore Sanfilippo (creator of Redis) publicly debated the correctness of Redlock. The conclusion: if losing a lock could lead to a disaster, use a CP system (ZooKeeper, etcd), not Redis.',
  },
  {
    id: 'sd-cap-045',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'junior',
    type: 'quiz',
    question: 'Which system is an example of a CP system used to store Kubernetes configuration?',
    options: [
      'Redis',
      'MongoDB',
      'etcd',
      'Cassandra',
    ],
    correctIndex: 2,
    explanation:
      'etcd is a distributed key-value store used by Kubernetes to store the entire cluster state (pods, services, configmaps, secrets). etcd uses Raft consensus, providing strong consistency. During a network partition, the minority of etcd nodes stops serving requests (CP behavior). This is critical for Kubernetes: it is better to temporarily reject new commands than to allow a split-brain with conflicting states.',
  },
  {
    id: 'sd-cap-046',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'middle',
    type: 'open',
    question: 'What are Session Guarantees and what types of guarantees exist?',
    sampleAnswer:
      'Session Guarantees are a set of consistency guarantees provided within a single client session. Types: 1) Read Your Writes: after a write, a client sees its own write on subsequent reads. 2) Monotonic Reads: if a client has read version X, subsequent reads return X or a newer version, never an older one. 3) Monotonic Writes: a client’s writes are applied in the order they were issued. 4) Writes Follow Reads: if a client read X and then wrote Y, then Y is based on a version no older than X. Implementation: sticky sessions (pinning a client to one replica), passing a version vector/timestamp between requests, causal consistency protocols. These guarantees are weaker than strong consistency, but provide intuitive behavior for the user.',
    explanation:
      'Session guarantees are a compromise between eventual consistency (too weak for good UX) and strong consistency (too expensive). Many applications need only read-your-writes -- the user must see their own changes, while changes from others may arrive with a delay.',
  },
  {
    id: 'sd-cap-047',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is a Write-Ahead Log (WAL) and how does it provide durability?',
    options: [
      'A log for tracking user actions',
      'A technique in which changes are written to a log before being applied to the main storage, enabling recovery after a failure',
      'A system error journal',
      'A log for debugging',
    ],
    correctIndex: 1,
    explanation:
      'A Write-Ahead Log (WAL) is a technique for providing durability: before changing data in the main storage, the change is written to an append-only log on disk. After a failure, the system recovers by replaying uncommitted writes from the WAL. Advantages: sequential writes to the WAL are faster than random writes, and operation atomicity. Used in: PostgreSQL (WAL), MySQL (redo log), Kafka (log-structured storage), LSM-trees (LevelDB, RocksDB). In distributed systems, the WAL is replicated via consensus (the Raft log = WAL).',
  },
  {
    id: 'sd-cap-048',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'middle',
    type: 'quiz',
    question: 'Which consistency level in Cassandra guarantees reading the latest written version?',
    options: [
      'ONE',
      'LOCAL_ONE',
      'QUORUM (provided W=QUORUM for the write)',
      'ANY',
    ],
    correctIndex: 2,
    explanation:
      'In Cassandra, to guarantee reading the latest write you need: R + W > N, where N is the replication factor. With QUORUM for reads and writes: if N=3, QUORUM=2, then R=2, W=2, R+W=4>3 -- a strong consistency guarantee. The ONE level provides maximum availability but eventual consistency -- you may read stale data. LOCAL_QUORUM provides consistency within a data center. ANY allows a write even if all replicas are unavailable (hinted handoff) -- the weakest level.',
  },
  {
    id: 'sd-cap-049',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'senior',
    type: 'open',
    question: 'Explain the Write Skew problem and how serializable isolation prevents it.',
    sampleAnswer:
      'Write Skew is an anomaly that occurs under snapshot isolation when two transactions read overlapping data and make mutually exclusive writes. Example: two doctors are on call, with a rule that at least one must remain. Both simultaneously check "how many are on call?" -- see 2, decide to leave, and update their own record. Result: nobody is on call. Under snapshot isolation, both transactions see a snapshot where 2 are on call, and they do not conflict on writes (they write different rows). Serializable isolation prevents this: 1) Serializable Snapshot Isolation (SSI): detects dangerous read-write dependency patterns and aborts one transaction. 2) Actual serial execution: running transactions serially (Redis, VoltDB). 3) Two-Phase Locking (2PL): read locks prevent concurrent modification.',
    explanation:
      'Write skew is hard to detect at the application level. If the business logic has constraints spanning multiple rows, snapshot isolation is insufficient -- serializable is needed. PostgreSQL has supported SSI since version 9.1.',
  },
  {
    id: 'sd-cap-050',
    block: 'sd',
    topic: 'cap-theorem',
    topicLabel: 'CAP Theorem',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is External Consistency and how does it differ from Linearizability?',
    options: [
      'They are the same thing, just different names',
      'External consistency guarantees that the order of transactions matches their actual real-time order; linearizability is defined for individual operations',
      'External consistency is weaker than linearizability',
      'External consistency applies only to non-relational databases',
    ],
    correctIndex: 1,
    explanation:
      'External Consistency (strict serializability) is a guarantee for transactions: if transaction T1 commits before T2 starts in real time, then T1 must precede T2 in the serialization order. Linearizability is defined for individual operations (read/write), not transactions. External consistency = linearizability + serializability. Google Spanner provides external consistency thanks to TrueTime. This is the strictest consistency model, creating the illusion of a single sequential copy of the data for all observers.',
  },
];
