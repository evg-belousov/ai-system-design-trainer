import type { Question } from '@/data/types';

export const loadBalancingQuestions: Question[] = [
  {
    id: 'sd-lb-001',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is the primary task of a Load Balancer?',
    options: [
      'Encrypting traffic between the client and the server',
      'Distributing incoming requests across multiple servers',
      'Caching responses to speed up the response time',
      'Monitoring the health of servers',
    ],
    correctIndex: 1,
    explanation:
      "The primary task of a load balancer is to distribute incoming network traffic across multiple servers (backends). This increases the system's throughput and provides high availability and fault tolerance. Although load balancers often perform additional functions (SSL termination, health checks, caching), distributing the load is their primary task.",
  },
  {
    id: 'sd-lb-002',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'middle',
    type: 'quiz',
    question: 'Which load balancing algorithm is best suited for servers with different performance characteristics?',
    options: [
      'Round Robin -- requests are distributed in a circular fashion',
      'Weighted Round Robin -- requests are distributed according to server weights',
      'Random -- requests are sent to a random server',
      "IP Hash -- the server is chosen based on a hash of the client's IP address",
    ],
    correctIndex: 1,
    explanation:
      'Weighted Round Robin assigns each server a weight proportional to its performance. A server with a weight of 3 will receive three times as many requests as a server with a weight of 1. This allows for efficient use of a heterogeneous infrastructure where servers have different CPU, RAM, and performance characteristics. Plain Round Robin distributes the load evenly and does not account for differences in server capacity.',
  },
  {
    id: 'sd-lb-003',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'middle',
    type: 'open',
    question: 'Explain the difference between load balancing at the L4 and L7 layers of the OSI model. In which scenarios is each option preferable?',
    sampleAnswer:
      'L4 (transport layer) balancing works at the TCP/UDP level: the routing decision is made based on the IP address and port without analyzing the content of the request. It is faster, consumes fewer resources, and is suitable for high-load scenarios where content-based routing is not needed. L7 (application layer) balancing analyzes the content of the HTTP request: URL, headers, cookies. It allows routing requests based on the path (/api to one backend, /static to another), content-based routing, SSL termination, and header modification. L4 is preferable for non-HTTP protocols and for maximum performance. L7 is preferable for web applications that need flexible routing, A/B testing, and canary deployments.',
    explanation:
      'The choice between L4 and L7 balancing is a trade-off between performance and functionality. In practice, both layers are often used: L4 at the external perimeter to distribute traffic between clusters, and L7 inside the cluster for intelligent routing. Examples of L4: AWS NLB, HAProxy in TCP mode. Examples of L7: Nginx, HAProxy in HTTP mode, AWS ALB, Envoy.',
  },
  {
    id: 'sd-lb-004',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'senior',
    type: 'open',
    question: 'How do you ensure high availability of the load balancer itself? What happens if the load balancer fails?',
    sampleAnswer:
      'The load balancer itself can become a single point of failure (SPOF), so its fault tolerance must be ensured. The main approaches are: 1) Active-Passive (hot standby): two load balancers, one active and one on standby. The VRRP (Virtual Router Redundancy Protocol) is used to automatically switch the virtual IP (VIP) when the active node fails. The switchover time is a few seconds. 2) Active-Active: multiple load balancers operate simultaneously, with traffic distributed among them via DNS Round Robin or Anycast. This provides better resource utilization. 3) DNS-based failover: multiple A records point to different load balancers; when one fails, DNS is updated (but TTL can slow down the switchover). 4) Cloud solutions (AWS ELB, GCP LB): the provider automatically ensures fault tolerance.',
    explanation:
      'In practice, a combination of approaches is used for mission-critical systems. For example, Anycast DNS routes traffic to the nearest cluster, while inside the cluster an Active-Passive pair of load balancers handles the traffic. It is important to conduct regular drills (chaos engineering) for load balancer failures to verify that the switchover works correctly.',
  },
  {
    id: 'sd-lb-005',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is a health check in the context of load balancing?',
    options: [
      "Verification of the server's SSL certificate",
      'Periodic checking of the availability and operability of the servers behind the load balancer',
      'Analysis of the CPU and memory load of the servers',
      'Verification of the correctness of HTTP responses on the client side',
    ],
    correctIndex: 1,
    explanation:
      'A health check is a mechanism by which the load balancer periodically checks the availability and operability of the backend servers. This is usually an HTTP request to a special endpoint (for example, /health), a TCP connection check, or the execution of a script. If a server does not respond or responds with an error, the load balancer stops sending traffic to it until it recovers. This is a key mechanism for ensuring high availability.',
  },
  {
    id: 'sd-lb-006',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What are sticky sessions in the context of load balancing?',
    options: [
      'A mechanism for encrypting session data',
      'Binding a client to the same server throughout the entire session',
      'Automatic extension of the session lifetime',
      'Copying the session to all servers in the cluster',
    ],
    correctIndex: 1,
    explanation:
      'Sticky sessions (session affinity) is a mechanism in which the load balancer routes all requests from one client to the same backend server. It is implemented via a cookie (the load balancer adds a cookie with a server identifier) or by the client IP address. Sticky sessions are necessary when the application stores session state in the server memory. However, they create uneven load and make scaling difficult. The better alternative is to move the session state to external storage (Redis) and use stateless servers.',
  },
  {
    id: 'sd-lb-007',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'junior',
    type: 'quiz',
    question: 'Which load balancing algorithm routes each new request to the server with the fewest active connections?',
    options: [
      'Round Robin',
      'Weighted Round Robin',
      'Least Connections',
      'IP Hash',
    ],
    correctIndex: 2,
    explanation:
      'Least Connections is a balancing algorithm that routes a new request to the server with the fewest current active connections. It is better than Round Robin when requests have different processing durations: a server with long-running requests will receive fewer new ones. This is especially useful for WebSocket connections and long-running operations. There is also a Weighted Least Connections variant that accounts for server performance.',
  },
  {
    id: 'sd-lb-008',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is DNS-based load balancing?',
    options: [
      'Encrypting DNS queries to protect against attacks',
      'Distributing traffic across servers by returning different IP addresses in DNS responses',
      'Caching DNS records on the load balancer side',
      'Using DNS to monitor server health',
    ],
    correctIndex: 1,
    explanation:
      'DNS-based balancing works at the DNS resolution level: the DNS server returns different IP addresses (A records) for a single domain name, distributing clients across servers. The simplest implementation is DNS Round Robin (multiple A records are rotated). More advanced solutions (Route53, Cloudflare) take into account the client geography, server health, and load. The drawback is that DNS caching (TTL) slows down the response to failures.',
  },
  {
    id: 'sd-lb-009',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is connection draining (deregistration delay) in load balancing?',
    options: [
      'Removing all connections when the load balancer restarts',
      'Completing existing connections before taking a server out of service, without accepting new requests',
      'Limiting the maximum number of connections per server',
      'Redirecting connections from a slow server to a fast one',
    ],
    correctIndex: 1,
    explanation:
      'Connection draining is a mechanism for gracefully taking a server out of service. When a server is marked for removal (during a deployment, scale-down, or based on health check results), the load balancer stops routing new requests to it but allows time to finish processing the current ones. A timeout is usually configured (for example, 30 seconds), after which the remaining connections are forcibly closed. This prevents client errors during server updates or maintenance.',
  },
  {
    id: 'sd-lb-010',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is the main advantage of an L7 load balancer over an L4 one when deploying microservices?',
    options: [
      'Higher throughput',
      'Lower resource consumption',
      'The ability to route requests based on the URL path, headers, and request content',
      'No need for SSL certificates',
    ],
    correctIndex: 2,
    explanation:
      'An L7 load balancer analyzes the content of an HTTP request and can route based on the URL path (/api/users → user-service, /api/orders → order-service), headers (A/B testing by cookie), request method, and even the request body. This is critically important for a microservice architecture, where a single entry URL must be routed to different services. An L4 load balancer sees only the IP and port and cannot perform content-based routing.',
  },
  {
    id: 'sd-lb-011',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'middle',
    type: 'open',
    question: 'Compare HAProxy and Nginx in the role of load balancers. What are the key differences and use cases for each?',
    sampleAnswer:
      'HAProxy is a specialized load balancer and proxy, originally designed for this task. Advantages: high performance, detailed balancing configuration (many algorithms, health checks, ACLs), support for L4 and L7, a runtime API for management, and excellent monitoring (stats page). Nginx is a web server and reverse proxy with a load balancing feature. Advantages: it can simultaneously serve static content, perform SSL termination, caching, compression, rate limiting, support HTTP/2 and gRPC, and offers Lua scripting for custom logic. Differences: HAProxy is better for pure load balancing with advanced health checks and session persistence. Nginx is better as a general-purpose reverse proxy that combines balancing with static content and caching. In practice, Nginx is often used as an edge proxy (SSL termination, static content) and HAProxy for internal balancing between services.',
    explanation:
      'Both tools are widely used and have a mature ecosystem. In the Kubernetes world, Nginx is often seen as an Ingress Controller (nginx-ingress), while HAProxy serves as an alternative ingress. Envoy Proxy is gaining popularity as a modern alternative to both, especially in service mesh architectures (Istio).',
  },
  {
    id: 'sd-lb-012',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is Global Server Load Balancing (GSLB)?',
    options: [
      'Load balancing within a single data center',
      'Distributing traffic across multiple geographically distributed data centers',
      'Limiting load at a global level through rate limiting',
      'Balancing requests to a global database',
    ],
    correctIndex: 1,
    explanation:
      'GSLB (Global Server Load Balancing) is the distribution of traffic across multiple data centers or regions. It is usually implemented via DNS-based routing (AWS Route53, Cloudflare) taking into account: the geographic proximity of the client (geo-routing), data center health (failover), and the load on each data center. GSLB provides minimal latency for users worldwide and fault tolerance at the level of entire regions. Technologies: Anycast, GeoDNS, latency-based routing.',
  },
  {
    id: 'sd-lb-013',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'senior',
    type: 'open',
    question: 'Describe how to implement a zero-downtime deployment using a load balancer. What deployment strategies exist?',
    sampleAnswer:
      'A zero-downtime deployment is updating an application without interrupting user service. Strategies using a load balancer: 1) Rolling deployment: servers are updated one at a time. The load balancer takes a server out of rotation (connection draining), updates it, checks the health check, and returns it to rotation. 2) Blue-Green deployment: two identical environments (blue is the current one, green is the new one). After deploying to green and verifying it, the load balancer switches all traffic from blue to green. Rollback is instant -- just switch back. 3) Canary deployment: the new version is deployed to a small portion of servers (1-5%). The load balancer routes a fraction of the traffic to them. If the metrics are successful, the share is gradually increased to 100%. 4) Feature flags: without changing the infrastructure, the new code is activated for a portion of users via flags. All strategies require backward compatibility of the API and DB schema between versions.',
    explanation:
      'In practice, the choice of strategy depends on the risks and rollback speed. Blue-Green provides an instant rollback but requires double the resources. Canary minimizes the blast radius but complicates monitoring. Rolling is the most economical, but rollback is slower. Kubernetes natively supports Rolling and, through Istio/Argo Rollouts, Canary and Blue-Green deployments.',
  },
  {
    id: 'sd-lb-014',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'senior',
    type: 'quiz',
    question: 'Which load balancing algorithm is used when response time needs to be minimized?',
    options: [
      'Round Robin',
      'Random',
      'Least Response Time',
      'Source IP Hash',
    ],
    correctIndex: 2,
    explanation:
      'Least Response Time is an algorithm that routes a request to the server with the lowest current response time and the fewest active connections. It accounts for not only the number of connections (like Least Connections) but also the actual response speed of the server. This makes it possible to automatically redirect traffic away from degrading servers. It is implemented in Nginx (least_time), HAProxy (leastconn with observe-layer), and AWS ALB. The drawback is the need to constantly measure latency, which adds overhead.',
  },
  {
    id: 'sd-lb-015',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'senior',
    type: 'open',
    question: 'What is a Service Mesh and how is it related to load balancing in a microservice architecture? Give examples of implementations.',
    sampleAnswer:
      'A Service Mesh is an infrastructure layer for managing communication between microservices. Each service is given a sidecar proxy (usually Envoy) that intercepts all incoming and outgoing traffic. The Service Mesh takes care of: 1) Client-side load balancing -- the sidecar knows about all instances of the target service and balances between them. 2) Service discovery -- automatic discovery of new instances. 3) Circuit breaking -- protection against cascading failures. 4) Retry/timeout -- automatic retries and timeouts. 5) mTLS -- mutual authentication and traffic encryption. 6) Observability -- metrics, tracing, logging. Examples: Istio (based on Envoy), Linkerd, Consul Connect. A Service Mesh moves the balancing and resilience logic out of the application code and into the infrastructure, simplifying development but complicating operations.',
    explanation:
      'A Service Mesh has become the de facto standard for large microservice architectures in Kubernetes. However, it adds operational complexity and overhead (an extra hop through the sidecar, resource consumption). For small systems (up to 10-20 services), a simple Kubernetes Service with kube-proxy is usually sufficient.',
  },
  {
    id: 'sd-lb-016',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'junior',
    type: 'quiz',
    question: 'Where is a load balancer usually placed in a web application architecture?',
    options: [
      'Behind the application servers, in front of the database',
      'Between the clients (users) and the application servers',
      'Between the database and the cache',
      'On the client side in the browser',
    ],
    correctIndex: 1,
    explanation:
      'A load balancer is usually placed between the clients and the application servers, receiving all incoming traffic and distributing it across the backends. In complex architectures, several levels of balancing are used: 1) DNS level -- between users and data centers. 2) L4 load balancer -- at the entrance to the data center. 3) L7 load balancer -- in front of the application servers. 4) Internal load balancer -- between microservices.',
  },
  {
    id: 'sd-lb-017',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'middle',
    type: 'open',
    question: 'What types of health checks exist and how do you choose the right one for your service?',
    sampleAnswer:
      'Types of health checks: 1) TCP check -- checks whether a port is open. The simplest one, but it does not verify the operability of the application. 2) HTTP check -- sends an HTTP request to an endpoint (usually /health or /healthz) and checks the response code (200 OK). 3) Deep health check (readiness) -- checks not only the application but also its dependencies (DB, cache, external APIs). Returns a detailed status. 4) Shallow health check (liveness) -- checks that the application process is alive and can handle requests, without checking dependencies. The choice depends on the scenario: for a load balancer, a shallow check is better -- if the DB is unavailable, there is no point in taking all servers out of rotation. A deep check is useful for monitoring and alerting. In Kubernetes, the liveness probe determines whether a pod needs to be restarted, while the readiness probe determines whether traffic can be routed to it.',
    explanation:
      'Proper configuration of health checks is critical: too strict and they will cause false positives and cascading server shutdowns; too lenient and they will route traffic to non-working servers. Important parameters: interval (how often to check), timeout (how long to wait for a response), and threshold (how many failed checks before taking it out of rotation).',
  },
  {
    id: 'sd-lb-018',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is Consistent Hashing in the context of load balancing and why is it needed?',
    options: [
      'An encryption algorithm to protect traffic',
      'A method of distributing requests that minimizes redistribution when adding/removing servers',
      'A way to verify data integrity during transmission',
      'An algorithm for compressing HTTP requests',
    ],
    correctIndex: 1,
    explanation:
      'Consistent Hashing in load balancing is used to bind specific requests (for example, by cache key or user ID) to particular servers. When a server is added or removed, only ~1/N of the requests are redistributed, rather than all of them. This is especially important when balancing in front of a distributed cache: it minimizes the number of cache misses when the topology changes. It is used in Nginx (hash upstream), HAProxy, as well as in Memcached and Redis Cluster.',
  },
  {
    id: 'sd-lb-019',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is SSL/TLS termination on a load balancer?',
    options: [
      'Encrypting traffic between the load balancer and the backend servers',
      'Decrypting HTTPS traffic at the load balancer and passing unencrypted HTTP to the backends',
      'Generating SSL certificates on the fly',
      'Blocking unencrypted HTTP traffic',
    ],
    correctIndex: 1,
    explanation:
      'SSL/TLS termination is a process in which the load balancer decrypts incoming HTTPS traffic and passes the requests to the backend servers over plain HTTP. Advantages: it offloads the encryption/decryption load from the application servers, provides centralized certificate management, and allows the L7 load balancer to analyze the request content. To increase security, you can use SSL re-encryption -- re-encrypting the traffic between the load balancer and the backends (but this adds overhead).',
  },
  {
    id: 'sd-lb-020',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'senior',
    type: 'open',
    question: 'Explain the "Power of Two Random Choices" concept in load balancing. Why can it be better than Round Robin or Least Connections?',
    sampleAnswer:
      'Power of Two Random Choices is a balancing algorithm in which, for each request, two servers are chosen at random, and the request is routed to the less loaded of the two. Despite its simplicity, this algorithm provides exponentially better load distribution compared to purely random selection. Advantages: 1) It does not require global knowledge of the state of all servers (unlike Least Connections, which must track all connections). 2) It works in distributed systems without a central coordinator. 3) It is resilient to stale load information (race conditions). 4) It automatically avoids overloaded servers. Disadvantages compared to Least Connections: slightly less optimal distribution, but significantly simpler to implement for distributed systems. It is used in Envoy proxy, Netflix Ribbon, and is described in the work of Michael Mitzenmacher.',
    explanation:
      'Power of Two Random Choices is a classic example of how a small change in an algorithm (choosing from two random servers instead of one) yields a significant improvement. It is mathematically proven that the maximum load on a server is reduced from O(log n / log log n) to O(log log n). This principle is applied not only in balancing but also in hash tables (cuckoo hashing) and other distributed algorithms.',
  },
  {
    id: 'sd-lb-021',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is a reverse proxy and how does it differ from a load balancer?',
    options: [
      'A reverse proxy works on the client side, while a load balancer works on the server side',
      'A reverse proxy is a proxy server in front of the backends, and balancing is one of its functions',
      'A reverse proxy encrypts traffic, while a load balancer distributes it',
      'They are the same thing, there is no difference',
    ],
    correctIndex: 1,
    explanation:
      'A reverse proxy is a server that receives client requests and forwards them to backend servers. Load balancing is one of the functions of a reverse proxy. Other functions: SSL termination, caching, compression, DDoS protection, URL rewriting, and adding headers. Nginx and HAProxy can work as a reverse proxy with a balancing function.',
  },
  {
    id: 'sd-lb-022',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'junior',
    type: 'quiz',
    question: 'Which protocol is used to check the availability of a web server in an HTTP health check?',
    options: [
      'An FTP request to the server',
      'An HTTP GET request to a special endpoint (for example, /health)',
      'A Ping (ICMP) request',
      'An SSH connection to the server',
    ],
    correctIndex: 1,
    explanation:
      'An HTTP health check sends an HTTP GET request to a special endpoint (usually /health, /healthz, or /status). The server must return HTTP 200 OK if it is ready to handle requests. This makes it possible to check not only network availability but also the operability of the application. The health endpoint can check the database connection and the availability of dependent services.',
  },
  {
    id: 'sd-lb-023',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What happens to client requests if the load balancer detects that a server has failed a health check?',
    options: [
      'The load balancer will continue to send requests to that server',
      'The load balancer will remove the server from the pool and stop routing new requests to it',
      'The load balancer will reboot the problematic server',
      'The load balancer will notify the client of an error and stop working',
    ],
    correctIndex: 1,
    explanation:
      'On a failed health check, the load balancer removes the server from the pool (marks it as unhealthy) and stops routing new requests to it. Requests are distributed among the remaining healthy servers. The load balancer continues to periodically check the unhealthy server and returns it to the pool when the health check starts passing successfully.',
  },
  {
    id: 'sd-lb-024',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'junior',
    type: 'open',
    question: 'Explain the difference between an external and an internal load balancer. Where is each applied?',
    sampleAnswer:
      'An external (public) load balancer has a public IP address and receives traffic from the internet. It sits at the network boundary and routes user requests to internal servers. It usually performs SSL termination and DDoS protection. Examples: AWS ALB/NLB with a public IP, Cloudflare Load Balancing. An internal (private) load balancer has only a private IP and is used to balance traffic between internal services (microservices). It is not accessible from the internet. Examples: AWS Internal ALB, Kubernetes Service. A typical architecture: external LB → web servers → internal LB → API services → internal LB → database.',
    explanation:
      'The separation into external and internal load balancers is an important aspect of network security. Internal services (DB, cache, backend API) should not be directly accessible from the internet. Internal balancing also allows microservices to find each other (service discovery) and provides fault tolerance for internal communication.',
  },
  {
    id: 'sd-lb-025',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'middle',
    type: 'quiz',
    question: 'Which balancing algorithm uses a hash of the URL or a request header for routing?',
    options: [
      'Round Robin',
      'Least Connections',
      'URL Hash / Request Hash',
      'Random',
    ],
    correctIndex: 2,
    explanation:
      'URL Hash (or Request Hash) computes a hash of the URL, a header, or another request parameter and routes the request to the server corresponding to that hash. This provides: 1) cache locality -- identical requests go to the same server, increasing the local cache hit rate; 2) session affinity without cookies -- requests from one user (by IP or header) go to the same server. In Nginx: hash $request_uri; or hash $remote_addr.',
  },
  {
    id: 'sd-lb-026',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What are health check interval and threshold in load balancer settings?',
    options: [
      'Interval is the response time of the server, threshold is the maximum allowable latency',
      'Interval is the frequency of checks, threshold is the number of failed checks before removing the server',
      'Interval is the connection lifetime, threshold is the maximum number of connections',
      'Interval is the caching period, threshold is the cache size',
    ],
    correctIndex: 1,
    explanation:
      'Health check interval is how often the load balancer checks each server (for example, every 5 seconds). Threshold is the number of consecutive failed checks after which a server is considered unhealthy (for example, 3 failures). Similarly, the healthy threshold is the number of successful checks needed to return it to the pool. These parameters strike a balance between fast failure detection and resilience to temporary issues.',
  },
  {
    id: 'sd-lb-027',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'middle',
    type: 'open',
    question: 'How do you configure load balancing for gRPC services? How does it differ from HTTP balancing?',
    sampleAnswer:
      'gRPC uses HTTP/2 with multiplexing of requests over a single connection. This creates a problem for L4 balancing: a single long-lived connection is not distributed across servers. Solutions: 1) L7 balancing with HTTP/2 support -- the load balancer terminates HTTP/2 and distributes individual gRPC calls. Nginx (with the grpc module), Envoy, and AWS ALB support this. 2) Client-side balancing -- the gRPC client itself knows about all servers and distributes requests. It is used with service discovery (Consul, etcd). 3) Lookaside balancing (gRPC-LB) -- the client requests a list of backends from a dedicated service. 4) Service Mesh (Istio, Linkerd) -- the sidecar proxy performs balancing at the call level. Important: a gRPC health check uses the grpc.health.v1.Health service, not HTTP.',
    explanation:
      'gRPC balancing is a common problem when migrating to microservices. By default, Kubernetes kube-proxy does L4 balancing, which is inefficient for gRPC. The solution is to use a headless service + client-side balancing or a Service Mesh.',
  },
  {
    id: 'sd-lb-028',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is a Kubernetes Ingress and what role does it perform?',
    options: [
      'An internal load balancer between the pods of a single service',
      'An L7 load balancer that manages external access to cluster services based on URL rules',
      'A pod autoscaling mechanism',
      'A service discovery service in Kubernetes',
    ],
    correctIndex: 1,
    explanation:
      'A Kubernetes Ingress is an L7 load balancer that manages external HTTP/HTTPS traffic into the cluster. Ingress allows: routing by URL path (/api → service-a, /web → service-b), routing by hostname (api.example.com → service-a), and SSL termination. An Ingress Controller (nginx-ingress, traefik, AWS ALB Ingress) implements these rules. Without Ingress, each service would require a separate LoadBalancer.',
  },
  {
    id: 'sd-lb-029',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'middle',
    type: 'quiz',
    question: 'Which type of Kubernetes Service provides load balancing between pods inside the cluster?',
    options: [
      'NodePort',
      'LoadBalancer',
      'ClusterIP',
      'ExternalName',
    ],
    correctIndex: 2,
    explanation:
      'ClusterIP is the default Service type in Kubernetes. It creates a virtual IP address inside the cluster that is accessible only from within. kube-proxy provides balancing (usually IPVS or iptables) between the pods matching the service selector. ClusterIP is the foundation for internal communication between microservices. NodePort and LoadBalancer are used for external access.',
  },
  {
    id: 'sd-lb-030',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'middle',
    type: 'open',
    question: 'Describe the Circuit Breaker pattern in the context of load balancing between microservices. How does it prevent cascading failures?',
    sampleAnswer:
      'Circuit Breaker is a pattern that prevents cascading failures when a service is unavailable. Three states: 1) Closed -- requests pass through normally; errors are counted. 2) Open -- after the error threshold is exceeded, all requests are immediately rejected without attempting to call the service. This prevents the accumulation of requests and resource exhaustion. 3) Half-Open -- after a timeout, a test request is allowed through; if it succeeds, it transitions to Closed, if not, back to Open. Advantages: fast failure (no waiting for a timeout each time), protection against overloading a dying service, and the ability to fall back (return cached data or a stub). Implementations: Resilience4j, Polly, Hystrix (deprecated), Envoy/Istio circuit breaker.',
    explanation:
      'Circuit Breaker is an important resilience pattern. Without it, a single slow service can block all threads of the calling service, causing a cascading failure of the entire system. Netflix popularized this pattern through Hystrix.',
  },
  {
    id: 'sd-lb-031',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is Anycast and how is it used for global load balancing?',
    options: [
      'A multicast technology for sending data to multiple recipients',
      'A single IP address is announced from multiple geographic locations, and traffic goes to the nearest one',
      'A protocol for synchronizing data between data centers',
      'A method of encrypting traffic between load balancers',
    ],
    correctIndex: 1,
    explanation:
      'Anycast is a technique in which a single IP address is announced (via BGP) from multiple geographic points of presence. The network automatically routes traffic to the nearest point based on BGP metrics. Advantages: low latency (the nearest server), automatic failover (if a point fails, traffic goes to the next nearest one), and DDoS protection (the attack is distributed across the points). It is used by Cloudflare, Google DNS (8.8.8.8), AWS Global Accelerator, and most CDNs.',
  },
  {
    id: 'sd-lb-032',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'senior',
    type: 'quiz',
    question: 'Which HAProxy operating mode provides L4 balancing with minimal overhead?',
    options: [
      'HTTP mode',
      'TCP mode',
      'Health mode',
      'Proxy Protocol mode',
    ],
    correctIndex: 1,
    explanation:
      'TCP mode in HAProxy provides L4 balancing: HAProxy operates at the transport layer without analyzing packet content. This gives minimal overhead and maximum performance. It is suitable for: non-HTTP protocols (MySQL, Redis, gRPC without parsing), when content-based routing is not needed, and for SSL passthrough (SSL is terminated on the backend). HTTP mode analyzes HTTP requests and supports content-based routing, but it is slower.',
  },
  {
    id: 'sd-lb-033',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'senior',
    type: 'open',
    question: 'How do you implement weighted load balancing based on the current performance of servers (adaptive load balancing)?',
    sampleAnswer:
      'Adaptive (or dynamic) load balancing automatically adjusts server weights based on their current performance. Approaches: 1) Response time tracking -- the load balancer measures the response time of each server and reduces the weight of slow ones. Nginx Plus and HAProxy support this (observe layer7). 2) Active health scores -- a combination of metrics (latency, error rate, active connections) forms a score that affects the weight. 3) Exponential weighted moving average (EWMA) -- smoothing measurements for resilience to outliers. 4) Server-reported load -- servers report their load via a health endpoint or headers. 5) External metrics -- obtaining metrics from Prometheus/CloudWatch to adjust weights via the load balancer API. Envoy supports locality-weighted load balancing based on zone metrics.',
    explanation:
      'Adaptive load balancing is the next level after static weights. It automatically responds to server degradation (GC pauses, network issues, resource constraints) without waiting for a health check failure. Netflix uses adaptive balancing in its systems.',
  },
  {
    id: 'sd-lb-034',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is the proxy protocol and why is it needed?',
    options: [
      'An encryption protocol between the load balancer and the backend',
      'A protocol for passing information about the original client (IP, port) through a proxy/load balancer',
      'A health check protocol for verifying the availability of the proxy',
      'A protocol for synchronizing configuration between load balancers',
    ],
    correctIndex: 1,
    explanation:
      'The Proxy Protocol (v1 and v2) is a protocol for passing information about the original client (source IP, destination IP, ports) when proxying. With L4 balancing, the backend sees the IP of the load balancer rather than the client. The Proxy Protocol adds a header with information about the client at the beginning of the connection. It is supported by HAProxy, AWS NLB, Nginx, and ELB. Important: the backend must support the Proxy Protocol, otherwise it will receive garbage data.',
  },
  {
    id: 'sd-lb-035',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'senior',
    type: 'open',
    question: 'Describe the load balancing architecture for a WebSocket application with millions of concurrent connections.',
    sampleAnswer:
      'Architecture for scalable WebSocket: 1) L4 balancing at the entry point -- NLB/HAProxy in TCP mode for minimal latency. L7 is not required, since the connection is stable after the handshake. 2) Sticky sessions -- a WebSocket connection must stay on the same server. IP hash or cookie-based persistence. 3) Connection-aware routing -- storing a user_id → server mapping in Redis to route messages. 4) Pub/Sub for inter-server delivery -- Redis Pub/Sub or Kafka to deliver messages between servers. If user A on server 1 sends a message to user B on server 2, it goes through pub/sub. 5) Graceful draining -- when taking a server out of service, notify clients to reconnect to another server. 6) Connection limits -- OS tuning (ulimit, net.core.somaxconn) to support millions of connections per server.',
    explanation:
      'WebSocket is harder to scale than stateless HTTP. Discord and Slack use similar architectures. It is important to minimize cross-server communication through smart sharding (by chat room, guild) -- messages within a shard do not require pub/sub.',
  },
  {
    id: 'sd-lb-036',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is an upstream in an Nginx configuration?',
    options: [
      'A client sending requests to Nginx',
      'A group of backend servers among which Nginx distributes requests',
      'An external DNS server',
      'A log file for recording requests',
    ],
    correctIndex: 1,
    explanation:
      'An upstream in Nginx is a configuration block that defines a group of backend servers for load balancing. The upstream specifies the server addresses, their weights, health check parameters, and the balancing algorithm. For example: upstream backend { server 10.0.0.1:8080 weight=3; server 10.0.0.2:8080; } -- requests are distributed between the two servers with weights of 3:1.',
  },
  {
    id: 'sd-lb-037',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What HTTP code does the load balancer return if all backend servers are unavailable?',
    options: [
      '404 Not Found',
      '500 Internal Server Error',
      '502 Bad Gateway or 503 Service Unavailable',
      '401 Unauthorized',
    ],
    correctIndex: 2,
    explanation:
      '502 Bad Gateway is returned when the load balancer cannot get a valid response from the backend (connection error, timeout). 503 Service Unavailable is returned when all servers are marked as unhealthy and there is nothing to handle the request. The difference: 502 means an attempt to connect was made but failed; 503 means the servers are unavailable based on the health check. Both codes indicate a temporary problem -- the client can retry.',
  },
  {
    id: 'sd-lb-038',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is failover in the context of load balancing?',
    options: [
      'Distributing the load across servers',
      'Automatically switching to a backup server when the primary one fails',
      'Disabling the load balancer when overloaded',
      'Encrypting traffic during transmission',
    ],
    correctIndex: 1,
    explanation:
      'Failover is the automatic switching to a backup resource when the primary one fails. In the context of balancing: when a backend server fails, traffic is automatically redirected to healthy servers. At the level of the load balancer itself: when the active load balancer fails, the passive (standby) one takes over its IP via VRRP. Failover ensures high availability (HA) of the system.',
  },
  {
    id: 'sd-lb-039',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'middle',
    type: 'open',
    question: 'How does a load balancer help implement A/B testing and canary deployments?',
    sampleAnswer:
      'An L7 load balancer can route requests based on headers, cookies, or percentage distribution: 1) A/B testing -- the load balancer checks a cookie (ab_variant=B) or a header and routes the user to the corresponding version. If there is no cookie, it randomly assigns a variant and sets a cookie for consistency. 2) Canary deployment -- percentage distribution: 95% of traffic goes to the stable version, 5% to the canary. The share is gradually increased as metrics succeed. 3) Feature flags via headers -- the application sets a header (X-Feature: new-checkout), and the load balancer routes to a server with this feature. Tools: Nginx split_clients, HAProxy ACL, AWS ALB weighted target groups, Istio VirtualService, Envoy route matching.',
    explanation:
      'The load balancer is a convenient place for A/B and canary, because it sees all traffic and can make decisions centrally. However, complex experiments are better managed through feature flag systems (LaunchDarkly, Split.io), which offer more flexibility.',
  },
  {
    id: 'sd-lb-040',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What advantage does using Envoy proxy provide over traditional load balancers?',
    options: [
      'Envoy is easier to configure than Nginx',
      'Envoy is designed for microservices: dynamic configuration, observability, service mesh support',
      'Envoy works only at the L4 layer',
      'Envoy requires no resources to operate',
    ],
    correctIndex: 1,
    explanation:
      'Envoy is a modern proxy created by Lyft for microservice architecture. Key advantages: 1) Dynamic configuration via the xDS API -- no reload is needed. 2) Built-in observability -- metrics (Prometheus), tracing (Jaeger, Zipkin), logs. 3) Support for modern protocols -- HTTP/2, gRPC, WebSocket out of the box. 4) Service mesh ready -- the foundation for Istio, AWS App Mesh. 5) Advanced load balancing -- zone-aware, locality, circuit breaking. It is used in Istio, Contour, Ambassador, AWS App Mesh.',
  },
  {
    id: 'sd-lb-041',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'senior',
    type: 'open',
    question: 'How do you implement geographic load balancing (Geo Load Balancing) for a global application?',
    sampleAnswer:
      'Geo Load Balancing routes users to the nearest data center: 1) GeoDNS -- the DNS server determines the client region by IP and returns the IP of the nearest data center. AWS Route53 Geolocation/Latency routing, Cloudflare. 2) Anycast -- a single IP is announced from all regions, and BGP routes to the nearest one. 3) Global Load Balancer -- Google Cloud Global LB, AWS Global Accelerator. They provide a single entry point with automatic geo-routing. 4) CDN with origin shield -- Cloudflare, CloudFront cache content at the edge and route to the nearest origin. Routing criteria: geographic proximity, measured latency, traffic cost, compliance (EU data in the EU). Failover: if the nearest region is unavailable, route to the next one.',
    explanation:
      'Geo balancing is critical for global applications: the difference in latency between continents is hundreds of milliseconds. Google and Netflix use a combination of approaches: Anycast for DNS, Global LB for the API, and CDN for content. It is important to consider compliance: GDPR requires processing EU data within the EU.',
  },
  {
    id: 'sd-lb-042',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is locality-aware load balancing in Envoy/Istio?',
    options: [
      'Balancing only between local servers',
      'Prioritizing servers in the same zone/region to reduce latency and cost',
      'Balancing based on the user language',
      'Distributing data across geographic locations',
    ],
    correctIndex: 1,
    explanation:
      'Locality-aware load balancing prioritizes endpoints in the same zone/region as the calling service. Advantages: 1) Less latency -- no cross-zone/region delays. 2) Lower cost -- cross-AZ traffic in AWS is billed. 3) Blast radius -- problems in one zone do not affect others. It is configured through locality priorities and weights. If local endpoints fail, it fails over to other zones. Envoy supports locality weighting and priority levels.',
  },
  {
    id: 'sd-lb-043',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'senior',
    type: 'open',
    question: 'Explain the concept of client-side load balancing and when it is preferable to server-side.',
    sampleAnswer:
      'Client-side load balancing is when the client (the calling service) itself chooses which backend to send a request to, knowing about all available instances. How it works: 1) Service Discovery -- the client gets a list of endpoints from a registry (Consul, etcd, Kubernetes API). 2) Load Balancing -- the client applies an algorithm (round robin, least connections) locally. 3) Health tracking -- the client tracks the success of requests and excludes problematic endpoints. Advantages: no single point of failure (no load balancer), fewer network hops, better adaptability (the client knows about latency). Disadvantages: complexity in the client, duplication of logic, harder to update algorithms. Implementations: gRPC client-side LB, Netflix Ribbon, Envoy sidecar (formally server-side, but next to the client). Preferable for: internal communication in microservices, gRPC (problems with L4 LB), when minimal latency is important.',
    explanation:
      'Client-side LB vs centralized LB is an architectural trade-off. A service mesh (Istio, Linkerd) is a compromise: balancing is performed by a sidecar proxy next to the client, but the logic is managed centrally by the control plane. This provides the advantages of both approaches.',
  },
  {
    id: 'sd-lb-044',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What happens if you use the Round Robin algorithm for servers with different performance characteristics?',
    options: [
      'The load will automatically even out',
      'Slow servers will be overloaded, while fast ones will be underutilized',
      'The load balancer will automatically exclude the slow servers',
      'The algorithm will adapt to the performance of the servers',
    ],
    correctIndex: 1,
    explanation:
      'Round Robin distributes requests evenly without accounting for server performance. If one server processes requests twice as slowly, it will accumulate a queue and become overloaded, while a fast server sits idle. Solutions: Weighted Round Robin (set weights manually), Least Connections (automatically routes fewer requests to loaded servers), Least Response Time (accounts for response time).',
  },
  {
    id: 'sd-lb-045',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'middle',
    type: 'quiz',
    question: 'Which Kubernetes component is responsible for L4 balancing of traffic to services?',
    options: [
      'Ingress Controller',
      'kube-proxy',
      'kubelet',
      'etcd',
    ],
    correctIndex: 1,
    explanation:
      'kube-proxy is a component that runs on each Kubernetes node and implements L4 balancing for Services. It configures iptables or IPVS rules to redirect traffic from the ClusterIP to the real pod IPs. Modes: iptables (default) -- chains of NAT rules; IPVS -- more performant, supports more balancing algorithms. The Ingress Controller handles L7 balancing for HTTP.',
  },
  {
    id: 'sd-lb-046',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'middle',
    type: 'open',
    question: 'How do you configure rate limiting on a load balancer and why is it needed?',
    sampleAnswer:
      'Rate limiting on a load balancer restricts the number of requests from a client over a period of time. Why: 1) Protection against DDoS and abuse. 2) Fair usage -- one client cannot take all the resources. 3) Protecting backends from overload. 4) Enforcing SLAs and API quotas. Implementation in Nginx: limit_req_zone by IP or API key, limit_req in a location. In HAProxy: a stick-table for tracking, http-request deny when exceeded. In AWS ALB: WAF rules. Configuration: rate (requests per second), burst (allowable spike), and the action when exceeded (429 Too Many Requests, delay, drop). Important: rate limiting by IP is ineffective behind NAT; it is better to use an API key or user ID.',
    explanation:
      'Rate limiting is an important part of API Gateway functionality. It is often combined with throttling (slowing down instead of rejecting) and quota management (limits per day/month). For distributed rate limiting (multiple load balancers), Redis is used to store the counters.',
  },
  {
    id: 'sd-lb-047',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is Direct Server Return (DSR) in load balancing?',
    options: [
      'A direct connection between the client and the server without a load balancer',
      'The server response goes directly to the client, bypassing the load balancer',
      'Automatic restart of the server on failure',
      'Backing up data on the server',
    ],
    correctIndex: 1,
    explanation:
      'Direct Server Return (DSR) is an L4 balancing technique in which the request goes through the load balancer, but the server sends the response directly to the client, bypassing the load balancer. Advantages: offloading the load balancer (the response is usually larger than the request), less latency on the response path. Requirements: servers must have the VIP on the loopback (and not respond to ARP), it works only for L4, and it does not work for NAT/SNAT. It is used for high-throughput scenarios: video streaming, transferring large files.',
  },
  {
    id: 'sd-lb-048',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'senior',
    type: 'open',
    question: 'How do you ensure zero-downtime when updating the load balancer itself?',
    sampleAnswer:
      'Updating the load balancer without downtime: 1) Active-Passive with VRRP -- update the passive one, switch the VIP, then update the former active one. Keepalived for Linux. 2) Active-Active with DNS -- multiple A records, remove the one being updated from DNS, wait for the TTL, update it, return it to DNS. 3) Rolling update in Kubernetes -- the Ingress Controller as a Deployment with multiple replicas. During an update, a new pod is created, then the old one is removed. 4) Blue-Green of load balancers -- two groups of LBs, switching via a global LB or DNS. 5) Graceful reload -- Nginx: nginx -s reload creates new worker processes, while the old ones serve existing connections until completion. HAProxy: hot reload via systemd or socket. 6) Cloud LBs -- AWS ALB are updated automatically by AWS without downtime.',
    explanation:
      'The load balancer is a critical component. Its downtime = downtime of the entire service. Cloud-managed load balancers (ALB, GCP LB) simplify this task -- the provider handles the updates. For self-managed ones, it is important to have an HA pair and a well-rehearsed failover procedure.',
  },
  {
    id: 'sd-lb-049',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'junior',
    type: 'open',
    question: 'Which metrics are important to monitor on a load balancer?',
    sampleAnswer:
      'Key load balancer metrics: 1) Request rate -- the number of requests per second (total and per backend). 2) Latency -- request processing time (p50, p95, p99); separately the load balancer time and backend response time. 3) Error rate -- the percentage of 4xx and 5xx responses; 502/503 indicate problems with the backends. 4) Active connections -- the current number of connections; approaching the limit is a problem. 5) Backend health -- the number of healthy/unhealthy servers. 6) Bandwidth -- inbound and outbound traffic. 7) Queue depth -- if the load balancer has a waiting queue. 8) SSL handshake time -- for HTTPS termination. 9) Connection reuse rate -- the efficiency of keep-alive. Alerting: error rate > 1%, p99 latency > SLA, healthy backends < minimum.',
    explanation:
      'Monitoring the load balancer gives the first insight into the health of the system. A rise in latency or error rate on the LB is the first symptom of problems. Tools: built-in metrics (HAProxy stats, Nginx status), export to Prometheus, cloud CloudWatch/Stackdriver.',
  },
  {
    id: 'sd-lb-050',
    block: 'sd',
    topic: 'load-balancing',
    topicLabel: 'Load Balancing',
    difficulty: 'senior',
    type: 'quiz',
    question: 'Which balancing algorithm is recommended for a stateless REST API with homogeneous servers?',
    options: [
      'IP Hash -- to preserve sessions',
      'Least Connections -- for long-lived connections',
      'Round Robin -- simple and effective for an even load',
      'Random -- for maximum unpredictability',
    ],
    correctIndex: 2,
    explanation:
      'For a stateless REST API with homogeneous servers, Round Robin is the optimal choice. Advantages: simplicity, predictability, even distribution, and minimal overhead. Not needed: IP Hash (no sessions), Least Connections (requests are short, connections do not accumulate). Least Connections is better for WebSocket or long-running requests. With differing server performance -- Weighted Round Robin or Least Response Time.',
  },
];
