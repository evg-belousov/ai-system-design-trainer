import type { Question } from '@/data/types';

export const authQuestions: Question[] = [
  {
    id: 'sd-auth-001',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is the difference between authentication and authorization?',
    options: [
      'Authentication checks whether a user has the right to perform an action; authorization confirms their identity',
      'Authentication confirms a user identity (who are you); authorization determines what actions they are allowed to perform (what can you do)',
      'Authentication and authorization are synonyms, both referring to password verification',
      'Authentication is used only for APIs, authorization is used only for web interfaces',
    ],
    correctIndex: 1,
    explanation:
      'Authentication (AuthN) is the process of confirming identity: "Are you really who you claim to be?" It is implemented through passwords, tokens, biometrics, and certificates. Authorization (AuthZ) is the process of checking access rights: "Do you have the right to perform this action?" It is implemented through roles, policies, and ACLs. Authentication always precedes authorization: first the system identifies the user, then checks their rights.',
  },
  {
    id: 'sd-auth-002',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What three parts make up a JWT (JSON Web Token)?',
    options: [
      'Username, Password, Signature',
      'Header, Payload, Signature',
      'Token, Refresh Token, Session ID',
      'Issuer, Subject, Audience',
    ],
    correctIndex: 1,
    explanation:
      'A JWT consists of three parts separated by dots: Header — contains the token type (JWT) and signing algorithm (HS256, RS256). Payload — contains claims: standard (iss, sub, exp, iat) and custom (role, user_id). Signature — HMAC or RSA signature for integrity verification. Each part is Base64Url-encoded. JWT is not encrypted by default — the payload can be read by anyone but cannot be modified without knowing the secret.',
  },
  {
    id: 'sd-auth-003',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is OAuth 2.0?',
    options: [
      'An authentication protocol for verifying user login and password',
      'An authorization protocol that allows an application to obtain limited access to a user\'s resources on another service without transmitting their password',
      'A token format for storing sessions on a server',
      'An encryption standard for data transmission over the network',
    ],
    correctIndex: 1,
    explanation:
      'OAuth 2.0 is an authorization protocol (not authentication!) that allows third-party applications to obtain limited access to a user\'s resources without exposing their credentials. Key roles: Resource Owner (user), Client (application), Authorization Server (issues tokens), Resource Server (stores resources). OAuth 2.0 issues an access token confirming access rights. For authentication on top of OAuth, OpenID Connect is used.',
  },
  {
    id: 'sd-auth-004',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'junior',
    type: 'quiz',
    question: 'Which OAuth 2.0 flow is recommended for SPA (Single Page Application) and mobile applications?',
    options: [
      'Implicit Flow',
      'Client Credentials Flow',
      'Authorization Code Flow with PKCE',
      'Resource Owner Password Credentials Flow',
    ],
    correctIndex: 2,
    explanation:
      'Authorization Code Flow with PKCE (Proof Key for Code Exchange) is recommended for public clients (SPAs, mobile apps) that cannot safely store client_secret. PKCE adds code_verifier (a random string) and code_challenge (its hash). The client sends code_challenge when requesting authorization and code_verifier when exchanging the code for a token. This prevents authorization code interception by attackers. Implicit Flow is considered deprecated and insecure (token in URL, no refresh token).',
  },
  {
    id: 'sd-auth-005',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'junior',
    type: 'quiz',
    question: 'How does an API key (API key) differ from an access token (for example, JWT)?',
    options: [
      'API key identifies the application (client); access token authorizes actions on behalf of a specific user',
      'API key and access token are the same thing, just different names',
      'API key is safer because it has an expiration time, while access token does not',
      'API key is used only for authentication, while access token is used only for encryption',
    ],
    correctIndex: 0,
    explanation:
      'An API key is a long string that identifies an application or project. It is used for rate limiting, billing, and basic access control. Usually not tied to a specific user. An access token (for example, JWT) is a token issued after user authentication, containing information about their identity and rights. It has an expiration time. API keys are convenient for server-to-server interaction, but not suitable for user authorization as they do not contain user context and scope.',
  },
  {
    id: 'sd-auth-006',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is MFA (Multi-Factor Authentication)?',
    options: [
      'Using multiple passwords to log into a system',
      'Authentication using two or more independent factors: knowledge (password), possession (phone/token), biometrics (fingerprint)',
      'Encrypting a password using multiple algorithms sequentially',
      'Authorizing a user simultaneously in multiple systems',
    ],
    correctIndex: 1,
    explanation:
      'MFA (Multi-Factor Authentication) requires confirming identity using multiple independent factors: 1) Something you know — password, PIN. 2) Something you have — phone (SMS, TOTP app), hardware key (YubiKey). 3) Something you are — biometrics (fingerprint, face). The key word is "independent": two passwords is not MFA. TOTP (Time-based One-Time Password, RFC 6238) is the most common second factor. FIDO2/WebAuthn is a modern standard resistant to phishing.',
  },
  {
    id: 'sd-auth-007',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'middle',
    type: 'quiz',
    question: 'Which OAuth 2.0 flow is used for inter-service (machine-to-machine) authentication?',
    options: [
      'Authorization Code Flow',
      'Implicit Flow',
      'Client Credentials Flow',
      'Device Authorization Flow',
    ],
    correctIndex: 2,
    explanation:
      'The Client Credentials Flow is used when the client acts on its own behalf, not on behalf of a user (machine-to-machine). The client sends its client_id and client_secret directly to the Authorization Server and receives an access token. There is no user redirect step. Used for: one backend service calling another backend service, cron jobs, CLI tools. Important: client_secret must be stored securely (environment variables, secrets manager), never in code.',
  },
  {
    id: 'sd-auth-008',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'middle',
    type: 'quiz',
    question: 'How does OpenID Connect (OIDC) differ from OAuth 2.0?',
    options: [
      'OIDC is a complete replacement for OAuth 2.0 with an incompatible protocol',
      'OIDC is a layer on top of OAuth 2.0, adding an authentication layer (ID Token with user information)',
      'OIDC is used only for mobile applications, while OAuth is used for web',
      'OIDC and OAuth 2.0 are identical protocols from different organizations',
    ],
    correctIndex: 1,
    explanation:
      'OpenID Connect (OIDC) is an identity layer on top of OAuth 2.0. OAuth 2.0 itself is an authorization protocol; it does not tell you WHO the user is. OIDC adds: ID Token (JWT with user claims: sub, name, email); a standard /userinfo endpoint; standard scopes (openid, profile, email); a Discovery process (.well-known/openid-configuration). Thus, OIDC = OAuth 2.0 (authorization) + ID Token (authentication). Google Sign-In, Apple Sign-In, and Azure AD use OIDC.',
  },
  {
    id: 'sd-auth-009',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is PKCE (Proof Key for Code Exchange) and what attack does it prevent?',
    options: [
      'Client-side token encryption for XSS protection',
      'Protection against authorization code interception by binding it to a specific client through code_verifier/code_challenge',
      'JWT signing using asymmetric cryptography',
      'Refresh token rotation mechanism to prevent replay attacks',
    ],
    correctIndex: 1,
    explanation:
      'PKCE (RFC 7636) prevents authorization code interception attacks. Process: 1) Client generates a random code_verifier. 2) Computes code_challenge = SHA256(code_verifier). 3) Sends code_challenge in the authorization request. 4) Receives authorization code. 5) When exchanging code for token, sends code_verifier. 6) Authorization Server verifies: SHA256(code_verifier) == code_challenge. Even if an attacker intercepts the authorization code (via custom URL scheme, redirect), they cannot exchange it for a token without code_verifier.',
  },
  {
    id: 'sd-auth-010',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'middle',
    type: 'quiz',
    question: 'How does RBAC differ from ABAC?',
    options: [
      'RBAC assigns rights based on user roles; ABAC makes decisions based on attributes of the user, resource, environment, and action',
      'RBAC is used for APIs, ABAC for databases',
      'RBAC is an outdated approach, ABAC is its direct replacement',
      'RBAC provides row-level access, ABAC provides table-level access',
    ],
    correctIndex: 0,
    explanation:
      'RBAC (Role-Based Access Control): users are assigned roles (admin, editor, viewer), each role has a set of permissions. Simple to implement and understand, but inflexible for complex rules. ABAC (Attribute-Based Access Control): decisions are made based on policies considering subject attributes (department, clearance level), object attributes (classification, owner), action (read, write), and context (time of day, IP address). Example: "A doctor can read a patient\'s medical record from their department during business hours." RBAC cannot express such a rule without creating many roles.',
  },
  {
    id: 'sd-auth-011',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'middle',
    type: 'quiz',
    question: 'How does SSO (Single Sign-On) based on SAML 2.0 work?',
    options: [
      'Users enter their password in each application, but the password is the same',
      'A user authenticates once at an Identity Provider (IdP), and the IdP transmits a SAML Assertion (signed XML document) to applications (Service Providers) to confirm identity',
      'All applications use a shared user database and directly verify the password',
      'A user receives a single JWT token that works in all applications',
    ],
    correctIndex: 1,
    explanation:
      'SAML 2.0 SSO: 1) User accesses a Service Provider (SP). 2) SP redirects to Identity Provider (IdP) with a SAML AuthnRequest. 3) IdP authenticates the user (if not already authenticated). 4) IdP creates a SAML Assertion — an XML document with user information, signed with the IdP digital signature. 5) IdP redirects the user back to SP with the SAML Response. 6) SP verifies the signature and extracts user data. When accessing another SP, steps 1-2 repeat, but step 3 is skipped (IdP already has a session).',
  },
  {
    id: 'sd-auth-012',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'middle',
    type: 'open',
    question: 'Describe the refresh token rotation strategy. Why is it important and how is it implemented?',
    sampleAnswer:
      'Refresh Token Rotation is a strategy where each time a refresh token is used, a new refresh token is issued and the old one is invalidated. Process: 1) Client sends refresh token to /token endpoint. 2) Authorization Server verifies refresh token, issues a new access token and a NEW refresh token. 3) Old refresh token is marked as used. 4) If someone attempts to use the already-used refresh token, this signals compromise: the server invalidates the ENTIRE token chain (including the current active refresh token), forcing the user to re-authenticate. This significantly reduces the window of vulnerability if a refresh token is leaked. RFC 6819 and OAuth 2.0 Security Best Current Practice recommend rotation for public clients.',
    explanation:
      'Without rotation, a stolen refresh token can be used indefinitely (until expiration). With rotation: if a token is stolen, either the legitimate user or the attacker uses it first. The second use triggers automatic reuse detection and full invalidation. Auth0, Okta, and other providers support refresh token rotation out of the box.',
  },
  {
    id: 'sd-auth-013',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'middle',
    type: 'open',
    question: 'Compare server-side sessions (server-side sessions) and stateless approach with JWT. What are the advantages and disadvantages of each?',
    sampleAnswer:
      'Server-side sessions: the server stores session state (in memory, Redis, DB), the client receives a session ID in a cookie. Pros: easy to invalidate (delete session), small cookie size, session data not visible to client. Cons: requires shared storage with multiple servers (sticky sessions or Redis), additional storage lookup on each request, harder to scale. JWT (stateless): the token contains all claims, the server stores no state. Pros: no need for shared storage, scales excellently, works across domains. Cons: cannot invalidate until expiration (needs a blocklist), larger size (sent in every request), payload visible to client (Base64, not encryption), size grows with claims. In practice, often combined: short-lived JWT (5-15 min) + server-side refresh token.',
    explanation:
      'The "JWT vs Sessions" question is one of the most common interview questions. The key difference is in state management. For microservices, JWT is more convenient (each service validates the signature without calling a centralized store). For monoliths, server-side sessions are simpler and safer. The hybrid approach is most practical.',
  },
  {
    id: 'sd-auth-014',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'middle',
    type: 'open',
    question: 'What is passwordless authentication? Describe the main approaches and their advantages.',
    sampleAnswer:
      'Passwordless authentication is authentication without passwords, using alternative factors. Main approaches: 1) Magic Link — a one-time link is sent to email. User clicks — authenticated. Simple but depends on email security. 2) WebAuthn/FIDO2 — authentication via hardware keys (YubiKey) or platform authenticators (Touch ID, Windows Hello). Most secure option, phishing-resistant. 3) OTP via SMS/email — one-time code. Less secure (SIM swapping, SMS interception). 4) Passkeys — evolution of WebAuthn, synchronized across devices through iCloud Keychain / Google Password Manager. Advantages of passwordless: no passwords to steal/leak, phishing resistance (WebAuthn), better UX (no need to remember passwords), reduced support burden (password resets).',
    explanation:
      'Apple, Google, and Microsoft jointly promote Passkeys as a password replacement. NIST recommends abandoning SMS OTP in favor of TOTP and FIDO2. Passwordless is a trend that significantly improves security because most attacks (credential stuffing, phishing) exploit weak or stolen passwords.',
  },
  {
    id: 'sd-auth-015',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'senior',
    type: 'quiz',
    question: 'Which JWT signing algorithm is preferable in a microservices architecture where many services must validate the token?',
    options: [
      'HS256 (HMAC with shared secret) — all services use one secret',
      'RS256 (RSA) — Authorization Server signs with private key, services verify with public key via JWKS endpoint',
      'none (no signature) — for faster validation',
      'AES256 — symmetric payload encryption',
    ],
    correctIndex: 1,
    explanation:
      'RS256 (RSA-SHA256) uses asymmetric cryptography: the Authorization Server signs the JWT with its private key, which only it holds. All Resource Servers verify the signature using the public key available via the JWKS (JSON Web Key Set) endpoint (/.well-known/jwks.json). Advantages: compromising one service does not allow forging tokens; the public key can be safely distributed; key rotation is supported via kid (Key ID) in the JWT header. HS256 requires transmitting the shared secret to all services, creating a single point of failure.',
  },
  {
    id: 'sd-auth-016',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What attack on JWT is possible with improper alg header validation?',
    options: [
      'SQL Injection through JWT payload',
      'Attacker sets alg: "none" and removes signature, bypassing verification',
      'DDoS attack by generating a large number of tokens',
      'Man-in-the-middle by replacing refresh token',
    ],
    correctIndex: 1,
    explanation:
      'The "alg: none" attack: if the server trusts the alg value from the JWT header, an attacker can set "alg: "none"", remove the signature, and get a valid token. Another attack: "algorithm confusion" — when RS256 is used, the server verifies the signature with the public key; an attacker changes alg to HS256 and signs the token with the public key (which is public) as an HMAC secret. Protection: ALWAYS explicitly specify the expected algorithm on the server (whitelist), NEVER rely on the alg value from the token.',
  },
  {
    id: 'sd-auth-017',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'senior',
    type: 'open',
    question: 'Design an authentication and authorization system for a microservices platform. Describe the choice of protocols, token management, and access control verification.',
    sampleAnswer:
      'Architecture: 1) Centralized Identity Provider (Keycloak/Auth0): supports OIDC, stores users, roles, permissions. 2) API Gateway as entry point: validates JWT (verifies RS256 signature, exp, iss, aud), extracts claims, passes user context in headers to downstream services. 3) Tokens: short-lived access token (JWT, 5-15 min) with claims (sub, roles, permissions); long-lived refresh token (opaque, stored in IdP DB) with rotation. 4) Authorization: coarse-grained at API Gateway by roles from JWT; fine-grained in each service by permissions and business rules (ABAC). 5) Service-to-service: Client Credentials Flow, mutual TLS, or service mesh (Istio). 6) Token introspection endpoint for verifying opaque token activity. 7) JWKS endpoint with key rotation (kid header). 8) Blacklist in Redis for JWT invalidation before expiration (for logout, password change). 9) Rate limiting on /token endpoint against brute force.',
    explanation:
      'Key principle: "never trust, always verify". Each microservice should independently validate JWT, not rely only on API Gateway. For critical operations (money transfer), step-up authentication may be required — a second factor prompt.',
  },
  {
    id: 'sd-auth-018',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'senior',
    type: 'open',
    question: 'How can you safely implement JWT invalidation before expiration? Describe approaches and their trade-offs.',
    sampleAnswer:
      'Problem: JWT is stateless — the server stores no state, so it cannot "revoke" an issued token. Approaches: 1) Short-lived tokens (5-15 min): reduces the vulnerability window but does not completely solve the problem. On logout, the token remains valid for 5-15 minutes. 2) Token Blacklist (deny list): on logout/password change, the jti (JWT ID) is added to Redis/in-memory store. Each request checks the blacklist. Trade-off: introduces stateful checking, Redis load, synchronization needed between instances. 3) Token Versioning: a token_version is stored in the DB for the user. JWT contains a version claim. To invalidate, the version is incremented. Trade-off: DB query on each request (can be cached). 4) Refresh Token Revocation: access token is short-lived, when refresh token is revoked, new access tokens are not issued. Trade-off: vulnerability window = access token TTL. 5) Event-driven invalidation: on logout, an event is published, all services update local blocklist. Trade-off: eventual consistency. Recommendation: short-lived access token (5 min) + refresh token revocation + Redis blacklist for critical cases.',
    explanation:
      'This is one of the main JWT authentication issues and a frequent senior interview question. There is no perfect solution: each approach is a trade-off between statelessness and control. In practice, a combination of short-lived tokens + server-side refresh tokens covers most scenarios.',
  },
  {
    id: 'sd-auth-019',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'senior',
    type: 'open',
    question: 'Explain how to implement fine-grained authorization in a distributed system. Compare approaches: embedded policy, centralized policy engine (OPA), and Google Zanzibar.',
    sampleAnswer:
      'Fine-grained authorization determines access at the level of individual objects and actions. Approaches: 1) Embedded Policy: authorization logic built into each service code (if user.role == "admin"). Pros: simple, fast. Cons: duplication, hard to maintain consistency, changes require redeployment. 2) Centralized Policy Engine (OPA/Open Policy Agent): policies described declaratively (Rego language). Services send requests to OPA with context (user, resource, action) and receive allow/deny. Pros: unified policies, audit, hot reload without redeployment. Cons: network call latency (solved with sidecar), Rego learning curve. 3) Google Zanzibar (ReBAC — Relationship-Based Access Control): models authorization through a relationship graph: user:alice → member → group:engineering → viewer → doc:123. Query: "Can alice view doc:123?" — graph traversal. Implementations: SpiceDB, Authzed, Ory Keto. Pros: natural modeling of complex hierarchies (Google Drive, GitHub), global consistency, millisecond checks. Cons: complexity, separate infrastructure needed.',
    explanation:
      'Google Zanzibar serves authorization for Google Drive, YouTube, Cloud, and other products with p50 latency < 10ms across trillions of relationship tuples. For most companies, OPA is the optimal balance between flexibility and complexity. For systems with hierarchical access structures (filesystem, organization hierarchy), the Zanzibar approach is most natural.',
  },
  {
    id: 'sd-auth-020',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is the safest approach for storing tokens on the client in a web application?',
    options: [
      'localStorage — convenient for JavaScript, persists between sessions',
      'sessionStorage — automatically cleared when the tab is closed',
      'HttpOnly Secure SameSite cookie — inaccessible to JavaScript, protected from XSS, CSRF, and interception',
      'URL parameters — visible in the address bar, easy to pass between pages',
    ],
    correctIndex: 2,
    explanation:
      'An HttpOnly cookie is inaccessible from JavaScript (document.cookie), protecting against XSS attacks (even if an attacker injects a script, they cannot access the token). The Secure flag guarantees transmission only over HTTPS. SameSite=Strict/Lax prevents sending cookies in cross-site requests (CSRF protection). localStorage/sessionStorage are vulnerable to XSS: any JS code on the page can read them. URL parameters are visible in logs, browser history, and Referer headers. Recommended pattern: access token in HttpOnly cookie, refresh token with rotation.',
  },
  {
    id: 'sd-auth-021',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is Zero Trust Security and what is its main principle?',
    options: [
      'A security model where the internal network is considered fully protected',
      'A security model where no user or system automatically receives trust, and every request must be authenticated and authorized',
      'A technology for encrypting data at rest',
      'A method for protecting against DDoS attacks',
    ],
    correctIndex: 1,
    explanation:
      'Zero Trust is a security model with the principle "never trust, always verify". Unlike perimeter security (castle-and-moat), where the internal network is considered trusted, Zero Trust requires: authentication and authorization of every request, network micro-segmentation, least privilege, continuous monitoring. Implementation: mTLS between services, identity-based access instead of network-based, MFA everywhere.',
  },
  {
    id: 'sd-auth-022',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is an Identity Provider (IdP)?',
    options: [
      'A database for storing user passwords',
      'A service that authenticates users and issues tokens or assertions for accessing other services',
      'A proxy server for API protection',
      'A tool for generating API keys',
    ],
    correctIndex: 1,
    explanation:
      'Identity Provider (IdP) is a centralized identity management service. Functions: storing credentials, authenticating users, issuing tokens (OAuth 2.0) or assertions (SAML), user and group management, MFA. Examples: Okta, Auth0, Azure AD, Keycloak (open-source), Google Identity. IdP separates authentication logic from applications — applications trust IdP tokens without storing passwords.',
  },
  {
    id: 'sd-auth-023',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What HTTP header is used to transmit Bearer token in API requests?',
    options: [
      'X-API-Key',
      'Authorization: Bearer <token>',
      'Cookie: token=<token>',
      'X-Auth-Token',
    ],
    correctIndex: 1,
    explanation:
      'Authorization header with Bearer scheme is the standard way to transmit access token (RFC 6750). Format: Authorization: Bearer eyJhbGciOiJIUzI1NiIs... Bearer token is usually JWT but can be opaque. Advantages: standardized, works cross-domain (unlike cookies), explicitly indicates authorization type. X-API-Key is used for API keys (app identification), Cookie for session-based auth.',
  },
  {
    id: 'sd-auth-024',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'junior',
    type: 'open',
    question: 'What is CORS and how is it related to authentication security?',
    sampleAnswer:
      'CORS (Cross-Origin Resource Sharing) is a browser mechanism that controls access to resources from other origins (domain + protocol + port). Without CORS, the browser blocks cross-origin requests from JavaScript. For authentication: if frontend (app.example.com) calls API (api.example.com), the server must return CORS headers: Access-Control-Allow-Origin, Access-Control-Allow-Credentials: true (for cookies). Security: improper CORS configuration can allow an attacker from a malicious site to send requests to the API on behalf of the user. Recommendations: whitelist allowed origins (not *), do not use Access-Control-Allow-Credentials with wildcard origin.',
    explanation:
      'CORS is a common cause of issues when developing SPAs with separate APIs. Preflight requests (OPTIONS) add latency. For credentials (cookies, Authorization header), explicit Access-Control-Allow-Credentials: true and a specific origin (not *) are required.',
  },
  {
    id: 'sd-auth-025',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is identity federation and what problem does it solve?',
    options: [
      'Merging multiple user databases into one',
      'Ability to use credentials from one system to access another without creating a new account, through trust relationships between IdPs',
      'Encrypting identification data during transmission',
      'Replicating users between data centers',
    ],
    correctIndex: 1,
    explanation:
      'Identity Federation allows users to authenticate in one system (home IdP) and access resources of another system (service provider) without creating a separate account. Examples: logging into corporate apps through Azure AD, logging into SaaS through Google/Okta, B2B integrations. Protocols: SAML 2.0, OIDC, WS-Federation. Advantages: SSO, centralized management, onboarding/offboarding in one place. Trust is established through certificate/metadata exchange.',
  },
  {
    id: 'sd-auth-026',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is scope in OAuth 2.0?',
    options: [
      'Access token lifetime',
      'Geographic region where the token is valid',
      'A set of permissions determining what resources and actions the client requests access to',
      'Application identifier in OAuth',
    ],
    correctIndex: 2,
    explanation:
      'Scope is a mechanism for limiting access in OAuth 2.0. The client requests scopes during authorization, the user confirms consent, and the token contains granted scopes. Examples: openid (OIDC identity), profile (name, photo), email, read:users, write:posts. Principle of least privilege: request only necessary scopes. Resource server checks scopes in the token before executing an operation. Scopes enable granular control: an application can read email but not send it.',
  },
  {
    id: 'sd-auth-027',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'middle',
    type: 'quiz',
    question: 'How does API Gateway authentication differ from authentication in each microservice?',
    options: [
      'API Gateway authentication is slower',
      'API Gateway validates token centrally at entry and passes identity context downstream, avoiding duplication in each service',
      'Microservices cannot validate tokens',
      'API Gateway supports only API keys',
    ],
    correctIndex: 1,
    explanation:
      'Centralized authentication at API Gateway: Gateway validates JWT (verifies RS256 signature, exp, iss, aud), extracts claims (user_id, roles), and passes them to downstream services in headers (X-User-ID, X-User-Roles). Advantages: single validation point, simplified microservices, centralized key management (JWKS). However, each microservice should independently verify authorization (can user_id perform action on resource). Zero Trust recommends re-validating the token in each service.',
  },
  {
    id: 'sd-auth-028',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'middle',
    type: 'open',
    question: 'How should an API be protected from credential stuffing attacks?',
    sampleAnswer:
      'Credential stuffing is automated testing of stolen login/password pairs on multiple services. Protection: 1) Rate limiting on /login endpoint: limit attempts by IP and by username. 2) Account lockout: temporary blocking after N failed attempts (with user notification). 3) CAPTCHA: after several failures or on suspicious activity (reCAPTCHA v3 invisible). 4) MFA: even if a password is leaked, the second factor provides protection. 5) Bot detection: fingerprinting analysis, behavior analysis, headless browser detection. 6) Passwordless: magic links and WebAuthn eliminate passwords. 7) Breach password check: verify password against haveibeenpwned API on registration/change. 8) Device fingerprinting: alert on login from new device. Monitoring: track failed login rate, login geography.',
    explanation:
      'Credential stuffing is one of the most common attacks. According to Akamai, up to 90% of login traffic on some sites is credential stuffing bots. A combination of rate limiting + MFA + bot detection significantly reduces attack success rate.',
  },
  {
    id: 'sd-auth-029',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'middle',
    type: 'open',
    question: 'Explain the concept of least privilege in the context of authorization. How can it be applied in practice?',
    sampleAnswer:
      'Principle of Least Privilege (PoLP): a user or system should have only the minimum necessary rights to perform their tasks. Application: 1) RBAC with granular roles: instead of "admin" — "billing_admin", "user_admin", "content_editor". 2) Service accounts: each microservice has its own service account with minimal rights (read-only to needed resources). 3) Temporary credentials: AWS IAM roles instead of long-lived keys. 4) Just-in-time access: rights granted for task execution time (PIM in Azure AD). 5) Scope in OAuth: request only necessary scopes. 6) Database: separate users with rights only to their tables. 7) Infrastructure as Code: review changes in permissions. Monitoring: unused permissions, access patterns. Regular audit: review and revoke unused rights.',
    explanation:
      'PoLP is a fundamental security principle that reduces blast radius on compromise. AWS recommends IAM Access Analyzer for identifying overly permissive policies. Challenge: balancing security with convenience, managing at scale.',
  },
  {
    id: 'sd-auth-030',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is API security and what are the main threats to APIs?',
    options: [
      'Protecting API source code from copying',
      'A set of measures to protect APIs from attacks: authentication, authorization, input validation, rate limiting, protection from OWASP API Security Top 10',
      'Encrypting all API responses',
      'Using only internal IPs for APIs',
    ],
    correctIndex: 1,
    explanation:
      'API Security includes protection from OWASP API Security Top 10 threats: Broken Object Level Authorization (BOLA/IDOR), Broken Authentication, Excessive Data Exposure, Lack of Resources & Rate Limiting, Broken Function Level Authorization, Mass Assignment, Security Misconfiguration, Injection, Improper Assets Management, Insufficient Logging & Monitoring. Protection measures: strict authentication (OAuth 2.0), granular authorization (ownership verification), input validation, rate limiting, WAF, anomaly monitoring, API gateway for centralized protection.',
  },
  {
    id: 'sd-auth-031',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is BOLA (Broken Object Level Authorization) and how can you protect against it?',
    options: [
      'Vulnerability in cryptographic algorithms',
      'Vulnerability where API does not verify whether a user has access to a specific object, allowing them to obtain others\' data by manipulating the ID',
      'DNS record attack',
      'Load balancing issue',
    ],
    correctIndex: 1,
    explanation:
      'BOLA (also IDOR — Insecure Direct Object References) is #1 in OWASP API Security Top 10. Example: GET /api/users/123/orders — if the API does not verify that the current user is 123, an attacker can obtain any user\'s orders by changing the ID. Protection: 1) Always verify object ownership (order.user_id == current_user.id). 2) Use unpredictable IDs (UUID instead of sequential). 3) Authorization checks in every endpoint, not just authentication. 4) Automated tests for BOLA. 5) API Gateway policies for verification.',
  },
  {
    id: 'sd-auth-032',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'senior',
    type: 'quiz',
    question: 'How to implement secure token storage on mobile devices?',
    options: [
      'Store tokens in SharedPreferences / UserDefaults in plain text',
      'Use platform-specific secure storage: Android Keystore / iOS Keychain with biometric protection',
      'Store tokens in local SQLite database',
      'Send token to server and request on each startup',
    ],
    correctIndex: 1,
    explanation:
      'Secure token storage on mobile: Android: Keystore System — hardware-backed storage for cryptographic keys. EncryptedSharedPreferences uses keys from Keystore. iOS: Keychain Services — secure enclave for storing credentials. Attributes: kSecAttrAccessibleWhenUnlocked (access only when device unlocked), biometric protection (Face ID/Touch ID for element access). Additionally: device binding (token tied to device ID), certificate pinning for MITM protection, jailbreak/root detection.',
  },
  {
    id: 'sd-auth-033',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'senior',
    type: 'open',
    question: 'Describe authentication system architecture for compliance requirements (SOC 2, HIPAA). What controls are necessary?',
    sampleAnswer:
      'Compliance requirements for authentication: 1) Strong authentication: MFA mandatory for all users (especially for PHI access in HIPAA). 2) Audit logging: logging all auth events (login, logout, failed attempts, MFA events) with retention per policy (SOC 2 typically 1 year). 3) Password policies: minimum length, complexity, rotation (although NIST 800-63 does not recommend mandatory rotation), breached password checks. 4) Session management: timeout, concurrent session limits, secure logout. 5) Access reviews: periodic audit of access rights, removing inactive accounts. 6) Encryption: TLS 1.2+ for transmission, password hashing (bcrypt/Argon2). 7) Break-glass procedures: documented emergency access. 8) Segregation of duties: admin rights division. Tools: IdP with compliance certifications (Okta, Azure AD), SIEM for audit logs.',
    explanation:
      'SOC 2 Type II requires demonstrating control operation over a period (typically 6-12 months). HIPAA requires PHI protection with documented policies. Compliance is not a checkbox but a continuous process with regular audits.',
  },
  {
    id: 'sd-auth-034',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'senior',
    type: 'open',
    question: 'How to implement secure service-to-service authentication in microservices architecture?',
    sampleAnswer:
      'Service-to-service auth approaches: 1) mTLS (mutual TLS): each service has a certificate issued by internal CA. On connection, both sides verify certificates. Service mesh (Istio) automates certificate issuance and rotation via sidecar. 2) JWT with service identity: each service has credentials (client_id/client_secret), obtains JWT from IdP, sends in Authorization header. 3) API keys with HMAC signing: request signing with shared secret for integrity and authentication. 4) SPIFFE/SPIRE: open standard for service identity. SPIFFE ID (spiffe://trust-domain/workload-id), SPIRE issues x509 SVID or JWT SVID. Recommendations: mTLS for network-level security + JWT for application-level authorization (claims with permissions). Zero Trust: do not trust network location, always verify identity.',
    explanation:
      'Service mesh with mTLS became the standard for Kubernetes. SPIFFE ensures interoperability between different systems. Important: secret and certificate rotation is automated, human factor minimized.',
  },
  {
    id: 'sd-auth-035',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'senior',
    type: 'open',
    question: 'Explain the concept of policy as code for authorization. Compare OPA and Cedar.',
    sampleAnswer:
      'Policy as Code: authorization rules described in a declarative language, versioned, tested, deployed as code. OPA (Open Policy Agent): Rego language, universal (Kubernetes admission, API authorization, Terraform). Example: allow { input.user.role == "admin" }. Decoupled: policies separate from application, OPA as sidecar or remote service. Cedar (AWS): typed language, developed for AWS Verified Permissions. Example: permit(principal, action, resource) when { principal.role == "admin" }. Strong typing, built-in hierarchy support. Comparison: OPA — mature, wide ecosystem, universal. Cedar — newer, better DX for authorization-specific use cases, AWS-native integration. Both support: unit testing policies, explain (why decision), audit logging.',
    explanation:
      'Policy as Code is an evolution from hardcoded authorization. Advantages: single source of truth, git history for audit, consistent enforcement, testability. Netflix, Airbnb use OPA for API authorization. AWS promotes Cedar as part of Verified Permissions service.',
  },
  {
    id: 'sd-auth-036',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is a session fixation attack and how to protect against it?',
    options: [
      'An attack where an attacker fixes the exchange rate',
      'An attack where an attacker sets a known session ID for a victim before authentication and then uses this ID to access the account',
      'An attack on session storage database',
      'A DDoS attack on session storage',
    ],
    correctIndex: 1,
    explanation:
      'Session fixation: 1) Attacker obtains a session ID (creates a session on site). 2) Forces the victim to authenticate with this session ID (via URL parameter or cookie injection). 3) After victim authentication, attacker uses the same session ID. Protection: regenerate session ID on login (create new session after successful authentication), do not accept session ID from URL parameters, set httpOnly and secure flags on session cookie, bind session to IP/User-Agent (with caution — may break legitimate scenarios).',
  },
  {
    id: 'sd-auth-037',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'junior',
    type: 'open',
    question: 'How should user passwords be securely stored? Explain the principles of password hashing.',
    sampleAnswer:
      'Principles of password storage: 1) Never store in plain text or reversible encryption. 2) Use specialized password hashing functions: bcrypt, Argon2, scrypt. Do NOT use MD5, SHA-1, SHA-256 without salt and key stretching. 3) Salt: unique random string for each password, stored with hash. Prevents rainbow table attacks. 4) Work factor / iterations: bcrypt cost, Argon2 memory/time. Slows brute-force. Configuration: hashing should take ~100-500ms. 5) Pepper: secret key shared across all passwords, stored separately from DB (HSM, environment variable). Adds defense-in-depth on DB leak. Modern standard: Argon2id (winner of Password Hashing Competition). Bcrypt is a proven choice. Regularly increase work factor as computing power grows.',
    explanation:
      'Proper password hashing is basic hygiene. The LinkedIn leak (2012) used unsalted SHA-1 — millions of passwords were compromised. Argon2 is recommended by OWASP. Bcrypt with cost 12+ is a good default.',
  },
  {
    id: 'sd-auth-038',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is the device authorization grant (device flow) in OAuth 2.0?',
    options: [
      'Flow for authorizing mobile devices',
      'Flow for devices with limited input capabilities (Smart TV, CLI, IoT), where user authorizes on another device by code',
      'Authorization using device biometrics',
      'Device registration in MDM system',
    ],
    correctIndex: 1,
    explanation:
      'Device Authorization Grant (RFC 8628): for devices without a browser or convenient input (TV apps, CLI, IoT). Process: 1) Device requests device_code and user_code from Authorization Server. 2) Shows user_code and URL (e.g., https://example.com/activate). 3) User on phone/computer visits the URL, enters code, authorizes. 4) Device periodically polls (polling) the Authorization Server. 5) After user authorization, device receives access token. Used: GitHub CLI, Netflix on TV, AWS CLI.',
  },
  {
    id: 'sd-auth-039',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is token binding and what problem does it solve?',
    options: [
      'Binding a token to a specific TLS connection to prevent replay attacks if the token is leaked',
      'Linking multiple tokens into one',
      'Encrypting a token before transmission',
      'Automatic token renewal',
    ],
    correctIndex: 0,
    explanation:
      'Token Binding (RFC 8471) is a mechanism for binding a token to a TLS connection through cryptographic connection. If a token is stolen, it is useless without the TLS private key. How it works: client generates a key pair, public key included in TLS handshake (Token Binding ID), token contains hash of this ID. Server verifies that the token was issued for this specific TLS connection. Problem: limited browser support (Chrome dropped support). Alternative: DPoP (Demonstrating Proof of Possession) — signing requests with client private key.',
  },
  {
    id: 'sd-auth-040',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'middle',
    type: 'open',
    question: 'How to implement impersonation (acting on behalf of another user) for support team safely?',
    sampleAnswer:
      'Impersonation (su, acting as) is a function for support/admin to access another user\'s account. Safe implementation: 1) Strict authorization: only certain roles (support_admin), with approval workflow for sensitive accounts. 2) Audit trail: logging all actions with impersonation marking (actor_id + target_user_id), displayed in user audit log. 3) Limited scope: impersonator sees data but cannot change password, MFA, billing. 4) Visual indicator: UI shows "Viewing as user X" banner, preventing accidental actions. 5) Time-limited: impersonation session auto-expires (30 min). 6) Notification: email to user about impersonation (optional). 7) Token: separate impersonation token with claims {sub: admin_id, act: {sub: user_id}} or JWT with actor claim. 8) Disable for sensitive accounts: VIP, admin accounts protected from impersonation.',
    explanation:
      'Impersonation is a necessary support function but high-risk. Without proper controls, abuse is inevitable. Stripe, GitHub document their impersonation practices. GDPR requires logging personal data access.',
  },
  {
    id: 'sd-auth-041',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What are verifiable credentials and how are they related to decentralized identity?',
    options: [
      'Credentials verifiable via blockchain',
      'W3C standard for digital credentials that can be cryptographically verified, issued by issuer, stored by holder, and presented to verifier',
      'Multi-factor authentication',
      'Biometric data for authentication',
    ],
    correctIndex: 1,
    explanation:
      'Verifiable Credentials (W3C standard) is a model for digital credentials. Participants: Issuer (issues credential, e.g., university — diploma), Holder (stores in wallet, controls), Verifier (verifies authenticity). Credential contains claims signed by issuer. Holder can selective disclosure — show only needed claims (age > 18 without birthdate). Related to DID (Decentralized Identifiers) — identifiers independent of central authority. Application: digital identity, education credentials, healthcare records. Microsoft Entra Verified ID, IBM Digital Health Pass are implementation examples.',
  },
  {
    id: 'sd-auth-042',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'senior',
    type: 'open',
    question: 'How to implement step-up authentication for critical operations?',
    sampleAnswer:
      'Step-up authentication is repeated or enhanced authentication for high-risk actions (money transfer, password change, PII access). Implementation: 1) Risk assessment: determine which operations require step-up (risk-based). 2) Authentication context (acr claim): JWT contains level of assurance (loa). Levels: password only (loa1), password + TOTP (loa2), hardware key (loa3). 3) On requesting critical operation: backend checks acr in token. If insufficient, return 403 with step-up requirement. 4) Frontend redirects to re-authentication with required acr. 5) After step-up: token issued with elevated acr and short TTL (5-10 minutes). 6) Audit: log all step-up events. Example flow: user logged in with password (acr: pwd), wants to transfer money → requires acr: mfa → enters TOTP → gets short-lived token with acr: mfa → performs transfer.',
    explanation:
      'Step-up auth balances UX and security. Do not require MFA for every action, but protect critical ones. OIDC spec includes acr_values for requesting specific auth level. Banking apps use step-up extensively.',
  },
  {
    id: 'sd-auth-043',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'senior',
    type: 'open',
    question: 'Describe best practices for secrets management (API keys, credentials) in applications.',
    sampleAnswer:
      'Secrets management best practices: 1) Never in code/git: use .gitignore, pre-commit hooks (git-secrets), scan repositories (truffleHog, GitGuardian). 2) Environment variables: basic approach but visible in process list. 3) Secrets managers: HashiCorp Vault, AWS Secrets Manager, Azure Key Vault, GCP Secret Manager. Centralized storage, API access, audit logging. 4) Rotation: automatic secret rotation (Vault dynamic secrets, AWS automatic rotation). 5) Least privilege: each service gets only its secrets. 6) Encryption at rest and in transit. 7) Short-lived credentials: temporary tokens instead of long-lived keys (AWS IAM roles, Vault dynamic database credentials). 8) Kubernetes: Secrets (base64, not encryption!), better use External Secrets Operator + Vault. 9) CI/CD: dedicated secrets management (GitHub Secrets, GitLab CI variables, HashiCorp Vault). Never log secrets.',
    explanation:
      'Secrets leak is one of the most common breach causes. HashiCorp Vault became the standard for secrets management. AWS/GCP/Azure native secrets managers integrate with cloud services. 12-factor app recommends environment variables, but secrets managers are safer.',
  },
  {
    id: 'sd-auth-044',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is a CSRF (Cross-Site Request Forgery) attack?',
    options: [
      'An attack where an attacker injects a script on a website',
      'An attack where a malicious site forces a user\'s browser to send a request to another site, using the victim\'s existing session',
      'Network traffic interception',
      'User password guessing',
    ],
    correctIndex: 1,
    explanation:
      'CSRF: user is authenticated on bank.com (session cookie). Visits evil.com, which contains <form action="bank.com/transfer" method="POST">. Browser automatically sends bank.com cookie with the request. Protection: 1) CSRF tokens — unique token in each form, verified by server. 2) SameSite cookie attribute: Strict (cookie not sent in cross-site requests), Lax (not sent in POST cross-site). 3) Check Origin/Referer headers. 4) Custom headers (X-Requested-With) — cross-origin requests cannot add custom headers without CORS preflight.',
  },
  {
    id: 'sd-auth-045',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is OAuth 2.0 Token Introspection?',
    options: [
      'Process of creating a new token',
      'Mechanism for verifying opaque token activity and metadata through a request to Authorization Server',
      'Decoding JWT token on the client',
      'Encrypting a token before transmission',
    ],
    correctIndex: 1,
    explanation:
      'Token Introspection (RFC 7662) is an endpoint for checking token status. Resource Server sends POST /introspect with token, Authorization Server returns: {active: true/false, sub, client_id, scope, exp...}. Used for opaque tokens (not JWT) that cannot be validated locally. Also useful for JWT: checking revocation (if token is blacklisted). Downside: latency on each Authorization Server request. Optimization: caching results (considering exp), using short-lived JWT + introspection for refresh tokens.',
  },
  {
    id: 'sd-auth-046',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'middle',
    type: 'open',
    question: 'How to implement multi-tenant authorization in a SaaS application?',
    sampleAnswer:
      'Multi-tenant authorization challenges: data isolation between tenants, different roles/permissions in different tenants, one user in multiple tenants. Approaches: 1) Tenant context: each request contains tenant_id (in JWT claims, header, URL path /org/{tenant_id}/...). 2) Row-level security: all DB queries filtered by tenant_id (PostgreSQL RLS policies). 3) Separate schemas/databases: full DB-level isolation (harder management). 4) Roles per tenant: user can be admin in tenant A and viewer in tenant B. JWT: {tenants: [{id: A, roles: [admin]}, {id: B, roles: [viewer]}]}. 5) Invitation flow: adding user to tenant via invite. 6) Tenant switching: UI allows switching between tenants. 7) Audit: logging includes tenant_id. 8) Billing/limits: enforcement of quotas per tenant. Implementation: tenant_id in JWT, middleware for extraction and validation, ABAC policies with tenant context.',
    explanation:
      'Multi-tenancy is a fundamental SaaS pattern. Isolation is critical for compliance (one tenant cannot see another\'s data). Slack, Notion, GitHub are examples of multi-tenant authorization with per-workspace roles.',
  },
  {
    id: 'sd-auth-047',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is DPoP (demonstrating proof of possession) in OAuth 2.0?',
    options: [
      'A method for encrypting tokens',
      'A mechanism for binding a token to the client by signing requests with a private key, preventing use of a stolen token',
      'A protocol for device authentication',
      'A method for storing tokens on the server',
    ],
    correctIndex: 1,
    explanation:
      'DPoP (RFC 9449) implements sender-constrained tokens. Client generates a key pair, when requesting a token sends DPoP proof (signed JWT with public key). Access token contains hash of public key (cnf claim). When using token, client signs each request with new DPoP proof. Resource server verifies: proof signature, correspondence of public key to cnf in token. If token is stolen, attacker cannot use it without the private key. Advantages over Token Binding: works at application layer, does not depend on TLS. Supported by Okta, Auth0.',
  },
  {
    id: 'sd-auth-048',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'senior',
    type: 'open',
    question: 'How to ensure secure logout in a distributed system with JWT?',
    sampleAnswer:
      'Problem: JWT is stateless, no centralized session state for invalidation. Logout strategies: 1) Short-lived access tokens: TTL 5-15 minutes, on logout just do not refresh. Residual risk: token valid until exp. 2) Token blacklist: on logout jti added to Redis/DB blacklist. Each request checks blacklist. Trade-off: adds state, Redis request latency. 3) Token versioning: user.token_version in DB, JWT contains version. On logout, version incremented, old tokens invalid. 4) Refresh token revocation: revoke refresh token, access token expires naturally. 5) Distributed cache invalidation: publish logout event, all services update local blacklist. 6) Logout from all devices: revoke all user refresh tokens, increment global token_version. Recommendation: short-lived access (5 min) + refresh token revocation + Redis blacklist for critical cases (password change, security breach).',
    explanation:
      'Secure logout is one of the hardest JWT problems. No perfect solution, all approaches are trade-offs. For most applications, short-lived access + refresh revocation is sufficient. For high-security systems (banking), real-time invalidation via blacklist may be needed.',
  },
  {
    id: 'sd-auth-049',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is consent management in the context of OAuth/OIDC?',
    options: [
      'Managing user passwords',
      'Process of requesting and storing user consent to app access with revocation ability',
      'Synchronizing settings between devices',
      'Managing admin roles',
    ],
    correctIndex: 1,
    explanation:
      'Consent management: when a third-party app requests data access, IdP shows consent screen with requested scopes (Read your email, Access your calendar). User explicitly agrees. Consent is saved — not requested again if scopes are the same (unless user revokes). User can revoke consent in IdP settings. GDPR requires granular consent, revocation ability, transparency. OAuth 2.0 prompt=consent forces consent screen. Incremental consent: app requests new scopes as needed.',
  },
  {
    id: 'sd-auth-050',
    block: 'sd',
    topic: 'auth',
    topicLabel: 'Authentication and Authorization',
    difficulty: 'senior',
    type: 'open',
    question: 'Describe secure authentication architecture for IoT devices.',
    sampleAnswer:
      'IoT authentication challenges: limited resources (CPU, memory), no UI for credential entry, physical device access, scale (millions of devices). Architecture: 1) Device provisioning: at manufacture, device gets unique identity (certificate or symmetric key), stored in secure element (TPM, HSM). 2) Initial bootstrap: device registers with IoT platform (AWS IoT, Azure IoT Hub) using provisioning certificate. 3) Runtime auth: mTLS with device certificate for each cloud connection. Periodic certificate rotation. 4) Token-based: for constrained devices — OAuth 2.0 Device Flow or pre-provisioned tokens. 5) Fleet management: group policies, remote certificate rotation, revocation list. 6) Secure boot: firmware integrity verification on load. 7) Physical security: tamper detection, secure element for key storage. Protocols: MQTT over TLS, CoAP with DTLS. Standards: FIDO Device Onboard (FDO) for zero-touch provisioning.',
    explanation:
      'IoT security is critical — compromised devices used for DDoS (Mirai botnet). AWS IoT Core, Azure IoT Hub provide managed device authentication. Secure element (hardware) is necessary for credential protection against physical extraction.',
  },
];
