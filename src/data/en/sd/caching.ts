import type { Question } from '@/data/types';

export const cachingQuestions: Question[] = [
  {
    id: 'sd-caching-001',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is the main goal of using caching in system design?',
    options: [
      'Reducing the volume of stored data',
      'Reducing latency and load on the primary storage by keeping frequently requested data in fast memory',
      'Ensuring data consistency between services',
      'Protecting data from unauthorized access',
    ],
    correctIndex: 1,
    explanation:
      'Caching is the practice of storing copies of frequently requested data in fast storage (usually RAM). This significantly reduces response latency (from milliseconds for a database query down to microseconds for a memory access) and decreases the load on the primary storage (the database). By the Pareto principle, 80% of requests usually target 20% of the data, which makes caching highly effective.',
  },
  {
    id: 'sd-caching-002',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is the Cache-Aside (Lazy Loading) strategy?',
    options: [
      'Data is written to the cache and the database simultaneously',
      'The application first checks the cache; on a miss it loads data from the database and puts it into the cache',
      'The cache automatically synchronizes with the database via CDC',
      'Data is first written to the cache and then asynchronously flushed to the database',
    ],
    correctIndex: 1,
    explanation:
      'Cache-Aside (Lazy Loading) is the most common caching strategy. The application first queries the cache. On a cache hit, the data is returned directly. On a cache miss, the application loads the data from the database, writes it into the cache, and returns it to the client. Advantages: only data that is actually requested ends up in the cache, and the cache can fail without data loss. Disadvantages: the first request is always slow (cold start), and there is a risk of getting out of sync with the database (stale data).',
  },
  {
    id: 'sd-caching-003',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'middle',
    type: 'open',
    question: 'Compare the Write-Through and Write-Behind (Write-Back) caching strategies. What are the advantages and disadvantages of each?',
    sampleAnswer:
      'Write-Through: data is written to the cache and the database simultaneously within a single operation. Advantages: the cache is always consistent with the database, the model is simple, and there is no risk of data loss. Disadvantages: higher write latency (writing to two stores) and unnecessary cache writes for data that may never be read. Write-Behind (Write-Back): data is written only to the cache and flushed to the database asynchronously (via a queue or on a timer). Advantages: very low write latency, the ability to batch writes to the database, and reduced load on the database. Disadvantages: risk of data loss if the cache fails before the flush, more complex implementation, and eventual consistency.',
    explanation:
      'The choice of strategy depends on the consistency and performance requirements. Write-Through is suitable for systems where consistency is critical (financial data, inventory). Write-Behind is for systems with a high write load where short-term data loss is acceptable (view counters, activity logs). In practice, Cache-Aside is often used for reads in combination with write-through for writes.',
  },
  {
    id: 'sd-caching-004',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'senior',
    type: 'open',
    question: 'What is the "cache stampede" (or "thundering herd") problem? How do you prevent it?',
    sampleAnswer:
      'A cache stampede occurs when a popular cache key expires (TTL expiry), and many concurrent requests simultaneously encounter a cache miss. They all hit the database at the same time for the same data, creating a load spike that can lead to an outage. Ways to prevent it: 1) Locking (mutex): only one request goes to the database while the others wait for the cache to be populated. 2) Probabilistic early expiration: each request has a small probability of refreshing the cache before the TTL expires, spreading refreshes over time. 3) Background refresh: a background process refreshes the cache before the TTL expires. 4) Stale-while-revalidate: return stale data while one request refreshes the cache. 5) A "forever" TTL with explicit invalidation.',
    explanation:
      'Cache stampede is a classic problem in high-load systems. In 2010, Facebook described how a single expired key in Memcached could trigger hundreds of thousands of database requests. Their solution was a lease-based approach: Memcached grants a "lease" to the first request, and the others receive a signal to wait. This is a pattern that must be considered when designing any system with caching at scale.',
  },
  {
    id: 'sd-caching-005',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'junior',
    type: 'quiz',
    question: 'Which eviction policy removes from the cache the item that has gone unused for the longest time?',
    options: [
      'FIFO (First In, First Out)',
      'LFU (Least Frequently Used)',
      'LRU (Least Recently Used)',
      'Random Replacement',
    ],
    correctIndex: 2,
    explanation:
      'LRU (Least Recently Used) is an eviction policy that removes the item that has not been accessed for the longest time. It is based on the assumption that recently used data is more likely to be requested again (the principle of temporal locality). LRU is one of the most popular eviction policies, used in Redis, Memcached, and many other caching systems. It is usually implemented using a combination of a hash table and a doubly linked list.',
  },
  {
    id: 'sd-caching-006',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is TTL (Time To Live) in the context of caching?',
    options: [
      'The time required to write data into the cache',
      'The time after which a cache entry is automatically considered stale and removed',
      'The maximum response time of the cache server',
      'The time it takes to synchronize the cache across multiple nodes',
    ],
    correctIndex: 1,
    explanation:
      'TTL (Time To Live) is the lifetime of a cache entry, after which the entry is considered expired and is removed or refreshed on the next request. TTL helps address the problem of stale data in the cache. Choosing a TTL is a trade-off: a short TTL ensures fresh data but increases the load on the database; a long TTL reduces the load but increases the risk of serving stale data. The TTL can differ for different types of data: reference data — hours, user profiles — minutes, stock quotes — seconds.',
  },
  {
    id: 'sd-caching-007',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What are a cache hit and a cache miss?',
    options: [
      'A cache hit is a cache error, a cache miss is a successful access',
      'A cache hit means the requested data is found in the cache; a cache miss means the data is not in the cache and the primary storage must be accessed',
      'A cache hit is adding data to the cache, a cache miss is removing data from the cache',
      'A cache hit means the cache is completely full, a cache miss means there is free space in the cache',
    ],
    correctIndex: 1,
    explanation:
      'A cache hit is the situation where the requested data is in the cache and is returned directly from it, without accessing the primary storage. A cache miss is when the data is not in the cache, and the system accesses slower storage (the database). The cache hit ratio (the percentage of hits) is a key metric for cache effectiveness. A good value is a 90-99% hit ratio. A low hit ratio means the cache is ineffective and you may need to reconsider the caching strategy or the cache size.',
  },
  {
    id: 'sd-caching-008',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'junior',
    type: 'quiz',
    question: 'Which HTTP header tells the browser how long it can use a cached copy of a resource?',
    options: [
      'Content-Type',
      'Cache-Control',
      'Authorization',
      'X-Forwarded-For',
    ],
    correctIndex: 1,
    explanation:
      'Cache-Control is the main HTTP header for controlling caching. Its directives define the caching policy: max-age=3600 (cache for 1 hour), no-cache (always revalidate freshness with the server), no-store (do not cache at all), public/private (whether it can be cached on intermediate proxies), immutable (the resource will not change). Together with the ETag and Last-Modified headers, Cache-Control provides effective caching on the browser side and the CDN, reducing server load and speeding up loading for users.',
  },
  {
    id: 'sd-caching-009',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'middle',
    type: 'quiz',
    question: 'How does the Read-Through strategy differ from Cache-Aside?',
    options: [
      'In Read-Through the cache itself loads data from the database on a miss; in Cache-Aside the application does it',
      'Read-Through works only with writes, Cache-Aside only with reads',
      'Read-Through does not use TTL, while Cache-Aside does',
      'There is no difference — they are the same thing',
    ],
    correctIndex: 0,
    explanation:
      'In the Cache-Aside strategy, the application manages the cache itself: it checks the cache, and on a miss it loads data from the database and writes it into the cache. In Read-Through, the cache provider itself is responsible for loading data on a miss — the application always talks only to the cache, and the cache itself goes to the database when needed. Read-Through simplifies the application code and centralizes the loading logic, but it requires support from the caching system. Examples of caches with Read-Through support: Ehcache, NCache, Oracle Coherence.',
  },
  {
    id: 'sd-caching-010',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is cache warming?',
    options: [
      'Increasing the cache size during a period of higher load',
      'Pre-populating the cache with data before the real load arrives',
      'Monitoring the temperature of the cache servers',
      'Gradually increasing the TTL of cached entries',
    ],
    correctIndex: 1,
    explanation:
      'Cache warming is pre-populating the cache with data before the system starts accepting real requests. This is important when restarting cache servers, deploying a new version, or launching a new data center. Without warming, all requests would be cache misses, creating a load spike on the database (the cold start problem). Ways to warm the cache: 1) A script that loads frequently requested data. 2) Replaying logs of previous requests. 3) Gradually routing traffic (through a load balancer). 4) Creating a cache snapshot and restoring it.',
  },
  {
    id: 'sd-caching-011',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'middle',
    type: 'open',
    question: 'What is multi-level caching? Give an example of a typical configuration of caching levels in a web application.',
    sampleAnswer:
      'Multi-level caching is the use of several caching levels, each with different characteristics in terms of speed, capacity, and distance from the client. Typical levels in a web application (from client to server): 1) Browser cache (L1) — the HTTP cache in the browser, controlled by the Cache-Control and ETag headers. The fastest — there is no network request. 2) CDN (L2) — a cache on the CDN edge servers close to the user. Caches static assets and sometimes dynamic content. 3) Reverse proxy / API Gateway (L3) — a cache at the level of Nginx, Varnish. Caches API responses. 4) In-process cache (L4) — a cache in the application memory (Guava Cache, Caffeine). The fastest server-side cache, but limited in size and not shared between instances. 5) Distributed cache (L5) — Redis, Memcached. Shared across all instances, but requires a network call. 6) Database cache — the database buffer pool (InnoDB Buffer Pool). Each level provides a different balance of speed, capacity, and data freshness.',
    explanation:
      'Multi-level caching allows each request to be served as close to the client as possible. The higher the level (closer to the client), the faster the response, but the harder the invalidation. The key challenge is ensuring consistency between levels. In practice, cascading invalidation is used: when data is updated, all levels from the database to the CDN are invalidated.',
  },
  {
    id: 'sd-caching-012',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What problem does the "cache invalidation" pattern solve, and why is it considered one of the two hardest problems in Computer Science?',
    options: [
      'Determining the cache size for optimal performance',
      'Choosing the optimal compression algorithm for cached data',
      'Ensuring the timely removal or update of stale data in the cache when the source changes',
      'Distributing the cache across multiple servers',
    ],
    correctIndex: 2,
    explanation:
      'Cache invalidation is the process of removing or updating cache entries when the corresponding data in the primary storage has changed. It is hard because: 1) You need to track all the places where the data is cached (including CDNs, browsers, in-process caches). 2) There is a time window between updating the database and invalidating the cache where the data is inconsistent. 3) In a distributed system, the invalidation must reach all nodes. 4) Race conditions: concurrent reads and writes can lead to stale data being cached. Phil Karlton of Netscape said: "There are only two hard things in Computer Science: cache invalidation and naming things."',
  },
  {
    id: 'sd-caching-013',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'senior',
    type: 'quiz',
    question: 'How does Redis Cluster provide horizontal scaling of a distributed cache?',
    options: [
      'Each node stores a full copy of all the data',
      'Data is automatically distributed across 16384 hash slots among the cluster nodes',
      'The client decides on its own which node to send the request to, without involving the cluster',
      'Redis Cluster does not support horizontal scaling',
    ],
    correctIndex: 1,
    explanation:
      'Redis Cluster uses 16384 hash slots to distribute keys among the nodes. Each node is responsible for a subset of the slots. A key is hashed (CRC16(key) mod 16384) to determine its slot. When a new node is added, some of the slots migrate to it (resharding) without stopping the cluster. Each node knows the mapping of slots to nodes and can redirect the client (a MOVED redirect) to the correct node. For fault tolerance, each master node has a replica that is automatically promoted if the master fails.',
  },
  {
    id: 'sd-caching-014',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'senior',
    type: 'open',
    question: 'What is cache coherence in distributed systems? What strategies exist for ensuring coherence?',
    sampleAnswer:
      'Cache coherence is ensuring the consistency of data across multiple caches when the data can be modified. In a distributed system with multiple application instances, each can have its own in-process cache, and a change made by one instance must be reflected in the caches of the others. Strategies: 1) Centralized cache (Redis/Memcached) — all instances use a single distributed cache, and the coherence problem is solved at the storage level. 2) Invalidation-based — when data changes, a message is sent (via Redis Pub/Sub, Kafka) to all instances with a command to remove the entry from the local cache. 3) Update-based — instead of invalidation, the new value is broadcast to update the cache. 4) TTL-based — each cache refreshes its data on its own when the TTL expires, tolerating short-term inconsistency. 5) Versioning — a version is incremented on each change, and the cached entry is checked against the version. In practice, approaches are combined: a centralized Redis for hot data + invalidation via Pub/Sub for in-process caches.',
    explanation:
      'Cache coherence in distributed systems is analogous to the cache coherence problem in multiprocessor systems, but at a different scale. CPUs use MESI/MOESI protocols; distributed systems use message-passing. The key trade-off: the stricter the coherence, the higher the latency and complexity. For most web applications, eventual consistency through TTL + event-based invalidation is the optimal solution.',
  },
  {
    id: 'sd-caching-015',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'junior',
    type: 'quiz',
    question: 'Which of the listed tools is a distributed cache?',
    options: [
      'PostgreSQL',
      'Redis',
      'Nginx',
      'Prometheus',
    ],
    correctIndex: 1,
    explanation:
      'Redis is a high-performance in-memory data store, widely used as a distributed cache. Redis keeps data in RAM, providing latency on the order of microseconds. It supports various data structures (strings, hashes, lists, sets, sorted sets), TTL for entries, persistence (RDB, AOF), and clustering (Redis Cluster) for horizontal scaling. Memcached is another popular distributed cache, simpler and more specialized.',
  },
  {
    id: 'sd-caching-016',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'middle',
    type: 'open',
    question: 'Explain the HTTP conditional requests mechanism using ETag and If-None-Match. How does it help with caching?',
    sampleAnswer:
      'An ETag (Entity Tag) is a resource version identifier generated by the server (usually a hash of the content or a version number). The mechanism works as follows: 1) The client requests a resource. The server responds with an ETag: "abc123" header. 2) The client caches the resource and the ETag. 3) On the next request, the client sends an If-None-Match: "abc123" header. 4) The server compares the ETag: if the resource has not changed, it returns 304 Not Modified with no response body (saving bandwidth). If it has changed, it returns 200 OK with the new resource and a new ETag. The Last-Modified / If-Modified-Since pair works similarly, but based on the modification date. Conditional requests save bandwidth and reduce server load, but they do not eliminate the request itself (unlike Cache-Control: max-age). The optimal approach is to combine them: Cache-Control for static resources (JS, CSS with a hash in the file name) and ETag for dynamic resources.',
    explanation:
      'Conditional requests are an important part of HTTP caching, especially for resources that cannot be cached for long (Cache-Control: no-cache). They allow the server to quickly confirm the freshness of the cached version without transferring the full content. This is especially effective for large resources (images, documents).',
  },
  {
    id: 'sd-caching-017',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'senior',
    type: 'quiz',
    question: 'Which approach to cache invalidation minimizes the load on the database when the TTL of a popular key expires simultaneously?',
    options: [
      'Set the same TTL for all keys',
      'Use a mutex (lock) — only one request refreshes the cache while the others wait',
      'Delete all cache keys on every database update',
      'Do not use TTL at all',
    ],
    correctIndex: 1,
    explanation:
      'The mutex (lock) approach is one of the main ways to prevent a cache stampede. When several requests encounter a cache miss for the same key, only the first one acquires the lock (for example, via Redis SETNX) and loads the data from the database. The remaining requests wait (with a short sleep and retry) or receive slightly stale data (stale-while-revalidate). Other approaches: probabilistic early expiration (each request has a small probability of refreshing the cache before the TTL expires) and background refresh (a background process refreshes the cache before the TTL expires).',
  },
  {
    id: 'sd-caching-018',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'senior',
    type: 'open',
    question: 'Compare Redis and Memcached as solutions for distributed caching. In which scenarios is each preferable?',
    sampleAnswer:
      'Redis: supports a variety of data structures (strings, hashes, lists, sets, sorted sets, HyperLogLog, streams), persistence (RDB snapshots, AOF log), replication, clustering (Redis Cluster), Lua scripting, pub/sub, and transactions. It is preferable when: you need complex data structures, you need persistence, you need pub/sub, or you require clustering with automatic failover. Memcached: a simple key-value cache, a multi-threaded architecture (better utilizes multi-core CPUs), a simple protocol, and automatic slab-based memory management. It is preferable when: you need the simplest possible cache, the data is only strings/serialized objects, you need maximum performance on a multi-core server, and you do not need persistence. In practice, Redis dominates thanks to its versatility, but Memcached remains relevant for pure caching scenarios with maximum performance (used at Facebook/Meta).',
    explanation:
      "Both tools provide latency on the order of microseconds and are widely used in production. Redis, starting from version 6, supports multi-threaded I/O, narrowing the performance gap with Memcached. The choice often depends on the ecosystem and the team's experience. In cloud environments, managed services are available: AWS ElastiCache supports both.",
  },
  {
    id: 'sd-caching-019',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is "cache penetration" and how do you protect against it?',
    options: [
      'An attack in which an adversary gains access to data through the cache',
      'A situation where requests for non-existent keys always pass through to the database, bypassing the cache',
      'A memory overflow of the cache server',
      'A data leak from the cache when it is restarted',
    ],
    correctIndex: 1,
    explanation:
      'Cache penetration is a problem where frequent requests for non-existent data pass through to the database every time, bypassing the cache (because there is nothing to cache). This can be either a legitimate scenario or an attack. Ways to protect against it: 1) Caching null values — write an "empty" result into the cache with a short TTL. 2) Bloom Filter — a probabilistic data structure that checks whether a key exists without accessing the database. 3) Input validation — filter out obviously invalid requests before the cache.',
  },
  {
    id: 'sd-caching-020',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'senior',
    type: 'open',
    question: 'Describe a caching strategy for a write-heavy workload. What patterns and trade-offs must be considered?',
    sampleAnswer:
      'For write-heavy systems, the standard caching strategies (Cache-Aside, Read-Through) are less effective because data is frequently invalidated. Approaches: 1) Write-Behind (Write-Back) — writes are buffered in the cache and asynchronously flushed to the database in batches (batching). Reduces the load on the database, but there is a risk of loss if the cache fails. 2) CQRS — separating the read and write models, with the cache serving only reads from a separate read model. 3) Event-driven invalidation — on a write, an event is sent via Kafka/Redis Pub/Sub to invalidate the caches of all instances. 4) Write-Around — the write goes only to the database, bypassing the cache. The cache is populated on read (Cache-Aside). Suitable when the written data is rarely read immediately. 5) Aggregating writes — instead of writing each increment (a view counter), aggregate in Redis (INCR) and periodically flush to the database. Trade-offs: durability vs performance, consistency vs latency, complexity vs reliability.',
    explanation:
      'Write-heavy systems (metrics, logs, IoT data, social media feeds) require a fundamentally different approach to caching than read-heavy ones. The key principle is to minimize synchronous writes to the database through buffering and aggregation. Systems like Twitter use fan-out-on-write (writing to the timeline cache of each follower when a tweet is published) rather than fan-out-on-read.',
  },
  {
    id: 'sd-caching-021',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is an in-memory cache and what is its main advantage?',
    options: [
      'A cache stored on an SSD disk',
      'A cache in RAM that provides access in microseconds',
      'A cache in cloud storage',
      'A cache distributed across processors',
    ],
    correctIndex: 1,
    explanation:
      'An in-memory cache stores data in RAM, which provides access times on the order of microseconds (compared to milliseconds for disk or network). Examples: Redis, Memcached for a distributed cache; Guava Cache, Caffeine for a local cache within the application. The limitation is the amount of memory, which is why eviction policies (LRU, LFU) are important.',
  },
  {
    id: 'sd-caching-022',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is a local cache and how does it differ from a distributed one?',
    options: [
      'A local cache is located in the geographically nearest data center',
      'A local cache is stored in the memory of a single process/server and is not accessible to other instances',
      'A local cache uses a local database',
      'A local cache works only for local users',
    ],
    correctIndex: 1,
    explanation:
      'A local (in-process) cache is stored in the memory of a single application process. Advantages: no network calls (faster), simplicity. Disadvantages: it is not shared between instances (data duplication), it is limited by the process memory, and invalidation is harder. A distributed cache (Redis) is accessible to all instances but requires a network call. They are often combined: a local L1 + a distributed L2.',
  },
  {
    id: 'sd-caching-023',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'junior',
    type: 'quiz',
    question: 'Which eviction policy removes from the cache the item that has been used the least frequently?',
    options: [
      'LRU (Least Recently Used)',
      'LFU (Least Frequently Used)',
      'FIFO (First In First Out)',
      'Random',
    ],
    correctIndex: 1,
    explanation:
      'LFU (Least Frequently Used) removes the item with the lowest number of accesses. Unlike LRU (which removes the least recently used item), LFU takes into account the frequency of use over all time. LFU is better suited when access frequency matters more than recency. Disadvantage: new items can be evicted immediately because their counter is low. There is also TinyLFU — an optimized variant.',
  },
  {
    id: 'sd-caching-024',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'junior',
    type: 'open',
    question: 'Explain what a cache key is and what factors need to be considered when designing one.',
    sampleAnswer:
      'A cache key is a unique identifier for an entry in the cache. When designing one, you need to consider: 1) Uniqueness — the key must unambiguously identify the data. For an API: method + URL + parameters + version. 2) Hierarchy — include context: user:123:profile vs global:config. 3) Length — keys that are too long consume memory and slow down lookups. 4) Readability — for debugging, the key should be understandable. 5) Invalidation — the structure should allow invalidating groups (user:123:* — all of a user\'s data). 6) Hash collision — for complex keys, use a hash, but remember about collisions. Example: product:category:electronics:page:1:sort:price.',
    explanation:
      'A well-designed cache key is the foundation of effective caching. Bad keys lead to: cache pollution (unnecessary data), cache misses (overly specific keys), and difficulty with invalidation. It is recommended to use the namespace:entity:id:attribute pattern.',
  },
  {
    id: 'sd-caching-025',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is the write-around caching strategy?',
    options: [
      'Data is written only to the cache, bypassing the database',
      'Data is written directly to the database, bypassing the cache; the cache is populated on read',
      'Data is written to the cache and the database simultaneously',
      'The cache is periodically synchronized with the database',
    ],
    correctIndex: 1,
    explanation:
      'Write-around is a strategy in which the write goes directly to the database, bypassing the cache. The cache is populated only on read (as in Cache-Aside). Advantages: it does not pollute the cache with data that may not be read immediately after the write. Disadvantages: the first read after a write is a cache miss. It is suitable when the written data is rarely read immediately (logs, historical records).',
  },
  {
    id: 'sd-caching-026',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'middle',
    type: 'quiz',
    question: 'Which HTTP header allows you to specify that a resource may be cached only in the private browser cache, but not on intermediate proxies?',
    options: [
      'Cache-Control: no-store',
      'Cache-Control: private',
      'Cache-Control: public',
      'Cache-Control: no-cache',
    ],
    correctIndex: 1,
    explanation:
      'Cache-Control: private indicates that the response is intended for a single user and may be cached only in the private browser cache, but not on a CDN or intermediate proxies. It is used for personalized content (a personal account, a shopping cart). public — allows caching everywhere. no-cache — requires revalidation. no-store — completely forbids caching.',
  },
  {
    id: 'sd-caching-027',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'middle',
    type: 'open',
    question: 'How do you implement caching in a microservices architecture? Which caching levels should be used?',
    sampleAnswer:
      'Caching in microservices is multi-level: 1) API Gateway cache — caching responses at the entry level; suitable for public, rarely changing data. 2) Service-level cache — each service has its own cache (Redis) for frequently requested data within its domain. Do not share the cache between services — that violates the bounded context. 3) In-process cache — Caffeine/Guava in each instance for hot data with a short TTL. 4) Database query cache — caching the results of frequent queries. 5) Sidecar cache — in a service mesh, the cache can be in a sidecar proxy. Invalidation: event-driven via Kafka/Redis Pub/Sub — when data changes, the service publishes an event, and other services invalidate their caches. Important: do not cache data that another service "owns" — request it through the API.',
    explanation:
      'Caching in microservices is harder than in a monolith because of the distribution and independence of services. The key principle is that each service is responsible for caching its own data. Cross-service caching creates coupling and consistency problems.',
  },
  {
    id: 'sd-caching-028',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is the cache-aside pattern with lazy loading, and when is it NOT suitable?',
    options: [
      'It is always suitable for any scenario',
      'It is not suitable when consistency is critical and you need up-to-date data right after a write',
      'It is not suitable for read-heavy systems',
      'It is not suitable when using Redis',
    ],
    correctIndex: 1,
    explanation:
      'Cache-Aside with lazy loading: on a read, the cache is checked, and on a miss, the data is loaded from the database and written into the cache. It is not suitable when: 1) Data is read immediately after a write — it will be a cache miss. 2) Strong consistency is critical — there is a window of inconsistency between updating the database and invalidating the cache. 3) The first request is important — the cold start problem. Alternatives: Write-Through for consistency, Cache Warming for the cold start.',
  },
  {
    id: 'sd-caching-029',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'middle',
    type: 'quiz',
    question: 'Which Redis data type is best suited for caching sorted lists with the ability to query by range?',
    options: [
      'String',
      'Hash',
      'Sorted Set (ZSET)',
      'List',
    ],
    correctIndex: 2,
    explanation:
      'A Redis Sorted Set (ZSET) stores unique elements with a numeric score and supports efficient operations: ZRANGE/ZREVRANGE (selection by index range), ZRANGEBYSCORE (selection by score range), ZRANK (the position of an element). It is ideal for: leaderboards (score = points), time-based feeds (score = timestamp), pagination of sorted data. Operation complexity: O(log N) for adding, O(log N + M) for a range selection.',
  },
  {
    id: 'sd-caching-030',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'middle',
    type: 'open',
    question: 'Explain the Refresh-Ahead (proactive cache refresh) pattern. When is it preferable to the standard TTL-based approach?',
    sampleAnswer:
      'Refresh-Ahead is the proactive refreshing of the cache before the TTL expires. When a request arrives for an entry that has less than X% of its lifetime remaining, a refresh from the database is started asynchronously, while the current request receives the existing data. Advantages: 1) No cache miss when the TTL expires — the data is always fresh. 2) No cache stampede — the refresh is asynchronous and handled by a single thread. 3) Stable latency — no sudden database accesses. When to use it: for popular data with a predictable access pattern; when latency is critical and a cache miss is unacceptable; for data updated by an external source (currency rates, weather). Implementation: a background process or a trigger on read that checks the remaining TTL.',
    explanation:
      'Refresh-Ahead is an advanced pattern for high-traffic systems. Netflix uses it for caching movie metadata — the data is always up to date and the latency is stable. It requires additional infrastructure (background jobs), but it pays off for critical hot data.',
  },
  {
    id: 'sd-caching-031',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is cache sharding and what problem does it solve?',
    options: [
      'Splitting the cache by data type',
      'Distributing the cache data across multiple nodes for horizontal scaling',
      'Encrypting the data in the cache',
      'Backing up the cache',
    ],
    correctIndex: 1,
    explanation:
      'Cache sharding is the distribution of data across multiple cache nodes by key (consistent hashing). It solves the problem of the limited memory and throughput of a single node. Each key is hashed to determine the target node. Redis Cluster uses 16384 hash slots. Advantages: horizontal scaling, load distribution. Difficulties: cross-shard operations (MGET on keys located on different shards), re-sharding when nodes are added.',
  },
  {
    id: 'sd-caching-032',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'senior',
    type: 'quiz',
    question: 'Which Redis mechanism provides atomic execution of multiple commands?',
    options: [
      'Pipelining',
      'Pub/Sub',
      'Transactions (MULTI/EXEC) or Lua scripts',
      'Replication',
    ],
    correctIndex: 2,
    explanation:
      'Redis Transactions (MULTI/EXEC) group commands for atomic execution — no other commands run between them. Lua scripts execute atomically on the server — the preferred way for complex logic. Pipelining — sending multiple commands without waiting for a response — is not atomic, it is just a latency optimization. Important: Redis transactions do not support rollback — all commands will be executed even if one of them errors.',
  },
  {
    id: 'sd-caching-033',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'senior',
    type: 'open',
    question: 'How do you implement a distributed rate limiter using Redis? What rate limiting algorithms exist?',
    sampleAnswer:
      'Rate limiting restricts the number of requests over a period. Algorithms: 1) Fixed Window — a counter of requests in a fixed window (a minute). Redis: INCR key, EXPIRE. Problem: a burst at the window boundary (100 at the end of one minute + 100 at the start of the next = 200 in 2 seconds). 2) Sliding Window Log — store the timestamp of each request and count over a sliding window. Redis: ZADD + ZRANGEBYSCORE. Accurate, but consumes memory. 3) Sliding Window Counter — a combination: a weighted average of the previous and current windows. A trade-off between accuracy and memory. 4) Token Bucket — tokens accumulate at a constant rate, and a request consumes a token. A Redis Lua script for atomicity. Allows bursts. 5) Leaky Bucket — requests go into a queue and are processed at a constant rate. Implementation in Redis: a Lua script for an atomic check-and-update, SET with NX/EX for a distributed lock.',
    explanation:
      'Distributed rate limiting is a classic task for Redis. Token Bucket is the most flexible algorithm (it allows bursts). For APIs, a sliding window or token bucket is usually used. Important: the rate limiter itself can become a bottleneck — Redis replication is needed.',
  },
  {
    id: 'sd-caching-034',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'senior',
    type: 'open',
    question: 'Explain a caching strategy for personalized content (recommendations, user feeds). What specifics need to be considered?',
    sampleAnswer:
      'Personalized content is hard to cache because it is unique for each user. Strategies: 1) Per-user cache key — cache by user:123:feed. Works for active users, but the cache hit rate is low for long-tail users. 2) Materialized feeds — precompute and store each user\'s feed (fan-out-on-write). Twitter uses this for active users. Expensive in terms of memory. 3) Compute on read with caching of components — do not cache the whole feed, but cache: users (user:123:profile), posts (post:456), recommendation vectors. 4) Edge caching with ESI (Edge Side Includes) — the CDN assembles the page from cacheable and personal blocks. 5) Segment-based caching — group users into segments (interests, region) and cache for the segment. 6) Hybrid — materialized for top users, compute for the rest.',
    explanation:
      'Personalization is one of the main challenges for caching. Netflix and Spotify use a multi-level approach: they cache models and vectors and compute recommendations near-realtime. It is important to find a balance between personalization and cache efficiency.',
  },
  {
    id: 'sd-caching-035',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is stale data in the context of caching?',
    options: [
      'Encrypted data in the cache',
      'Outdated data in the cache that no longer matches the source',
      'Data compressed to save space',
      'Data marked for deletion',
    ],
    correctIndex: 1,
    explanation:
      'Stale data is outdated data in the cache that does not match the current state in the primary storage. It arises from: a long TTL, invalidation delay, race conditions during updates. Consequences: the user sees outdated information. Solutions: a short TTL, event-driven invalidation, versioning. Sometimes stale data is acceptable (stale-while-revalidate) to improve performance.',
  },
  {
    id: 'sd-caching-036',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What type of caching is used in the browser to store the resources of a web page?',
    options: [
      'Server-side cache',
      'Distributed cache',
      'Browser HTTP cache',
      'Database cache',
    ],
    correctIndex: 2,
    explanation:
      'The browser HTTP cache stores resources (HTML, CSS, JS, images) locally in the browser based on HTTP headers (Cache-Control, ETag, Last-Modified). On a repeat request, the browser either uses the cached copy (when max-age is valid) or makes a conditional request (If-None-Match) to check freshness. This is the fastest cache — there is no network request at all.',
  },
  {
    id: 'sd-caching-037',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is cache eviction and when does it happen?',
    options: [
      'Adding data to the cache',
      'Removing data from the cache to free up space or upon TTL expiry',
      'Updating data in the cache',
      'Replicating the cache between servers',
    ],
    correctIndex: 1,
    explanation:
      'Cache eviction is the removal of entries from the cache. It happens: 1) Upon TTL expiry (time-based eviction). 2) When memory runs low (space-based eviction) — an LRU, LFU, or FIFO policy is applied. 3) Upon explicit invalidation (explicit eviction) — a DELETE by key. Redis uses: volatile-lru (LRU among keys with a TTL), allkeys-lru (LRU across all keys), noeviction (an error when memory runs low), and other policies.',
  },
  {
    id: 'sd-caching-038',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'middle',
    type: 'open',
    question: 'How do you cache the results of API requests that depend on many parameters? How do you manage cache key explosion?',
    sampleAnswer:
      'With many parameters, the number of unique keys can explode (cache key explosion), reducing the hit rate. Strategies: 1) Parameter normalization — sort the parameters, convert to lowercase, remove default values. GET /users?sort=name&page=1 = GET /users?page=1&sort=name. 2) Selective caching — cache only popular parameter combinations. Track the hit rate by key patterns. 3) Parameter bucketing — round numeric parameters (price: 99 -> 100), limit the possible values. 4) Layered caching — cache the base result without filters and apply the filters at the application level. 5) Computed cache keys — hash the parameters for a fixed-length key. 6) TTL tiering — popular combinations with a long TTL, rare ones with a short TTL or no caching.',
    explanation:
      'Cache key explosion is a real problem for APIs with flexible parameters. The solution depends on the usage patterns. Log analysis shows which combinations are actually used — often 90% of requests target 10% of the combinations.',
  },
  {
    id: 'sd-caching-039',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is a CDN edge cache and what content is usually cached in it?',
    options: [
      'A database cache on edge servers',
      'A cache of static and dynamic content on CDN servers that are geographically close to users',
      'A cache of the application source code',
      'A cache of the server configuration',
    ],
    correctIndex: 1,
    explanation:
      'A CDN edge cache is a cache on the CDN edge servers (Points of Presence) located closer to the end users. What is cached: static assets (images, CSS, JS, video), API responses with the appropriate headers, rendered HTML. It is controlled through Cache-Control headers and CDN rules. The edge cache radically reduces latency (tens vs hundreds of milliseconds) and offloads the origin servers.',
  },
  {
    id: 'sd-caching-040',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'senior',
    type: 'quiz',
    question: 'Which pattern allows returning stale data from the cache while fresh data is being loaded in the background?',
    options: [
      'Write-Through',
      'Stale-While-Revalidate',
      'Cache-Aside',
      'Read-Through',
    ],
    correctIndex: 1,
    explanation:
      'Stale-While-Revalidate is a pattern in which the cache returns stale data immediately while simultaneously starting a background refresh. The client gets a fast response (even if stale), and the next request will get fresh data. It is used in HTTP (Cache-Control: stale-while-revalidate=60), SWR libraries (React), and CDNs. It is ideal when speed matters more than absolute freshness (news feeds, catalogs).',
  },
  {
    id: 'sd-caching-041',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'senior',
    type: 'open',
    question: 'How do you implement caching for a GraphQL API? What features of GraphQL make caching harder?',
    sampleAnswer:
      'GraphQL makes caching harder: 1) POST requests — browsers and CDNs do not cache POST by default. 2) Dynamic queries — each client can request different fields, creating unique queries. 3) Nesting — a single query can touch many entities. Strategies: 1) Persisted Queries — replace the query with a hash, turn it into a GET request, and cache it on the CDN. 2) Response caching — cache the full response by the hash of the query + variables. 3) Normalized cache (Apollo Client) — the client-side cache normalizes data by id and reuses it across queries. 4) Field-level caching — cache the resolvers of individual fields (the @cacheControl directive). 5) DataLoader — batching and caching at the request level to prevent N+1. 6) HTTP caching hints — return Cache-Control based on the minimum TTL of the fields in the response.',
    explanation:
      'GraphQL caching is an actively developing area. Apollo Server supports cache control directives; Stellate (formerly GraphCDN) is a specialized CDN for GraphQL. The key principle is to cache at the lowest possible level (field/entity) for better reuse.',
  },
  {
    id: 'sd-caching-042',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is cache warming through traffic replay?',
    options: [
      'Copying the cache from production to staging',
      'Replaying real requests from logs to populate the cache before launch',
      'Gradually increasing the TTL of entries',
      'Synchronizing the cache between data centers',
    ],
    correctIndex: 1,
    explanation:
      'Traffic replay is a cache warming method in which real requests from logs are replayed against a new instance to populate the cache. Tools: GoReplay, tcpreplay. Advantages: the cache is populated with real data in real proportions. It is used when: launching a new cache cluster, migrating between versions, disaster recovery. Important: replay during off-peak or with a rate limit so as not to overload the database.',
  },
  {
    id: 'sd-caching-043',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'junior',
    type: 'open',
    question: 'What is Memcached and how does it differ from Redis?',
    sampleAnswer:
      'Memcached is a high-performance distributed cache that stores key-value pairs in memory. Key differences from Redis: 1) Data structures: Memcached — only strings; Redis — strings, hashes, lists, sets, sorted sets, streams, and others. 2) Persistence: Memcached — only in-memory; Redis — supports RDB/AOF persistence. 3) Replication: Memcached — no built-in support; Redis — master-replica replication. 4) Multi-threading: Memcached — multi-threaded; Redis — single-threaded (multi-threaded I/O since v6). 5) Protocol: Memcached — a simple text/binary protocol; Redis — the richer RESP. When to use Memcached: a simple key-value cache, maximum performance, or when it is already used in the infrastructure. When to use Redis: you need data structures, persistence, pub/sub, or a universal solution.',
    explanation:
      'Memcached remains relevant for pure caching scenarios thanks to its simplicity and performance. Facebook (Meta) is the largest user of Memcached in the world. Redis dominates thanks to its versatility — it is not only a cache, but also a data structure store, a queue, and pub/sub.',
  },
  {
    id: 'sd-caching-044',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'middle',
    type: 'quiz',
    question: 'Which Redis command lets you set a value only if the key does not exist (atomically)?',
    options: [
      'SET key value',
      'SETNX key value or SET key value NX',
      'GET key',
      'APPEND key value',
    ],
    correctIndex: 1,
    explanation:
      'SETNX (SET if Not eXists) or SET key value NX sets the value only if the key does not exist. It returns 1 (success) or 0 (the key already exists). It is used for: distributed locks (SET lock_key value NX EX 10), preventing overwrites, cache stampede prevention (only one request goes to the database). SET with the NX (not exists) and XX (only if exists) options is the more modern way.',
  },
  {
    id: 'sd-caching-045',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'middle',
    type: 'open',
    question: 'How do you implement cache invalidation when related data is updated (cascading invalidation)?',
    sampleAnswer:
      'Cascading invalidation is the invalidation of dependent keys when the source data changes. Approaches: 1) Tag-based invalidation — each key is associated with tags (user:123, product:456). When user:123 changes, all keys with that tag are invalidated. Implementation: a Redis SET to store the tag -> keys relationships. 2) Dependency graph — an explicit dependency graph: a profile depends on a user and settings. On a change — traverse the graph. 3) Event-driven — on an update, publish an event, and the subscribers know which keys to invalidate. 4) Pattern-based — use wildcards: SCAN MATCH user:123:* and delete all. Slow but simple. 5) Versioning — instead of invalidation, change the version in the key: user:123:v2:profile. Old versions expire naturally. Important: invalidation should be atomic or idempotent.',
    explanation:
      'Cascading invalidation is one of the hardest caching problems. In practice, a conservative approach is often used: invalidate more than necessary to guarantee consistency. Tag-based invalidation is implemented in some cache libraries (Symfony Cache).',
  },
  {
    id: 'sd-caching-046',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is cache coalescing (or request coalescing)?',
    options: [
      'Merging multiple cache servers into one',
      'Combining identical concurrent requests into one to prevent duplicate loads',
      'Compressing data in the cache',
      'Merging multiple cache keys',
    ],
    correctIndex: 1,
    explanation:
      'Request coalescing is the combining of several concurrent requests for the same key into a single request to the source. When 100 requests simultaneously encounter a cache miss, only one goes to the database while the rest wait for the result. It prevents a cache stampede. Implementations: singleflight (Go), async memoization, Nginx proxy_cache_lock. It is important to configure a wait timeout so as not to block for too long.',
  },
  {
    id: 'sd-caching-047',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'senior',
    type: 'open',
    question: 'How do you design caching for a system with a high availability requirement? What fault tolerance patterns are applied?',
    sampleAnswer:
      'High availability caching: 1) Redis Sentinel — monitoring master-replica with automatic failover. Sentinel processes watch the master and promote a replica if it fails. 2) Redis Cluster — sharding + replication with automatic failover at the slot level. 3) Multi-tier caching — an L1 in-process cache + an L2 distributed cache. If L2 fails, the system degrades but keeps working. 4) Circuit breaker — when the cache is unavailable, go directly to the database temporarily without overloading it with cache retries. 5) Fallback to stale — on a cache error, return stale data (if stored locally). 6) Write-through with async — if the cache fails, continue writing to the database and restore the cache later. 7) Geographic replication — Redis Enterprise Active-Active for multi-DC. 8) Cache-aside graceful degradation — the system works even with a complete cache failure (slower, but it works).',
    explanation:
      'The cache must not be a single point of failure. The rule: the system should work (albeit slower) with a complete cache failure. Netflix uses fallback patterns everywhere — when there are problems with the cache, they return generic recommendations instead of personalized ones.',
  },
  {
    id: 'sd-caching-048',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What problem arises when using a local cache across multiple application instances?',
    options: [
      'A local cache works slower than a distributed one',
      'Data in different instances can differ (inconsistency)',
      'A local cache requires more memory',
      'A local cache does not support TTL',
    ],
    correctIndex: 1,
    explanation:
      'When using a local cache across multiple instances, the data becomes inconsistent: instance A updated the data and its cache, but instance B still serves the old data from its local cache. Solutions: 1) Event-driven invalidation — on an update, broadcast an event to all instances (Redis Pub/Sub). 2) A short TTL — reduce the window of inconsistency. 3) Use a distributed cache instead of a local one. 4) A combination: a local cache with a very short TTL + a distributed cache.',
  },
  {
    id: 'sd-caching-049',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is the cache miss penalty and how do you minimize it?',
    options: [
      'A penalty for using a cache that is too large',
      'The time and resources spent loading data on a cache miss',
      'The cost of write operations to the cache',
      'The overhead of maintaining cache consistency',
    ],
    correctIndex: 1,
    explanation:
      'The cache miss penalty is the time and resources spent retrieving data from the source on a cache miss. It includes: the network latency to the database, the query execution time, serialization, and writing to the cache. Minimization: 1) Cache warming — pre-populating the cache. 2) Read-ahead — preemptively loading related data. 3) Optimizing database queries — indexes, denormalization. 4) Multi-tier caching — an L1 miss -> L2 hit is faster than an L1 miss -> DB. 5) Background refresh — refreshing before the TTL expires.',
  },
  {
    id: 'sd-caching-050',
    block: 'sd',
    topic: 'caching',
    topicLabel: 'Caching',
    difficulty: 'senior',
    type: 'open',
    question: 'Describe a caching architecture for an e-commerce platform with millions of products and high requirements for the freshness of prices and availability.',
    sampleAnswer:
      'E-commerce caching architecture: 1) Catalog (products) — a long TTL (hours), invalidation on an event from the PIM. Elasticsearch + Redis for search and product cards. 2) Prices — a short TTL (minutes) or event-driven updates. Critical for the business, staleness is unacceptable. A separate price service with a hot cache. 3) Inventory (availability) — real-time or near-real-time. Write-through to Redis on changes. For display — eventual consistency is OK; for placing an order — a strict check. 4) User cart — Redis with persistence, per-user keys. 5) Search results — cache popular queries, invalidate when prices in the results change (tag-based). 6) Category pages — ESI/fragment caching, with product cards cached separately. 7) Recommendations — precomputed and cached, updated in batches. 8) CDN layer — product static assets, HTML for SEO. Multi-tier: Browser -> CDN -> Nginx -> Application (L1) -> Redis (L2) -> Database.',
    explanation:
      'E-commerce caching is a balance between performance and freshness. Prices and availability are critical — showing a wrong price = a legal and reputational problem. Amazon uses separate systems for "browse" (eventual consistency is OK) and "buy" (strict consistency).',
  },
];
