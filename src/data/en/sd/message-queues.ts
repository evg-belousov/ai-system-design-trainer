import type { Question } from '@/data/types';

export const messageQueuesQuestions: Question[] = [
  {
    id: 'sd-mq-001',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is the main problem that message queues solve in distributed systems?',
    options: [
      'Encrypting data in transit between services',
      'Asynchronous communication between services and decoupling of components',
      'Storing persistent data instead of a database',
      'Load balancing across servers',
    ],
    correctIndex: 1,
    explanation:
      'Message queues let services exchange data asynchronously: the sender places a message in the queue and continues working without waiting for it to be processed. This provides decoupling -- services do not depend on each other directly, which improves fault tolerance and scalability. Queues also smooth out load spikes (load leveling): during a burst of requests, messages accumulate in the queue and are processed as resources become available.',
  },
  {
    id: 'sd-mq-002',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'middle',
    type: 'quiz',
    question: 'How does the "queue" model (Point-to-Point) differ from the "publish-subscribe" (Pub/Sub) model?',
    options: [
      'In a queue, a message is processed by one consumer; in Pub/Sub it is delivered to all subscribers',
      'A queue works synchronously, Pub/Sub asynchronously',
      'Pub/Sub guarantees message order, while a queue does not',
      'In a queue you cannot filter messages, but in Pub/Sub you can',
    ],
    correctIndex: 0,
    explanation:
      'In the Point-to-Point (queue) model, each message is processed by exactly one consumer. This is suitable for task distribution: for example, processing orders where each order must be processed once. In the Pub/Sub (publish-subscribe) model, a message is delivered to all subscribers of a topic. This is suitable for broadcasting events: for example, an "order created" event can be received by the notification service, the analytics service, and the inventory service.',
  },
  {
    id: 'sd-mq-003',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'middle',
    type: 'open',
    question: 'Compare Apache Kafka and RabbitMQ. In which scenarios is each preferable?',
    sampleAnswer:
      'Apache Kafka is a distributed stream-processing platform with a log architecture. Messages are stored on disk in ordered logs, and consumers read by offset. Kafka is optimal for: stream processing, event sourcing, log aggregation, high throughput (millions of messages/sec), and when you need to retain message history and re-read it. RabbitMQ is a classic message broker with support for the AMQP protocol. It provides flexible routing through exchanges (direct, topic, fanout, headers), delivery acknowledgements, TTL, and dead-letter queues. RabbitMQ is better for: traditional task queues, complex message routing, when you need message priorities, and when the data volume is moderate and each message is processed once.',
    explanation:
      'Kafka and RabbitMQ solve different problems, although their use cases partially overlap. The key architectural difference: in Kafka, consumers manage their own offset (pull model); in RabbitMQ, the broker pushes messages to consumers (push model). Kafka is better suited for big data and stream processing, RabbitMQ for microservice communication and task queues.',
  },
  {
    id: 'sd-mq-004',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'senior',
    type: 'open',
    question: 'What are message delivery guarantees? Explain the difference between at-most-once, at-least-once, and exactly-once. Why is exactly-once so hard to implement?',
    sampleAnswer:
      'At-most-once: a message is delivered no more than once. The sender does not retry on error, so messages may be lost. The simplest and fastest implementation. Suitable for non-critical data (metrics, logs). At-least-once: a message is delivered at least once. The sender retries until it receives an acknowledgement, so duplicates are possible. The consumer must be idempotent. The most common guarantee. Exactly-once: a message is delivered exactly once. Hard to implement, because in a distributed system it is impossible to atomically perform "receive message + process + acknowledge". Any step can fail. It is implemented through a combination of: an idempotent producer (Kafka with idempotence), transactions (Kafka Transactions), and deduplication on the consumer side (by a unique message ID). In practice, "exactly-once" often means "at-least-once delivery + idempotent processing".',
    explanation:
      'Exactly-once semantics is the "holy grail" of distributed systems. Theoretically it is impossible in the general case (essentially the Two Generals Problem). In practice, systems like Apache Kafka provide exactly-once within their own ecosystem (Kafka Streams, Kafka Connect), but when interacting with external systems you have to rely on the idempotency of operations.',
  },
  {
    id: 'sd-mq-005',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is a Dead Letter Queue (DLQ)?',
    options: [
      'A queue for storing encrypted messages',
      'A special queue for messages that could not be processed after several attempts',
      'A queue with the highest processing priority',
      'A temporary queue that is deleted after all messages are processed',
    ],
    correctIndex: 1,
    explanation:
      'A Dead Letter Queue (DLQ) is a special queue to which messages are redirected when they could not be processed successfully after a given number of attempts. Reasons for ending up in a DLQ: deserialization errors, business validation failures, unavailability of dependent services. A DLQ lets you avoid losing problematic messages and analyze them later, without blocking the processing of the other messages in the main queue.',
  },
  {
    id: 'sd-mq-006',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What are a producer and a consumer in the context of message queues?',
    options: [
      'Producer is the queue administrator, consumer is queue monitoring',
      'Producer is the component that sends messages to the queue, consumer is the component that reads and processes messages from the queue',
      'Producer is the queue creator, consumer is the one that deletes the queue',
      'Producer encrypts messages, consumer decrypts them',
    ],
    correctIndex: 1,
    explanation:
      'A producer (publisher) is a system component that creates and sends messages to a queue or topic. A consumer (subscriber) is a component that reads messages from the queue and processes them. The producer and consumer work independently (decoupling): the producer does not know who will process the message or when. This allows producers and consumers to be scaled independently, and it provides resilience to the temporary unavailability of one of the components.',
  },
  {
    id: 'sd-mq-007',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is the main advantage of asynchronous communication through message queues over synchronous HTTP calls?',
    options: [
      'Asynchronous communication is always faster than synchronous',
      'Queues provide service decoupling, load-spike smoothing, and fault tolerance',
      'Queues do not require a network connection',
      'Asynchronous communication is easier to debug',
    ],
    correctIndex: 1,
    explanation:
      'The main advantages of queues: 1) Decoupling -- the sender does not depend on the availability of the receiver. 2) Load leveling -- during a load spike, messages are buffered in the queue and processed as resources become available. 3) Fault tolerance -- if a consumer goes down, messages are preserved in the queue. 4) Scaling -- you can add consumers to increase throughput. Drawbacks: complexity of debugging, eventual consistency, additional infrastructure.',
  },
  {
    id: 'sd-mq-008',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What does the "at-least-once" delivery guarantee mean?',
    options: [
      'The message is guaranteed to be delivered exactly once',
      'The message is delivered at least once, but duplicates are possible',
      'The message may be lost, but is never duplicated',
      'The message is delivered to all subscribers simultaneously',
    ],
    correctIndex: 1,
    explanation:
      'At-least-once is the guarantee that a message will be delivered to the consumer at least once. If the acknowledgement (ack) from the consumer is not received (due to a network or consumer failure), the broker will resend the message. This may lead to duplicates, so the consumer must be idempotent -- processing the same message multiple times must not change the result. Ways to ensure idempotency: a unique message ID + a table of processed IDs, using UPSERT instead of INSERT.',
  },
  {
    id: 'sd-mq-009',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is a partition in Apache Kafka?',
    options: [
      'A section for storing cluster metadata',
      'An ordered, immutable sequence of messages within a topic that enables parallel processing',
      'A mechanism for isolating topics from one another',
      'A backup copy of a topic on another broker',
    ],
    correctIndex: 1,
    explanation:
      'A partition is the main unit of parallelism in Kafka. Each topic is divided into one or more partitions. A partition is an ordered, append-only log of messages. Messages within a single partition are strictly ordered and have a unique offset. Different partitions can be processed by different consumers in parallel. The message key determines which partition it lands in (hash(key) mod num_partitions), which guarantees ordering for messages with the same key.',
  },
  {
    id: 'sd-mq-010',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is a consumer group in Apache Kafka?',
    options: [
      'A group of topics combined by business logic',
      'A set of consumers that jointly read a topic, where each partition is processed by only one consumer in the group',
      'A group of producers sending messages to a single topic',
      'A cluster of brokers serving a single group of clients',
    ],
    correctIndex: 1,
    explanation:
      'A consumer group is a logical group of consumers that jointly read a topic. Kafka guarantees that each partition is assigned to exactly one consumer in the group. This provides: 1) Parallel processing -- each consumer processes its own partitions. 2) Scaling -- adding consumers increases throughput (up to the number of partitions). 3) Fault tolerance -- if a consumer goes down, its partitions are automatically redistributed (rebalancing). Different consumer groups read the data independently -- each receives all messages (the pub/sub pattern).',
  },
  {
    id: 'sd-mq-011',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'middle',
    type: 'open',
    question: 'What types of exchange exist in RabbitMQ and how do they differ? Give practical examples of using each type.',
    sampleAnswer:
      'An exchange in RabbitMQ is a component that receives messages from the producer and routes them to queues according to certain rules. Exchange types: 1) Direct -- routing by an exact match of the routing key. A message with routing key "order.created" lands only in queues bound with the same key. Example: distributing tasks by type (email queue, sms queue). 2) Fanout -- the message is copied to all bound queues, the routing key is ignored. Example: broadcasting notifications to all subsystems (analytics, logging, monitoring). 3) Topic -- routing by a routing key pattern with wildcards (* -- one word, # -- zero or more words). The key "order.eu.created" matches "order.*.created" and "order.#". Example: regional order processing. 4) Headers -- routing by message headers rather than by the routing key. Example: filtering by data format (JSON vs XML) or priority.',
    explanation:
      'RabbitMQ\'s exchange model provides flexible routing that Kafka lacks (where routing is only by topic and partition). This is an advantage of RabbitMQ for complex routing scenarios. In practice, the Direct (for point-to-point) and Topic (for pub/sub with filtering) exchanges are used most often.',
  },
  {
    id: 'sd-mq-012',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is an offset in Apache Kafka?',
    options: [
      'The time delay between sending and receiving a message',
      'A unique sequential number of a message in a partition by which the consumer tracks its read position',
      'The distance between the producer and consumer on the network',
      'The number of messages skipped on error',
    ],
    correctIndex: 1,
    explanation:
      'An offset is a unique sequential number (0, 1, 2, ...) assigned to each message in a partition. The consumer tracks its current offset for each partition -- this is its "bookmark" in the log. Kafka does not delete messages after they are read (unlike traditional queues) -- they are retained until the retention period expires. This allows: re-reading messages (replay), multiple consumer groups reading independently, and recovering after failures. Offsets are committed (saved) by the consumer in Kafka (the __consumer_offsets topic) or in external storage.',
  },
  {
    id: 'sd-mq-013',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'middle',
    type: 'quiz',
    question: 'How do you ensure message processing order in a distributed system with a queue?',
    options: [
      'Order is guaranteed automatically in any configuration',
      'Use a single partition/queue, but this limits throughput',
      'Send messages with the same key so they land in the same partition -- order is guaranteed within a partition',
      'Order cannot be guaranteed in a distributed system',
    ],
    correctIndex: 2,
    explanation:
      'In Kafka, message order is guaranteed only within a single partition. To ensure ordering for related messages (for example, all events of a single order), you need to send them with the same key (order_id) -- then they land in the same partition. This lets you scale processing (many partitions) while preserving the order of logically related messages. Global ordering of all messages requires a single partition, which limits parallelism. In RabbitMQ, order is guaranteed within a single queue with a single consumer.',
  },
  {
    id: 'sd-mq-014',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'senior',
    type: 'open',
    question: 'What is backpressure in messaging systems and how do you implement it? What are the consequences of the absence of backpressure?',
    sampleAnswer:
      'Backpressure is a mechanism by which a consumer or broker signals the producer to slow down sending messages so as not to overload the system. Without backpressure, the producer can send messages faster than the consumer processes them, which leads to: queue overflow (OOM), growing processing latency, message loss, and cascading failures. Implementation: 1) Limiting queue size -- when full, the broker rejects new messages (RabbitMQ: x-max-length policy, Kafka: the producer gets an error when the buffer is full). 2) Rate limiting on the producer -- limiting the send rate (Kafka: max.in.flight.requests.per.connection, linger.ms). 3) Credit-based flow control -- RabbitMQ uses TCP credit flow to limit the producer rate. 4) Consumer-side throttling -- the consumer controls the read rate (Kafka pull model). 5) Prefetch limit -- limiting the number of unacknowledged messages (RabbitMQ: prefetch count). 6) Lag monitoring -- alerting on growing consumer lag (Kafka consumer lag).',
    explanation:
      'Backpressure is a critical mechanism for production systems. Without it, any difference in speed between producer and consumer will inevitably lead to problems. Kafka by design provides natural backpressure through the pull model: the consumer determines its own read rate. RabbitMQ uses a push model, so backpressure is implemented through prefetch and credit flow.',
  },
  {
    id: 'sd-mq-015',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'senior',
    type: 'quiz',
    question: 'Which serialization format provides the best performance and schema evolution support for messages in Kafka?',
    options: [
      'JSON -- a universal and human-readable format',
      'XML -- the standard for enterprise systems',
      'Apache Avro with Schema Registry -- a compact binary format with schema version management',
      'Plain Text -- the simplest format',
    ],
    correctIndex: 2,
    explanation:
      'Apache Avro combined with the Confluent Schema Registry is the recommended approach for Kafka. Avro provides: compact binary serialization (less traffic and storage than JSON), schema evolution (backward/forward compatibility -- you can add/remove fields without breaking consumers), and strong typing. The Schema Registry stores and versions schemas, checking compatibility when new versions are registered. Alternatives: Protocol Buffers (Google, popular in gRPC), Thrift (Facebook), MessagePack. JSON is simpler for debugging, but less efficient and does not support native schema evolution.',
  },
  {
    id: 'sd-mq-016',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'senior',
    type: 'open',
    question: 'How does Kafka provide exactly-once semantics through an idempotent producer and transactions? What limitations exist?',
    sampleAnswer:
      'Kafka implements exactly-once through two mechanisms: 1) Idempotent producer (enable.idempotence=true): Kafka assigns each producer a Producer ID (PID) and each message a Sequence Number. The broker tracks the last sequence number for each PID and partition, rejecting duplicates on resend. This provides exactly-once for one producer to one partition. 2) Transactions (Kafka Transactions): the producer can atomically write messages to several partitions/topics and commit the consumer offset -- either all writes are applied or none. A consumer with isolation.level=read_committed sees only committed messages. Limitations: exactly-once works only within the Kafka ecosystem (Kafka -> Kafka). When interacting with external systems (databases, HTTP APIs), additional idempotency is needed. Transactions increase latency and reduce throughput. The maximum number of partitions in a single transaction is limited.',
    explanation:
      'Exactly-once in Kafka is an engineering achievement, but it is important to understand its boundaries. It works for the "read-process-write" pattern within Kafka (Kafka Streams). For external systems (writing to PostgreSQL, sending HTTP), the Transactional Outbox Pattern or idempotent operations are needed. In practice, most systems use at-least-once + idempotency as a simpler and more reliable approach.',
  },
  {
    id: 'sd-mq-017',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is an acknowledgement (ack) in message processing?',
    options: [
      'Encrypting a message before sending',
      'Confirmation from the consumer to the broker that a message was processed successfully',
      'Checking message integrity in transit',
      'Writing a log entry about receiving a message',
    ],
    correctIndex: 1,
    explanation:
      'An acknowledgement (ack) is a signal from the consumer to the broker that a message has been successfully received and processed. After receiving the ack, the broker removes the message from the queue (in RabbitMQ) or commits the offset (in Kafka). If the ack is not received (the consumer crashed), the broker considers the message unprocessed and will redeliver it (to another consumer, or the same one after recovery). It is important to send the ack only after full processing, otherwise the message may be lost. Auto-ack (acknowledging immediately on receipt) is faster but risks data loss.',
  },
  {
    id: 'sd-mq-018',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'middle',
    type: 'open',
    question: 'What is the Transactional Outbox pattern and what problem does it solve when working with message queues?',
    sampleAnswer:
      'The Transactional Outbox solves the atomicity problem of the operation "update the DB + send a message to the queue". The problem: when updating an order, you need to save the changes to the DB and send an event to Kafka. If you save to the DB and then send to Kafka -- a crash between the steps means the data is updated but the event is lost. If you send to Kafka first -- the event is sent but the DB is not updated. The Transactional Outbox solution: 1) The service writes the business data AND the event to an outbox table in a single DB transaction. 2) A separate process (relay/poller) reads new records from the outbox and sends them to Kafka. 3) After a successful send, it marks the record as processed. An alternative is Change Data Capture (CDC): a tool like Debezium reads the database WAL and publishes changes to Kafka, including outbox table records. CDC is more reliable than polling and does not load the DB with extra queries.',
    explanation:
      'The Transactional Outbox is one of the most important patterns in event-driven architectures. Without it, consistency between the database and the message queue cannot be guaranteed. Debezium + Kafka Connect is a popular implementation of the CDC approach. The pattern is especially important when using Database per Service, where distributed transactions cannot be used.',
  },
  {
    id: 'sd-mq-019',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'senior',
    type: 'quiz',
    question: 'How does Kafka ensure high data availability when a broker fails?',
    options: [
      'Data is stored only in memory and does not survive a failure',
      'Through partition replication -- each partition has a leader and several follower replicas on different brokers',
      'Through automatic backup to cloud storage',
      'Through mirroring the entire cluster to a backup data center',
    ],
    correctIndex: 1,
    explanation:
      'Kafka replicates data through the In-Sync Replicas (ISR) mechanism. Each partition has one leader (serving reads and writes) and several follower replicas on other brokers. Followers replicate data from the leader synchronously or asynchronously. The ISR is the set of replicas that have "caught up" with the leader. With the acks=all setting, the producer receives an acknowledgement only after a write to all ISR replicas. When the leader fails, the cluster controller automatically elects a new leader from the ISR. The min.insync.replicas and acks parameters define the balance between durability and performance.',
  },
  {
    id: 'sd-mq-020',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'senior',
    type: 'open',
    question: 'Describe an event-processing architecture using Event Sourcing and Kafka. What advantages and challenges does this approach create?',
    sampleAnswer:
      'Event Sourcing is a pattern in which the system state is determined by a sequence of events (an event log) rather than by a current snapshot. Kafka is ideal as an event store thanks to: its append-only log, long-term retention, replay capability, and ordering within a partition. The architecture: 1) A Command is validated and creates an Event. 2) The event is written to a Kafka topic (the event store). 3) Projections (read models) are built from the event stream for different views: one projection in PostgreSQL for queries, another in Elasticsearch for search, a third in Redis for caching. 4) On failure, a projection is rebuilt from Kafka (replay). Advantages: a complete history of changes (audit log), the ability to restore any past state, natural integration with CQRS, temporal queries. Challenges: eventual consistency, complexity of error handling (compensating events instead of DELETE/UPDATE), growth in data volume (compaction helps partially), complexity of event schema migration, a steep learning curve.',
    explanation:
      'Event Sourcing + Kafka is a powerful combination, but it does not suit every system. It is justified for: financial systems (auditing all transactions), systems with concurrent updates (CRDTs, collaborative editing), and analytics platforms. For simple CRUD applications, Event Sourcing is overkill. It is important to remember: Kafka is not a full-fledged event store (no support for optimistic concurrency); specialized solutions like EventStoreDB exist for that.',
  },
  {
    id: 'sd-mq-021',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is a broker in a messaging system?',
    options: [
      'A client that sends messages',
      'A server that receives, stores, and delivers messages',
      'A library for working with queues',
      'A message serialization format',
    ],
    correctIndex: 1,
    explanation:
      'A message broker is an intermediary server that receives messages from producers, stores them, and delivers them to consumers. The broker provides: decoupling (the sender does not know about the receivers), durability (messages are not lost), and routing (directing messages by rules). Examples of brokers: RabbitMQ, Apache Kafka, Apache ActiveMQ, Amazon SQS.',
  },
  {
    id: 'sd-mq-022',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is a topic in Apache Kafka?',
    options: [
      'A subject for discussion among developers',
      'A category or channel to which messages are published',
      'A Kafka configuration file',
      'A performance metric',
    ],
    correctIndex: 1,
    explanation:
      'A topic in Kafka is a named channel (category) for publishing messages. Producers send messages to a topic, consumers read from a topic. A topic is divided into partitions for parallelism. Examples of topics: orders, user-events, logs. Topics can have different retention and replication factor settings.',
  },
  {
    id: 'sd-mq-023',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What advantage does using queues for email delivery provide over synchronous sending?',
    options: [
      'The email is sent faster',
      'The user does not wait for sending, and the system is resilient to email-service failures',
      'No email server is needed',
      'Email delivery is guaranteed',
    ],
    correctIndex: 1,
    explanation:
      'With asynchronous sending through a queue: 1) The user gets a response immediately, without waiting for the email to be sent. 2) During temporary unavailability of the email service, messages are buffered and sent later. 3) You can control the rate (so as not to overload the email provider). 4) Retry on errors without affecting the main flow. This is a typical pattern of using queues.',
  },
  {
    id: 'sd-mq-024',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'junior',
    type: 'open',
    question: 'Explain the difference between synchronous and asynchronous communication between services. When should each approach be used?',
    sampleAnswer:
      'Synchronous communication (HTTP/gRPC): the calling service waits for a response. Advantages: simplicity, an immediate result, easier debugging. Drawbacks: dependence on the availability of the receiver, cascading failures. Use it: when you need an immediate response (checking a balance, authorization), for query operations. Asynchronous communication (Message Queues): the sender does not wait for processing. Advantages: decoupling, fault tolerance, scalability, smoothing of load spikes. Drawbacks: harder debugging, eventual consistency, additional infrastructure. Use it: for command operations (creating an order), when the result is not needed immediately, for integration between domains, for event-driven architecture.',
    explanation:
      'Most systems combine both approaches. A typical pattern: the API Gateway synchronously processes the request, saves to the DB, and publishes an event to a queue. Background tasks (notifications, analytics) are processed asynchronously.',
  },
  {
    id: 'sd-mq-025',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is message TTL (Time To Live) in message queues?',
    options: [
      'The lifetime of the connection to the broker',
      'The time after which an unused message is deleted or moved to a DLQ',
      'The maximum message processing time',
      'The interval between sending messages',
    ],
    correctIndex: 1,
    explanation:
      'Message TTL is the maximum lifetime of a message in a queue. If a message is not processed within this time, it is deleted or moved to a Dead Letter Queue. Why: to prevent the accumulation of stale messages, to clear the queue of "stuck" messages, and for business logic (a message is relevant only for N minutes). In RabbitMQ: x-message-ttl. In Kafka: retention.ms at the topic level.',
  },
  {
    id: 'sd-mq-026',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is prefetch in RabbitMQ and why is it needed?',
    options: [
      'Preloading the queue configuration',
      'The number of messages a consumer can receive without acknowledgement',
      'Caching messages on the client side',
      'Forecasting the number of messages',
    ],
    correctIndex: 1,
    explanation:
      'Prefetch (QoS) is a limit on the number of unacknowledged messages the broker will send to a consumer. Why: 1) Even load distribution -- without prefetch, one fast consumer could grab all the messages. 2) Backpressure -- a slow consumer will not receive more messages than it can process. Settings: prefetch=1 -- fair distribution, but lower throughput. prefetch=10-50 -- a balance. In RabbitMQ: channel.basicQos(prefetchCount).',
  },
  {
    id: 'sd-mq-027',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'middle',
    type: 'open',
    question: 'What is stream processing and how does it differ from batch processing? What tools are used?',
    sampleAnswer:
      'Batch processing is processing data in large chunks periodically (hourly, daily). Example: a nightly ETL, report generation. Tools: Hadoop MapReduce, Spark (batch mode). Characteristics: high throughput, high latency (hours). Stream processing is processing data in real time as it arrives. Example: fraud detection, real-time analytics, alerting. Tools: Apache Kafka Streams, Apache Flink, Apache Spark Streaming, AWS Kinesis. Characteristics: low latency (milliseconds to seconds), continuous processing. Differences: batch -- a bounded dataset, stream -- unbounded. Batch optimizes throughput, stream optimizes latency. A modern trend is unified batch+stream (Lambda/Kappa architecture, Apache Beam).',
    explanation:
      'Stream processing is becoming the standard for real-time use cases. Kafka + Kafka Streams (or Flink) is a popular stack. It is important to understand the trade-offs: stream processing is more complex (ordering, exactly-once, state management) but provides real-time insights.',
  },
  {
    id: 'sd-mq-028',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is the retention period in Apache Kafka?',
    options: [
      'The time the connection to the broker is kept alive',
      'The period during which messages are stored in a topic, after which they are deleted',
      'The time to process a single message',
      'The interval between data replications',
    ],
    correctIndex: 1,
    explanation:
      'The retention period is the time messages are stored in a Kafka topic. Unlike traditional queues, Kafka does not delete messages after they are read -- they are retained until the retention period expires. This allows: re-reading messages (replay), multiple consumer groups reading independently, and recovering after failures. Settings: retention.ms (time), retention.bytes (size). There is also log compaction -- storing only the latest value for each key.',
  },
  {
    id: 'sd-mq-029',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'middle',
    type: 'quiz',
    question: 'Which messaging pattern allows the sender to receive a reply from the receiver?',
    options: [
      'Fire-and-forget',
      'Request-Reply (RPC over messaging)',
      'Publish-Subscribe',
      'Fan-out',
    ],
    correctIndex: 1,
    explanation:
      'Request-Reply is a pattern in which the sender waits for a reply to its message. Implementation: the message contains a reply-to (the address of the reply queue) and a correlation-id. The receiver processes the request and sends the reply to the specified queue with the same correlation-id. RabbitMQ Direct Reply-to simplifies this pattern. It is used for: RPC over queues, when you need a reply but with the advantages of asynchrony (retry, timeout).',
  },
  {
    id: 'sd-mq-030',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'middle',
    type: 'open',
    question: 'How do you ensure idempotency of message processing? Why is it important under at-least-once delivery?',
    sampleAnswer:
      'Idempotency is a property of an operation whereby executing it multiple times yields the same result as executing it once. Under at-least-once delivery, a message may be delivered multiple times (retry after a failure), so processing must be idempotent. Ways to ensure it: 1) Unique message ID + deduplication table -- store the IDs of processed messages and ignore duplicates. 2) Idempotent operations -- use UPSERT instead of INSERT, SET balance = 100 instead of balance += 10. 3) Version/timestamp checks -- update only if the version is newer. 4) Natural idempotency -- some operations are idempotent by nature (DELETE, SET). 5) Database constraints -- UNIQUE constraints prevent duplication. Redis with SETNX for deduplication, a Kafka producer with enable.idempotence=true.',
    explanation:
      'Idempotency is a key principle of reliable message processing. Without it, at-least-once turns into an at-least-once effect, which can lead to duplicate orders, double charges, and so on. Design handlers to be idempotent from the very start.',
  },
  {
    id: 'sd-mq-031',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is log compaction in Apache Kafka?',
    options: [
      'Compressing logs to save space',
      'Storing only the latest message for each key, deleting the previous ones',
      'Merging several partitions into one',
      'Archiving old logs',
    ],
    correctIndex: 1,
    explanation:
      'Log compaction is a retention mode in Kafka in which only the latest message is kept for each key. Previous values are removed during compaction (a background process). It is used for: changelog topics (current state), configurations, user profiles -- when the latest value matters rather than the history. Unlike time-based retention, a compacted topic can keep data indefinitely. A message with a null value (a tombstone) means deletion of the key.',
  },
  {
    id: 'sd-mq-032',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'senior',
    type: 'quiz',
    question: 'Which tool lets you implement Change Data Capture from a database into Kafka?',
    options: [
      'Kafka Connect',
      'Debezium',
      'Kafka Streams',
      'Schema Registry',
    ],
    correctIndex: 1,
    explanation:
      'Debezium is an open-source platform for CDC (Change Data Capture). It reads changes from a database WAL/binlog and publishes them to Kafka. It supports: PostgreSQL, MySQL, MongoDB, SQL Server, Oracle. Advantages over polling: low latency, no extra query load on the DB, accurate ordering of changes. It runs as a Kafka Connect connector. It is used for: data synchronization, event-driven architecture, cache invalidation, microservice integration.',
  },
  {
    id: 'sd-mq-033',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'senior',
    type: 'open',
    question: 'Explain the Kafka Connect architecture and when to use it instead of custom producers/consumers.',
    sampleAnswer:
      'Kafka Connect is a framework for integrating Kafka with external systems. Architecture: Workers -- processes that run connectors. Connectors -- plugins for specific systems (JDBC, S3, Elasticsearch). Tasks -- units of parallelism within a connector. Source connectors -- read from an external system into Kafka. Sink connectors -- write from Kafka into an external system. Advantages: ready-made connectors (hundreds available), automatic offset management, scaling (distributed mode), monitoring via a REST API, schema evolution with Schema Registry. When to use it: integration with popular systems (databases, S3, Elasticsearch), ETL/ELT pipelines, CDC with Debezium. When to use a custom producer/consumer: unique business logic, complex transformation, non-standard sources.',
    explanation:
      'Kafka Connect is a production-ready solution for typical integrations. Writing a custom consumer to write to PostgreSQL is reinventing the wheel. Connectors are tested, support exactly-once (some), and handle edge cases. Confluent Hub is a repository of connectors.',
  },
  {
    id: 'sd-mq-034',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'senior',
    type: 'open',
    question: 'How do you implement exactly-once processing when writing from Kafka to a database? What patterns are used?',
    sampleAnswer:
      'Exactly-once when writing to an external system requires coordinating commits: 1) Transactional Outbox + CDC -- write the result and the offset in a single DB transaction, CDC publishes the result. Guarantees exactly-once but adds latency. 2) Idempotent writes + at-least-once -- store message_id in the DB, skip on a duplicate. Simple, works for most cases. 3) Two-Phase Commit (2PC) -- XA transactions between Kafka and the DB. Slow, complex, rarely used. 4) Kafka Transactions + DB in one system -- Kafka Connect JDBC Sink with exactly-once.source=true (requires Kafka transactions). 5) State store (Kafka Streams) -- keep state in Kafka, not in an external DB. Kafka Streams provides exactly-once internally. In practice: idempotent processing is the simplest and most reliable approach. "Exactly-once" when interacting with external systems is often an illusion.',
    explanation:
      'Exactly-once between Kafka and an external system is an unsolved problem in the general case (the Two Generals Problem). In practice, people use: idempotency (at-least-once + deduplication), transactional outbox, or accept at-least-once with idempotent processing.',
  },
  {
    id: 'sd-mq-035',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is a message payload?',
    options: [
      'The address of the message recipient',
      'The payload -- the data carried in the message',
      'The message headers',
      'The message identifier',
    ],
    correctIndex: 1,
    explanation:
      'A message payload is the main data carried in a message. In addition to the payload, a message usually contains: headers (metadata), a key (for partitioning in Kafka), a timestamp, a message ID. The payload can be in any format: JSON, Avro, Protobuf, plain text. The payload size affects performance -- Kafka is efficient for messages up to ~1MB.',
  },
  {
    id: 'sd-mq-036',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'junior',
    type: 'quiz',
    question: 'Which AWS service provides a managed message queue?',
    options: [
      'AWS Lambda',
      'Amazon SQS (Simple Queue Service)',
      'Amazon RDS',
      'AWS CloudWatch',
    ],
    correctIndex: 1,
    explanation:
      'Amazon SQS is a fully managed message queue service. Two queue types: Standard (at-least-once, best-effort ordering, high throughput) and FIFO (exactly-once, strict ordering, up to 300 msg/sec without batching). Advantages: serverless (no servers to manage), automatic scaling, integration with Lambda, SNS. An alternative for pub/sub: Amazon SNS.',
  },
  {
    id: 'sd-mq-037',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is message batching and why is it used?',
    options: [
      'Grouping messages by recipient',
      'Sending several messages in one request to increase throughput',
      'Backing up messages',
      'Filtering messages by criteria',
    ],
    correctIndex: 1,
    explanation:
      'Message batching is grouping several messages into one request to reduce the overhead of network calls. In Kafka: the producer batch.size and linger.ms control batching on the producer side. A consumer can read a batch of messages (max.poll.records). Advantages: higher throughput, better compression, fewer network round-trips. Trade-off: higher latency (waiting for the batch to fill). Batching is critical for high-throughput scenarios.',
  },
  {
    id: 'sd-mq-038',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'middle',
    type: 'open',
    question: 'Describe the Competing Consumers pattern. How does it help scale message processing?',
    sampleAnswer:
      'Competing Consumers is a pattern in which several consumers read from a single queue, "competing" for messages. Each message is processed by only one consumer. How it works: a message is handed to one of the available consumers, and after processing and ack the message is removed. Scaling: adding consumers increases throughput linearly (up to the limits of the queue/partitions). Implementations: RabbitMQ -- several consumers on a single queue. Kafka -- a consumer group, where each partition is assigned to one consumer. Limitations: order is not guaranteed between consumers (in Kafka it is guaranteed within a partition). On a consumer failure -- redelivery (duplicates are possible). Used for: task processing (task queue), load distribution.',
    explanation:
      'Competing Consumers is the foundation of scalable message processing. In Kubernetes a typical implementation: a Deployment with N pod replicas, each a consumer. An HPA can automatically scale by queue depth or consumer lag.',
  },
  {
    id: 'sd-mq-039',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'middle',
    type: 'quiz',
    question: 'Which Kafka Streams mechanism lets you store processing state locally?',
    options: [
      'Kafka Topics',
      'State Stores (backed by RocksDB)',
      'Consumer Groups',
      'Schema Registry',
    ],
    correctIndex: 1,
    explanation:
      'State Stores in Kafka Streams are local state stores for stateful operations (aggregations, joins, windowing). By default they use RocksDB (an embedded key-value store). State is automatically replicated to a Kafka changelog topic for recovery after a failure. Advantages: fast access (local disk), fault tolerance (changelog), exactly-once (with Kafka transactions). Operations: count(), aggregate(), join() use state stores.',
  },
  {
    id: 'sd-mq-040',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is consumer lag in Kafka and why is it important to monitor?',
    options: [
      'The network latency between consumer and broker',
      'The difference between the last message in a partition and the consumer\'s read position',
      'The time to process a single message',
      'The number of consumer errors',
    ],
    correctIndex: 1,
    explanation:
      'Consumer lag is the number of unread messages: the difference between the log-end-offset (the last message) and the committed-offset (the consumer\'s position). Why it matters: growing lag means the consumer is not keeping up with the producer -- the system is degrading. Causes: slow processing, too few consumers, problems with dependencies. Monitoring: Kafka JMX metrics, Burrow, Prometheus kafka_consumergroup_lag. An alert on lag above a threshold is critical for production.',
  },
  {
    id: 'sd-mq-041',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'senior',
    type: 'open',
    question: 'Compare Apache Kafka and Amazon Kinesis. When would you choose each?',
    sampleAnswer:
      'Apache Kafka: self-hosted or Confluent Cloud. Advantages: full control, a rich ecosystem (Kafka Streams, Connect, ksqlDB), unlimited retention, log compaction, exactly-once semantics, open source. Drawbacks: operational complexity (ZooKeeper/KRaft), requires DevOps expertise. Amazon Kinesis: a fully managed AWS service. Advantages: serverless, integration with AWS (Lambda, Firehose, Analytics), scales on demand, no operations. Drawbacks: vendor lock-in, retention up to 7 days (365 with extended), more expensive at high volumes, fewer features than Kafka. Choice: Kinesis -- an AWS-native application, you want managed, moderate volume. Kafka -- high throughput, you need advanced features (compaction, exactly-once), multi-cloud, you have expertise. Alternative: Amazon MSK -- managed Kafka on AWS.',
    explanation:
      'Kinesis is simpler to start with in the AWS ecosystem, Kafka is more powerful and flexible. Many start with Kinesis and migrate to Kafka (or MSK) as requirements grow. MSK is a compromise: Kafka features with managed operations.',
  },
  {
    id: 'sd-mq-042',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is event replay and why is it an important Kafka capability?',
    options: [
      'Resending failed messages',
      'The ability to re-read historical messages by resetting the consumer offset',
      'Replaying events in a test environment',
      'Replicating events between clusters',
    ],
    correctIndex: 1,
    explanation:
      'Event replay is the ability to re-read messages from any position (offset). In Kafka, messages are not deleted after being read (unlike traditional queues) and are retained until the retention period expires. Why: recovering after an error in a consumer (re-read and reprocess), building new projections/views from events, debugging, testing. How: reset the consumer group offset to the desired position (kafka-consumer-groups --reset-offsets). Event replay is the foundation of the Event Sourcing architecture.',
  },
  {
    id: 'sd-mq-043',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What happens to a message in the queue if the consumer sends a negative acknowledgement (nack)?',
    options: [
      'The message is deleted from the queue',
      'The message is returned to the queue for reprocessing or sent to a DLQ',
      'The message is sent back to the producer',
      'Nothing, the message stays with the consumer',
    ],
    correctIndex: 1,
    explanation:
      'On a negative acknowledgement (nack/reject) in RabbitMQ the message is: returned to the queue for redelivery (requeue=true) or discarded/moved to a DLQ (requeue=false with a configured dead-letter exchange). This lets you handle transient errors (retry) and isolate persistent ones (a DLQ for analysis). In Kafka there is no explicit nack -- the consumer controls the offset and does not commit the offset of a problematic message.',
  },
  {
    id: 'sd-mq-044',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'middle',
    type: 'open',
    question: 'How do you organize priority message queues? What approaches exist?',
    sampleAnswer:
      'Priority queues mean processing important messages before less important ones. Approaches: 1) RabbitMQ Priority Queue -- native support for priorities (x-max-priority). High-priority messages are processed first. Limitation: extra memory consumption. 2) Separate queues -- high-priority, medium, low. Consumers process high first, then medium. Implementation: multiple consumers with different distribution. 3) Kafka: no native priorities. Solution: separate topics by priority, the consumer reads the high-priority topic first. 4) Weighted consumers -- more consumers for the high-priority queue. 5) Consumer-side prioritization -- read a batch, sort by priority, process. Trade-off: priorities complicate the system, and starvation of low-priority messages is possible.',
    explanation:
      'Priority queues are needed for: critical business operations (payments are more important than analytics), SLA-based processing (premium users), graceful degradation (processing only what is important under overload). The simplest solution is separate, explicitly managed queues.',
  },
  {
    id: 'sd-mq-045',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is Apache Pulsar and how does it differ from Kafka?',
    options: [
      'Pulsar is a fork of Kafka under a different name',
      'Pulsar is distributed messaging with separation of compute and storage, native multi-tenancy, and geo-replication',
      'Pulsar is a monitoring tool for Kafka',
      'Pulsar is a message serialization protocol',
    ],
    correctIndex: 1,
    explanation:
      'Apache Pulsar is a distributed messaging and streaming platform (Yahoo, now Apache). Key differences from Kafka: 1) Separation of compute and storage -- brokers are stateless, data lives in Apache BookKeeper. This simplifies scaling. 2) Native multi-tenancy -- isolation at the platform level. 3) Geo-replication -- built-in replication between data centers. 4) Unified messaging -- queuing and streaming in one system. 5) Tiered storage -- automatic offload to S3. Trade-off: more complex operationally (more components), a smaller ecosystem than Kafka.',
  },
  {
    id: 'sd-mq-046',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'senior',
    type: 'open',
    question: 'How do you implement distributed tracing for messages passing through several services and queues?',
    sampleAnswer:
      'Distributed tracing for async messaging: 1) Propagate trace context -- add trace-id and span-id to message headers. OpenTelemetry standardizes the format (the traceparent header). 2) Producer span -- create a span when sending a message, record the topic and key in attributes. 3) Consumer span -- on receipt, extract the context from the headers and create a child span. 4) Instrumentation -- Kafka/RabbitMQ libraries with OpenTelemetry support (automatic propagation). 5) Visualization -- Jaeger, Zipkin, Datadog show the trace across sync and async calls. Specifics: an async span may last a long time (time in the queue), batching complicates it (many messages = many spans), sampling is important for high volumes. Tools: OpenTelemetry SDK, Jaeger, Zipkin, commercial APMs.',
    explanation:
      'Tracing through queues is critical for understanding end-to-end latency and for debugging. Without context propagation, the link between producer and consumer is lost. OpenTelemetry is the de facto standard, and most messaging libraries have an integration.',
  },
  {
    id: 'sd-mq-047',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is Kafka KRaft mode and why is it important?',
    options: [
      'A message compression mode in Kafka',
      'A new consensus mode without ZooKeeper that simplifies Kafka\'s architecture',
      'An encryption protocol in Kafka',
      'A high-performance mode',
    ],
    correctIndex: 1,
    explanation:
      'KRaft (Kafka Raft) is a new metadata management mode in Kafka without ZooKeeper. Before KRaft, Kafka required a separate ZooKeeper cluster to store metadata (topics, partitions, configuration). KRaft uses a built-in Raft consensus among Kafka controllers. Advantages: simpler operations (no ZooKeeper), faster partition recovery, better scaling (millions of partitions). KRaft became production-ready in Kafka 3.3+, and ZooKeeper is deprecated.',
  },
  {
    id: 'sd-mq-048',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'middle',
    type: 'quiz',
    question: 'Which pattern provides message processing within a time window?',
    options: [
      'Partitioning',
      'Windowing (tumbling, sliding, session windows)',
      'Sharding',
      'Batching',
    ],
    correctIndex: 1,
    explanation:
      'Windowing is a stream-processing pattern for grouping events into time windows. Types: Tumbling window -- fixed non-overlapping windows (every 5 minutes). Sliding window -- overlapping windows (5 minutes every minute). Session window -- windows based on activity (closes after an inactivity gap). Hopping window -- like sliding, but with a fixed step. It is used for: aggregations (count per minute), joining streams, detecting patterns. Implementations: Kafka Streams, Flink, Spark Streaming.',
  },
  {
    id: 'sd-mq-049',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'junior',
    type: 'open',
    question: 'Which metrics are important to monitor for a message queue system?',
    sampleAnswer:
      'Key metrics of a messaging system: 1) Queue depth / Consumer lag -- the number of unprocessed messages. Growth = a problem with consumers. 2) Throughput -- messages/sec for producers and consumers. 3) Latency -- the time from sending to processing (end-to-end), time in the queue. 4) Error rate -- failed deliveries, consumer errors, DLQ messages. 5) Consumer group status -- the number of active consumers, partition assignment. 6) Replication lag (Kafka) -- how far replicas lag behind the leader. 7) Disk usage -- for persistent queues. 8) Connection count -- to the broker. 9) Message age -- the age of the oldest message in the queue. Alerts: lag above a threshold, error rate > X%, consumer down, replication lag high. Tools: Prometheus + Grafana, built-in metrics (Kafka JMX, RabbitMQ Management), Datadog, New Relic.',
    explanation:
      'Consumer lag is the main metric for Kafka. Queue depth is for traditional queues. Growth in these metrics is the first sign of problems. It is important to set up alerts before the system degrades critically.',
  },
  {
    id: 'sd-mq-050',
    block: 'sd',
    topic: 'message-queues',
    topicLabel: 'Message Queues',
    difficulty: 'senior',
    type: 'open',
    question: 'Describe the Saga pattern for distributed transactions using message queues. How do you handle failures?',
    sampleAnswer:
      'Saga is a pattern for distributed transactions without 2PC. A business transaction is broken into a sequence of local transactions, each with a compensating action. Implementation with queues (Choreography): 1) Each service, after completing a step, publishes an event (OrderCreated). 2) The next service is subscribed to the event and performs its step (ReserveInventory). 3) On error, a compensating event is published (InventoryReservationFailed). 4) Previous services listen for failure events and perform a rollback (CancelOrder). Orchestration: a central coordinator manages the sequence, invoking services through commands in queues. Handling failures: compensating transactions must be idempotent, retry with backoff, a timeout for steps, a saga state machine to track progress. Tools: Temporal, Camunda, AWS Step Functions, a custom implementation with Kafka.',
    explanation:
      'A Saga over queues is more reliable than synchronous calls, but more complex. Choreography is simpler for small sagas, orchestration for complex ones with branching. The key: design compensating actions from the very start, and test failure scenarios.',
  },
];
