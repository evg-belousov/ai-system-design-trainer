import type { Question } from '@/data/types';

export const sdDatabasesQuestions: Question[] = [
  {
    id: 'sd-db-001',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is the key difference between SQL and NoSQL databases?',
    options: [
      'SQL databases are always faster than NoSQL',
      'SQL databases use a fixed schema and the SQL language, while NoSQL offers flexible schemas and different data models',
      'NoSQL databases do not support replication',
      'SQL databases are not suitable for horizontal scaling',
    ],
    correctIndex: 1,
    explanation:
      'SQL (relational) databases use fixed schemas with tables, rows, and columns and the SQL language for queries. They provide ACID transactions and strong consistency. NoSQL databases offer flexible data models (document, columnar, key-value, graph), do not require a fixed schema, and usually scale horizontally better. The choice depends on the data structure, consistency requirements, and scalability needs.',
  },
  {
    id: 'sd-db-002',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is Master-Slave (Primary-Replica) replication and what problem does it solve?',
    options: [
      'Automatic backup of data to tape drives',
      'The primary replica accepts writes while secondaries serve reads, increasing throughput and fault tolerance',
      'Splitting data horizontally across multiple servers',
      'Encryption of data in transit between servers',
    ],
    correctIndex: 1,
    explanation:
      'In the Master-Slave (Primary-Replica) scheme, one server (master/primary) accepts all write operations and replicates changes to one or more slave/replica servers. Replica servers serve read queries, which allows distributing the read load (read scaling). If the master fails, one of the slaves can be promoted to a new master (failover). It is important to account for replication lag, which can lead to reading stale data.',
  },
  {
    id: 'sd-db-003',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'middle',
    type: 'open',
    question: 'When should you choose a document database (e.g., MongoDB) over a relational one (e.g., PostgreSQL)? Provide concrete examples.',
    sampleAnswer:
      'A document database is preferable in the following cases: 1) The data has a hierarchical or nested structure that maps poorly onto the relational model (a product catalog with different attributes for different categories, user profiles with a variable set of fields). 2) The data schema changes frequently in the early stages of development -- a schema-less approach speeds up iterations. 3) You need horizontal scalability out of the box -- MongoDB supports sharding as a built-in feature. 4) The access pattern is reading/writing whole documents (no complex JOINs needed). 5) Content systems (CMS), configuration management systems, IoT platforms. A relational database is a better fit when the following matter: complex relationships between data, ACID transactions, complex analytical queries, a strict data schema.',
    explanation:
      'The choice between SQL and NoSQL is not a question of "which is better" but of "which is the right fit." In practice, many systems use polyglot persistence: a relational database for transactional data, a document database for catalogs, Redis for caching, Elasticsearch for full-text search. It is important to choose a DBMS based on access patterns, consistency requirements, and scale.',
  },
  {
    id: 'sd-db-004',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'senior',
    type: 'open',
    question: 'Explain the ACID principle in the context of distributed databases. Why are full ACID transactions hard to implement in distributed systems?',
    sampleAnswer:
      'ACID is a set of transaction properties: Atomicity (a transaction executes fully or not at all), Consistency (data moves from one valid state to another), Isolation (concurrent transactions do not affect each other), Durability (the results of a committed transaction are preserved). In distributed systems, ACID is hard to implement because: 1) Atomicity requires coordination between nodes (2PC -- the two-phase commit protocol, which is slow and blocking). 2) Consistency under network partitions cannot coexist with availability (the CAP theorem). 3) Isolation at the Serializable level in a distributed system significantly reduces performance. 4) Durability requires write acknowledgment from several nodes (a quorum). For this reason, many NoSQL systems offer weakened guarantees (BASE: Basically Available, Soft state, Eventually consistent).',
    explanation:
      'The trade-off between ACID and performance in distributed systems is one of the central topics of system design. Modern databases offer various levels of guarantees: Google Spanner provides global ACID transactions via the TrueTime API, CockroachDB uses Serializable isolation, and Cassandra offers a tunable consistency level. It is important to understand which guarantees your application actually needs.',
  },
  {
    id: 'sd-db-005',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'junior',
    type: 'quiz',
    question: 'Which type of NoSQL database is best suited for storing a social graph (friends, subscriptions, recommendations)?',
    options: [
      'Document (MongoDB)',
      'Columnar (Cassandra)',
      'Key-value (Redis)',
      'Graph (Neo4j)',
    ],
    correctIndex: 3,
    explanation:
      'Graph databases (Neo4j, Amazon Neptune, JanusGraph) are specifically designed for storing and processing relationships between entities. They represent data as nodes and edges, which allows efficient execution of relationship queries: "friends of friends," "shortest path between users," "recommendations based on connections." In a relational database, such queries would require multiple JOINs with exponentially growing complexity.',
  },
  {
    id: 'sd-db-006',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is a database index and why is it needed?',
    options: [
      'A backup copy of a table for fault tolerance',
      'A data structure that speeds up searching for rows in a table at the cost of extra memory and slower writes',
      'A tool for encrypting data in a table',
      'A mechanism for automatically deleting stale data',
    ],
    correctIndex: 1,
    explanation:
      'A database index is an additional data structure (usually a B-tree or B+tree) that speeds up searching for rows by certain columns. Without an index, the DB performs a full table scan (scanning all rows). With an index, the search is performed in O(log n). Trade-offs: indexes take up additional disk space, slow down INSERT/UPDATE/DELETE operations (the index must be updated), and require maintenance (rebuilding when fragmented). It is important to index columns that are frequently searched, but not to create more indexes than necessary.',
  },
  {
    id: 'sd-db-007',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'junior',
    type: 'quiz',
    question: 'Which type of NoSQL database stores data in a "key-value" format?',
    options: [
      'MongoDB',
      'Cassandra',
      'Redis',
      'Neo4j',
    ],
    correctIndex: 2,
    explanation:
      'Redis is the most popular key-value database. Key-value stores provide the simplest data model: each element is stored as a "key-value" pair. Operations are limited: GET by key, PUT key-value, DELETE by key. Advantages: extremely high performance (O(1) access), simplicity, good scalability. They are used for: caching, session storage, rate limiting, queues. Other examples: DynamoDB, Memcached, Riak.',
  },
  {
    id: 'sd-db-008',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is connection pooling in the context of databases?',
    options: [
      'Replication of data between several DB servers',
      'Reusing pre-created connections to the DB instead of creating a new one for each request',
      'Encryption of connections between the application and the DB',
      'Balancing requests across several databases',
    ],
    correctIndex: 1,
    explanation:
      'Connection pooling is a mechanism for reusing connections to a database. Creating a TCP connection and authenticating with the DB are expensive operations (tens of milliseconds). The pool maintains a set of ready connections: the application takes a connection from the pool, runs a query, and returns it. This significantly reduces latency and the load on the DB server. Key settings: minPoolSize (minimum number of connections), maxPoolSize (maximum), connectionTimeout (time to wait for a free connection). Tools: HikariCP (Java), PgBouncer (PostgreSQL), ProxySQL (MySQL).',
  },
  {
    id: 'sd-db-009',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is a column-family store and for which tasks is it optimal?',
    options: [
      'A relational DB optimized for JOIN operations',
      'A store that organizes data by columns instead of rows, optimal for analytical queries and time-series processing',
      'A database for storing binary files',
      'A graph DB with support for node families',
    ],
    correctIndex: 1,
    explanation:
      'Column-family stores (Cassandra, HBase, ScyllaDB) organize data by columns rather than by rows. The data of a single column is stored sequentially on disk, which allows efficient reading/aggregation of one column across millions of rows. They are optimal for: time series (IoT, metrics), analytical queries (sum, avg over a column), data with a large number of columns of which few are queried. Cassandra additionally provides linear scalability and high availability through a peer-to-peer architecture with no single point of failure.',
  },
  {
    id: 'sd-db-010',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is the "Database per Service" pattern in a microservices architecture?',
    options: [
      'All services use a single shared database for consistency',
      'Each microservice has its own database that other services cannot access directly',
      'A database is allocated per user',
      'Each table is stored in a separate database',
    ],
    correctIndex: 1,
    explanation:
      'Database per Service is a pattern in which each microservice owns its own database and is the only one that can read from and write to it. Other services obtain data only through the owner service\'s API. Advantages: independent deployment, freedom to choose a DBMS for each service (polyglot persistence), no coupling at the data level. Disadvantages: the complexity of ensuring consistency between services (Saga, eventual consistency are needed), the inability to perform JOINs across databases of different services, data duplication.',
  },
  {
    id: 'sd-db-011',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'middle',
    type: 'open',
    question: 'Explain the CQRS (Command Query Responsibility Segregation) pattern with separate stores for reads and writes. When is it justified?',
    sampleAnswer:
      'CQRS splits the system into two parts: the Command side (writes) and the Query side (reads) with different data models and, optionally, different stores. The Command side uses a normalized model (e.g., PostgreSQL) to ensure write consistency. The Query side uses a denormalized model optimized for specific read queries (Elasticsearch for search, Redis for fast access, materialized views). Synchronization between the sides happens through events: on a write, an event is generated that projects the data into the read model. CQRS is justified when: 1) The read load significantly exceeds writes (the sides can be scaled independently). 2) The data models for reading and writing differ greatly. 3) Complex data representations are needed for reading (aggregations, full-text search). 4) The system uses Event Sourcing. It is not justified for simple CRUD applications -- it adds complexity.',
    explanation:
      'CQRS is often used together with Event Sourcing, but this is not mandatory. The key trade-off is eventual consistency: the read model may lag behind the write model for the time it takes to process events (usually milliseconds to seconds). This needs to be accounted for in UX: for example, after creating an order, show confirmation from the write side without waiting for the read model to update.',
  },
  {
    id: 'sd-db-012',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What are NewSQL databases and what problem do they solve?',
    options: [
      'A new version of the SQL language with extended syntax',
      'NoSQL databases with support for SQL syntax',
      'Next-generation relational databases that combine ACID transactions with horizontal scalability',
      'Tools for migrating data between SQL and NoSQL',
    ],
    correctIndex: 2,
    explanation:
      'NewSQL is a class of relational DBMSs that aim to combine the ACID guarantees of traditional SQL systems with the horizontal scalability of NoSQL. They solve the dilemma of "SQL (ACID, but scales poorly) vs NoSQL (scales, but no ACID)." Examples: Google Spanner (globally distributed, uses TrueTime for synchronization), CockroachDB (an open-source analog of Spanner), TiDB (MySQL-compatible), YugabyteDB (PostgreSQL-compatible). NewSQL is suitable for applications that need both strict transactions and horizontal scaling.',
  },
  {
    id: 'sd-db-013',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'senior',
    type: 'open',
    question: 'Explain the CAP theorem and its practical significance when choosing a database for a distributed system. Provide examples of CP and AP systems.',
    sampleAnswer:
      'The CAP theorem states that in a distributed system it is impossible to simultaneously guarantee all three properties: Consistency (all nodes see the same data), Availability (every request receives a response), Partition Tolerance (the system works during network partitions). Since network partitions are inevitable in distributed systems, the real choice is between CP and AP. CP systems (consistency + partition tolerance): during a network partition they refuse to serve requests in order to preserve consistency. Examples: PostgreSQL (single-node), HBase, MongoDB (when configured with majority write concern), etcd, ZooKeeper. AP systems (availability + partition tolerance): during a partition they keep responding, but the data may be stale. Examples: Cassandra, DynamoDB, CouchDB, Riak. In practice this is a spectrum rather than a binary choice: many databases allow tuning the consistency level (Cassandra: ONE, QUORUM, ALL). Important: CAP describes behavior during network partitions; under normal operation most systems provide both C and A.',
    explanation:
      'The CAP theorem is an important conceptual framework, but in practice the PACELC model (an extension of CAP) is more useful: during a Partition -- a choice between A and C; Else (normal operation) -- a choice between Latency and Consistency. This explains why Cassandra (PA/EL) and DynamoDB (PA/EL) sacrifice consistency for speed even without partitions, while Spanner (PC/EC) provides strong consistency at the cost of higher latency.',
  },
  {
    id: 'sd-db-014',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is a Write-Ahead Log (WAL) and what role does it play in ensuring durability?',
    options: [
      'A log of read queries for cache optimization',
      'A journal where changes are written before being applied to the main data files, enabling recovery after a crash',
      'A slow-query log for performance optimization',
      'A mechanism for auditing the actions of database users',
    ],
    correctIndex: 1,
    explanation:
      'A Write-Ahead Log (WAL) is a mechanism in which all changes are first written to a sequential log on disk and only then applied to the data files. This ensures durability: in the event of a system crash, the DB can restore its state by replaying the WAL records. WAL also improves performance: sequential writing to the log is faster than random updates of data files. WAL is used in PostgreSQL, MySQL (InnoDB redo log), SQLite, Kafka (commit log), and many other systems. Replication is built on WAL: a replica receives and replays the master\'s WAL.',
  },
  {
    id: 'sd-db-015',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'senior',
    type: 'open',
    question: 'Compare approaches to data denormalization. When is denormalization justified and what problems does it create?',
    sampleAnswer:
      'Denormalization is the deliberate duplication or grouping of data to optimize reads at the cost of complicating writes. Approaches: 1) Adding computed/duplicated columns -- storing the author\'s name in the articles table instead of a JOIN with the authors table. 2) Materialized views -- precomputed and stored query results. 3) Embedding -- in document databases, storing related data inside a document (an address inside a user document). 4) Aggregate tables -- precomputed aggregations (number of likes, average rating). It is justified when: reads significantly outweigh writes, JOINs become a bottleneck, minimal read latency is needed, the data is rarely updated. Problems: update anomalies (data must be updated in several places), increased storage volume, the difficulty of maintaining consistency, more complex write code.',
    explanation:
      'Denormalization is one of the key scaling tools. Practically all high-load systems use denormalization to some degree. It is important to denormalize deliberately, based on real query patterns, and not prematurely. A good practice is to use CQRS, where the write model is normalized and the read model is denormalized.',
  },
  {
    id: 'sd-db-016',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is ORM (Object-Relational Mapping)?',
    options: [
      'A database performance monitoring tool',
      'A technology for automatically mapping objects in program code to rows of a relational DB',
      'A protocol for replicating data between servers',
      'A method for encrypting data in a database',
    ],
    correctIndex: 1,
    explanation:
      'ORM (Object-Relational Mapping) is a technology that allows working with relational DB data through objects in code, without writing SQL queries by hand. ORM automatically converts objects into SQL and back. Examples: Sequelize (Node.js), SQLAlchemy (Python), Hibernate (Java), Prisma (TypeScript). Advantages: faster development, protection against SQL injection, abstraction from the specific DBMS. Disadvantages: the N+1 problem, suboptimal queries, complexity with non-standard operations, hidden complexity.',
  },
  {
    id: 'sd-db-017',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'middle',
    type: 'open',
    question: 'What is a document store? Provide examples of typical data structures and query patterns for which they are optimal.',
    sampleAnswer:
      'A document store is a type of NoSQL database that stores data as semi-structured documents (usually JSON/BSON). Each document is a self-contained unit of data with an arbitrary structure. DBMS examples: MongoDB, CouchDB, Amazon DocumentDB, Firebase Firestore. They are optimal for: 1) Product catalogs -- products of different categories have different attributes (electronics vs clothing). 2) User profiles -- various sets of fields, nested lists (addresses, preferences). 3) Content management systems (CMS) -- articles with different structures, metadata, tags. 4) Configurations -- hierarchical application settings. 5) Logs and events -- semi-structured data with a variable set of fields. Typical patterns: reading/writing whole documents by ID, nested queries (find documents where nested.field = value), aggregation pipeline for analytics, text search over document content.',
    explanation:
      'Document databases are best suited when data naturally maps to a "document" -- a self-contained unit with a nested structure. An antipattern is using a document database for highly connected data that requires frequent JOIN-like operations between documents. In such cases a relational database will be more efficient.',
  },
  {
    id: 'sd-db-018',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'senior',
    type: 'quiz',
    question: 'Which transaction isolation level does PostgreSQL use by default?',
    options: [
      'Read Uncommitted',
      'Read Committed',
      'Repeatable Read',
      'Serializable',
    ],
    correctIndex: 1,
    explanation:
      'PostgreSQL uses the Read Committed isolation level by default. At this level, a transaction sees only data committed before the start of each statement within the transaction. This prevents dirty reads but allows non-repeatable reads and phantom reads. Stricter levels (Repeatable Read, Serializable) provide better isolation but reduce performance. It is important to choose the isolation level deliberately: financial operations may require Serializable, while for most web applications Read Committed is sufficient.',
  },
  {
    id: 'sd-db-019',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is the N+1 problem when working with a database?',
    options: [
      'A limit on the maximum number of connections to the DB',
      'A situation where fetching N related objects executes 1 main query and N additional ones instead of a single JOIN query',
      'An error when trying to create more than N+1 indexes on a table',
      'The inability to store more than N+1 rows in a single table',
    ],
    correctIndex: 1,
    explanation:
      'The N+1 problem is a classic performance issue, especially when using an ORM. Example: load 100 users (1 query), then for each load their orders (100 queries) = 101 queries instead of a single JOIN query. Solutions: 1) Eager loading (JOIN) -- load the data with a single JOIN query. 2) Batch loading -- load related data in a batch (WHERE user_id IN (1,2,3...)). 3) The DataLoader pattern -- automatic grouping of queries (used in GraphQL). The N+1 problem can dramatically reduce performance: 1000 objects = 1001 queries to the DB.',
  },
  {
    id: 'sd-db-020',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'senior',
    type: 'open',
    question: 'How do you implement the Saga pattern for managing distributed transactions between microservices with separate databases?',
    sampleAnswer:
      'Saga is a pattern for managing distributed transactions in which a business transaction is broken down into a sequence of local transactions in different services. Each step has a compensating operation (rollback). Two implementation approaches: 1) Choreography -- each service, after completing its local transaction, publishes an event that triggers the next step. On error, a cancellation event is published that triggers the compensating operations. Pros: simplicity, no central coordinator. Cons: it is hard to track state, cyclic dependencies. 2) Orchestration -- a central orchestrator (Saga Execution Coordinator) manages the sequence of steps, calling each service and handling errors. Pros: explicit logic, easy to track state. Cons: the orchestrator is a potential single point of failure. Example: creating an order = (1) Reserve the item -> (2) Charge the funds -> (3) Create the delivery. On error at step 3: compensation = (2\') Refund the funds -> (1\') Cancel the reservation. Tools: Temporal, Camunda, AWS Step Functions.',
    explanation:
      'Saga is a trade-off: instead of an ACID transaction we get ACD (without Isolation). Between steps, the system is in an intermediate state, which must be accounted for in business logic. Compensating operations must be idempotent. In practice, the Saga pattern significantly complicates the system, so it should be applied only when database-per-service is truly necessary.',
  },
  {
    id: 'sd-db-021',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is a Primary Key in a relational database?',
    options: [
      'An encryption key for protecting the table\'s data',
      'A unique identifier for each row in a table',
      'The first column in a table',
      'A key for joining two tables',
    ],
    correctIndex: 1,
    explanation:
      'A Primary Key is a column (or combination of columns) that uniquely identifies each row in a table. Properties: uniqueness (no duplicates), not null (a value is required), one per table. It can be natural (email, SSN) or surrogate (auto-increment ID, UUID). A Primary Key is automatically indexed.',
  },
  {
    id: 'sd-db-022',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is a Foreign Key?',
    options: [
      'A key for accessing the database from outside',
      'A reference to the primary key of another table that establishes a relationship between tables',
      'A backup primary key',
      'A key for encrypting external connections',
    ],
    correctIndex: 1,
    explanation:
      'A Foreign Key is a column that references the Primary Key of another table. It ensures referential integrity: you cannot create an order for a non-existent customer. The DBMS can automatically apply actions when a related record is deleted/updated: CASCADE, SET NULL, RESTRICT. The Foreign Key is the foundation of the relational data model.',
  },
  {
    id: 'sd-db-023',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'junior',
    type: 'quiz',
    question: 'Which type of database is best suited for storing time series data?',
    options: [
      'Relational (PostgreSQL)',
      'Document (MongoDB)',
      'Specialized time-series (InfluxDB, TimescaleDB)',
      'Graph (Neo4j)',
    ],
    correctIndex: 2,
    explanation:
      'Time-series databases (InfluxDB, TimescaleDB, Prometheus) are optimized for: append-only writes with a timestamp, aggregations over time intervals, automatic compression and retention of old data, downsampling (aggregation for long-term storage). They are used for: metrics, IoT data, financial quotes, logs. TimescaleDB is a PostgreSQL extension that is SQL-compatible.',
  },
  {
    id: 'sd-db-024',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'junior',
    type: 'open',
    question: 'Explain what database normalization is and why it is needed. Name the main normal forms.',
    sampleAnswer:
      'Normalization is the process of organizing data to reduce duplication and ensure integrity. The main normal forms: 1NF -- atomic values (no arrays in cells), unique rows. 2NF -- 1NF + all non-key attributes depend on the entire primary key (not on a part of it). 3NF -- 2NF + no transitive dependencies (a non-key attribute does not depend on another non-key attribute). BCNF -- a stricter version of 3NF. Why: it reduces duplication, prevents anomalies during UPDATE/DELETE, simplifies maintaining consistency. Trade-off: more JOINs, more complex queries. Denormalization is the reverse process for optimizing reads.',
    explanation:
      'In practice, most OLTP systems are normalized to 3NF. They are denormalized for read performance based on real query patterns. Data warehouses (OLAP) are often denormalized (star schema) for fast analytics.',
  },
  {
    id: 'sd-db-025',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is eventual consistency and when is it acceptable?',
    options: [
      'Data is always consistent across all nodes',
      'Data will become consistent after some time, if there are no new updates',
      'Data will never be consistent',
      'Consistency is guaranteed only for transactions',
    ],
    correctIndex: 1,
    explanation:
      'Eventual consistency is a consistency model in which the system guarantees that data will become consistent across all replicas after some time (usually milliseconds to seconds), if there are no new updates. It is acceptable when: a small synchronization delay is acceptable (social networks, likes), high availability is more important than instant consistency, the system is geographically distributed. It is not suitable for: financial transactions, reservation systems.',
  },
  {
    id: 'sd-db-026',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is a database migration and why is it needed?',
    options: [
      'Moving the database to another server',
      'Versioned changes to the database schema, applied automatically',
      'Migrating data between different DBMSs',
      'Backing up the database',
    ],
    correctIndex: 1,
    explanation:
      'A database migration is a versioned change to the DB schema (adding tables, columns, indexes) with the ability to roll back. Why: synchronizing the schema between developers and environments, tracking the change history, automating deployment, the ability to roll back. Tools: Flyway, Liquibase (Java), Alembic (Python), Knex (Node.js), Rails Migrations (Ruby). Important: migrations should be idempotent and backward compatible.',
  },
  {
    id: 'sd-db-027',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'middle',
    type: 'open',
    question: 'How do you choose between PostgreSQL and MongoDB for a new project? What criteria should be considered?',
    sampleAnswer:
      'Selection criteria: 1) Data structure: PostgreSQL -- a fixed schema, complex relationships between entities, JOINs. MongoDB -- a flexible schema, nested documents, frequent structure changes. 2) Consistency requirements: PostgreSQL -- ACID, strict transactions. MongoDB -- eventual consistency by default (ACID for a single document). 3) Query patterns: PostgreSQL -- complex analytical queries, aggregations. MongoDB -- CRUD over documents, flexible queries. 4) Scaling: PostgreSQL -- vertical + read replicas (sharding is harder). MongoDB -- built-in sharding. 5) Ecosystem: PostgreSQL -- mature, extensions (PostGIS, pg_vector). MongoDB -- good integration with the JS stack. Examples: PostgreSQL -- finance, ERP, CRM. MongoDB -- CMS, product catalogs, IoT.',
    explanation:
      'There is no universally "best" database. The choice depends on the specific requirements. In practice, many projects use both: PostgreSQL for transactional data, MongoDB for content and logs. It is important not to choose NoSQL just because it is "trendy."',
  },
  {
    id: 'sd-db-028',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is connection pool exhaustion and how do you prevent it?',
    options: [
      'The pool of connections fills up, after which new requests wait or receive an error',
      'A leak of connections from the pool',
      'An overflow of the connection buffer',
      'Running out of disk space in the DB',
    ],
    correctIndex: 0,
    explanation:
      'Connection pool exhaustion happens when all connections in the pool are busy and new requests cannot obtain a connection. Causes: slow queries, connection leaks (connections not returned to the pool), too small a pool, transactions holding connections for a long time. Prevention: set maxPoolSize appropriately for the load, timeouts for obtaining a connection, monitoring active connections, optimizing slow queries, using a connection timeout.',
  },
  {
    id: 'sd-db-029',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is a multi-model database?',
    options: [
      'A database with several servers',
      'A database that supports several data models (documents, graphs, key-value) in a single system',
      'A database with several schemas',
      'A distributed database',
    ],
    correctIndex: 1,
    explanation:
      'A multi-model database supports several data models in a single system. Examples: ArangoDB (documents, graphs, key-value), CosmosDB (documents, graphs, columns, key-value), PostgreSQL with extensions (relational + JSON + graph with pgRouting). Advantages: a single tool for different tasks, less operational complexity, unified transactions across models. Disadvantages: it may not be optimal for each model compared to a specialized DB.',
  },
  {
    id: 'sd-db-030',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'middle',
    type: 'open',
    question: 'What is database federation and when is it applied?',
    sampleAnswer:
      'Database federation is the splitting of a database by functionality (vertical partitioning): different groups of tables are stored in different databases/servers. Example: a users DB, a products DB, an orders DB -- each on its own server. The difference from sharding: sharding splits a single table horizontally, while federation splits tables vertically by domain. Advantages: independent scaling of domains, load isolation, different DBMSs for different tasks. It is applied: during the transition from a monolith to microservices (database per service), when domains have different scaling requirements, for isolating critical data. Difficulties: no JOINs across databases, distributed transactions.',
    explanation:
      'Federation is the first step in scaling a database before sharding. It is often applied during the decomposition of a monolith: each microservice gets its own database. It is important to correctly define the domain boundaries (bounded contexts from DDD).',
  },
  {
    id: 'sd-db-031',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is a distributed SQL (or NewSQL) database?',
    options: [
      'A SQL database in the cloud',
      'A distributed DBMS with a SQL interface, ACID transactions, and horizontal scaling',
      'A database with master-slave replication',
      'A SQL database without transactions',
    ],
    correctIndex: 1,
    explanation:
      'Distributed SQL (NewSQL) is a class of DBMSs that combine the SQL interface and ACID guarantees of relational databases with the horizontal scaling of NoSQL. Examples: Google Spanner, CockroachDB, YugabyteDB, TiDB. Characteristics: automatic sharding, distributed transactions, SQL compatibility, strong consistency. It is suitable when both transactions and scaling are needed (finance, retail). Trade-off: higher latency due to coordination between nodes.',
  },
  {
    id: 'sd-db-032',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'senior',
    type: 'quiz',
    question: 'Which mechanism does CockroachDB use to ensure serializable isolation in a distributed system?',
    options: [
      'Two-Phase Locking (2PL)',
      'Optimistic Concurrency Control with Hybrid Logical Clocks',
      'Pessimistic Locking',
      'Read Committed isolation',
    ],
    correctIndex: 1,
    explanation:
      'CockroachDB uses optimistic concurrency control (OCC) with Hybrid Logical Clocks (HLC) to ensure serializable isolation without global time synchronization. HLC combines physical time with logical counters to determine the order of events. Transactions execute optimistically; conflicts are detected at commit time. Google Spanner uses TrueTime (GPS + atomic clocks), which is unavailable outside Google, while CockroachDB is a software alternative.',
  },
  {
    id: 'sd-db-033',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'senior',
    type: 'open',
    question: 'How do you implement soft delete in a database, and what consequences does it have for performance and architecture?',
    sampleAnswer:
      'Soft delete means marking records as deleted instead of physically removing them (DELETE). Implementation: a deleted_at column (NULL = active, timestamp = deleted) or an is_deleted boolean. Consequences: 1) All SELECTs must filter WHERE deleted_at IS NULL -- forgetting = showing deleted data. 2) Indexes grow -- deleted records take up space. A partial index helps: CREATE INDEX ON users (id) WHERE deleted_at IS NULL. 3) Unique constraints become more complex -- an email must be unique among active records, but may be duplicated among deleted ones. 4) Foreign keys -- a deleted parent record with active children. 5) Auditing and recovery -- soft delete simplifies recovery. Architecturally: instead of soft delete, event sourcing or a separate archive table is often better. ORMs (Django, Rails) support soft delete via middleware.',
    explanation:
      'Soft delete seems simple but creates many edge cases. Alternatives: event sourcing (full history), audit log (a separate change table), an archive table (move instead of delete). The choice depends on requirements: compliance, recovery, auditing.',
  },
  {
    id: 'sd-db-034',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'senior',
    type: 'open',
    question: 'Explain how MVCC (Multi-Version Concurrency Control) works and why it is important for database performance.',
    sampleAnswer:
      'MVCC is a mechanism in which each transaction sees a snapshot of the data as of the moment it began, rather than the current state. On an UPDATE, a new version of the row is created, and the old one is preserved for active transactions. Readers do not block writers and vice versa. How it works in PostgreSQL: each row has xmin (the ID of the transaction that created it) and xmax (the ID of the transaction that deleted/updated it). A transaction sees a row if xmin is committed and less than its snapshot, and xmax is not committed or greater than the snapshot. Advantages of MVCC: high concurrency (readers do not block), consistent reads without locks, efficient long-running queries. Disadvantages: accumulation of old versions (bloat), the need for VACUUM in PostgreSQL, the overhead of storing versions. MVCC is used in: PostgreSQL, MySQL InnoDB, Oracle, CockroachDB.',
    explanation:
      'MVCC is the foundation of the high performance of modern DBMSs. Without MVCC, every read would block writes and vice versa. In PostgreSQL it is important to configure autovacuum to clean up old versions, otherwise tables become bloated (table bloat).',
  },
  {
    id: 'sd-db-035',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What are CRUD operations in the context of databases?',
    options: [
      'Data encryption operations',
      'Create, Read, Update, Delete -- the basic data operations',
      'Backup operations',
      'Replication operations',
    ],
    correctIndex: 1,
    explanation:
      'CRUD is an acronym for the four basic data operations: Create (INSERT), Read (SELECT), Update (UPDATE), Delete (DELETE). These operations form the foundation of most applications. REST APIs often map to CRUD: POST -> Create, GET -> Read, PUT/PATCH -> Update, DELETE -> Delete. Understanding CRUD is important for designing APIs and the DB schema.',
  },
  {
    id: 'sd-db-036',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is a composite index?',
    options: [
      'An index created from several tables',
      'An index that includes several columns',
      'A backup copy of an index',
      'An index with a compression function',
    ],
    correctIndex: 1,
    explanation:
      'A composite index is an index built on several columns. For example: CREATE INDEX ON orders (user_id, created_at). The order of the columns matters: the index is effective for queries that filter by the leading columns (user_id) or by all of them. A query only on created_at does not use this index effectively. Rule: arrange columns from high to low cardinality and by frequency of use in WHERE.',
  },
  {
    id: 'sd-db-037',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is a query execution plan and why analyze it?',
    options: [
      'A plan for backing up queries',
      'A step-by-step plan for executing a query, showing how the DBMS will retrieve the data',
      'A schedule for executing queries',
      'The database API documentation',
    ],
    correctIndex: 1,
    explanation:
      'A query execution plan is the plan that the DBMS optimizer builds to execute a query. It shows: which indexes are used (or a sequential scan), the JOIN order, the join methods (hash, nested loop, merge), the estimated cost and actual rows. Commands: EXPLAIN (the plan), EXPLAIN ANALYZE (the plan + actual execution). Why: to find inefficient queries (a sequential scan on a large table), to understand index usage, to optimize performance.',
  },
  {
    id: 'sd-db-038',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'middle',
    type: 'open',
    question: 'What factors should be considered when choosing between a UUID and an auto-increment ID as a primary key?',
    sampleAnswer:
      'Auto-increment ID: Advantages: compact (4-8 bytes vs 16 bytes for a UUID), efficient B-tree indexes (sequential insertion), readable. Disadvantages: predictable (a security risk for a public API), problems with sharding (collisions), cannot be generated on the client. UUID: Advantages: globally unique without coordination (ideal for distributed systems), can be generated on the client, does not reveal information. Disadvantages: more space (16 bytes), B-tree index fragmentation (random insertion), less readable. Compromises: UUIDv7 (a time-ordered UUID) -- preserves sortability, ULID -- sortable, URL-safe. Snowflake ID (Twitter) -- 64-bit, time-based, distributed. Choice: a distributed system -> UUID/ULID. A monolith with a single DB -> auto-increment. A public API -> UUID (hides the number of records).',
    explanation:
      'Choosing an ID is an architectural decision with long-term consequences. Migrating from one type to another is painful. With sharding, auto-increment requires additional coordination (a ticket server, ranges). UUIDs are safer for future scaling.',
  },
  {
    id: 'sd-db-039',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is database partitioning and how does it differ from sharding?',
    options: [
      'They are the same thing',
      'Partitioning is splitting within a single DB, while sharding is distributing across several servers',
      'Partitioning is for NoSQL, sharding is for SQL',
      'Sharding is a type of partitioning for indexes',
    ],
    correctIndex: 1,
    explanation:
      'Partitioning is splitting a table into parts (partitions) within a single DBMS. The data is physically stored separately, but logically it is a single table. PostgreSQL: partition by range, list, hash. Sharding is distributing data across several independent DB servers. Each shard is a separate database. Partitioning: simpler, a single server, built-in DBMS support. Sharding: scaling beyond a single server, but more complex (coordination, routing).',
  },
  {
    id: 'sd-db-040',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is CDC (Change Data Capture)?',
    options: [
      'A backup mechanism',
      'Capturing data changes in the DB for replication or synchronization with other systems',
      'A data encryption protocol',
      'A database monitoring system',
    ],
    correctIndex: 1,
    explanation:
      'CDC (Change Data Capture) is a technology for tracking and capturing changes in a database (INSERT, UPDATE, DELETE) for replication or integration with other systems. Methods: log-based (reading WAL/binlog -- Debezium, Maxwell), trigger-based (triggers on tables), timestamp-based (polling by updated_at). Applications: synchronization with Elasticsearch, event-driven architecture, data warehouse ETL, cache invalidation. Debezium + Kafka is a popular stack for CDC.',
  },
  {
    id: 'sd-db-041',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'senior',
    type: 'open',
    question: 'How do you implement full-text search in a database? Compare built-in DBMS solutions with Elasticsearch.',
    sampleAnswer:
      'Built-in full-text search: PostgreSQL: tsvector/tsquery, GIN indexes. It supports: stemming, ranking, phrase search. For Russian -- pg_trgm, hunspell dictionaries. It is simpler to integrate and transactionally consistent with the data. Limitations: scaling only with the DB, fewer analysis capabilities. MySQL: FULLTEXT index (InnoDB). Basic functionality, less flexible than PostgreSQL. MongoDB: text index. Simple search, limited functionality. Elasticsearch: Advantages: powerful text analysis, facets, aggregations, relevance tuning, horizontal scaling, near real-time search. Disadvantages: separate infrastructure, data synchronization (dual write or CDC), eventual consistency with the main DB, operational complexity. Recommendations: simple search, small data -> the DBMS built-in search. Complex search, scale, faceted search -> Elasticsearch. They are often combined: the DBMS for CRUD, Elasticsearch for search.',
    explanation:
      'The choice of a search solution depends on the requirements. For an MVP, PostgreSQL\'s built-in search is often sufficient. As the requirements grow and become more complex (autocomplete, fuzzy search, analytics) -- migration to Elasticsearch. It is important to plan data synchronization in advance.',
  },
  {
    id: 'sd-db-042',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is database replication lag and how do you monitor it?',
    options: [
      'The delay between a request and the DB response',
      'The delay between applying changes on the master and the replica',
      'The time to create a backup',
      'Network connection latency',
    ],
    correctIndex: 1,
    explanation:
      'Replication lag is the delay between applying a transaction on the master (primary) and its appearance on the replica. Causes: load on the replica, network latency, large transactions, slow disks. Consequences: reading stale data from replicas. Monitoring: PostgreSQL -- pg_stat_replication, MySQL -- SHOW SLAVE STATUS (Seconds_Behind_Master). Solutions: read-your-writes consistency (reading from the master after a write), synchronous replication (at the cost of latency), monitoring and alerts on lag.',
  },
  {
    id: 'sd-db-043',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'junior',
    type: 'quiz',
    question: 'What is NULL in databases and what problems does it create?',
    options: [
      'An empty string',
      'A special value meaning the absence of data',
      'Zero',
      'An error in the data',
    ],
    correctIndex: 1,
    explanation:
      'NULL is a special value meaning the absence or unknownness of data. It is not an empty string and not zero. Problems: NULL = NULL returns NULL (not true), so IS NULL is needed for checking; aggregate functions ignore NULL (AVG does not count it); indexes may not include NULL; complex logic (three-valued logic). Best practices: NOT NULL constraints where possible, COALESCE to replace NULL with a default value.',
  },
  {
    id: 'sd-db-044',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'middle',
    type: 'open',
    question: 'How do you organize the storage of hierarchical data (trees) in a relational database?',
    sampleAnswer:
      'Ways to store trees in an RDBMS: 1) Adjacency List -- each row stores a parent_id. Simple, but recursive queries are slow. PostgreSQL WITH RECURSIVE helps. 2) Nested Sets -- each node has left and right numbers that define the boundaries of the subtree. Fast subtree reads, but slow inserts (renumbering). 3) Materialized Path -- store the full path: "/1/2/5/". Fast search for descendants (LIKE "/1/2/%"), but the path can be long. 4) Closure Table -- a separate table with all ancestor-descendant relationships. Fast queries for any relationships, but a lot of space and complex inserts. 5) ltree (PostgreSQL) -- a built-in type for paths, GiST indexes. Choice: frequent reads, rare changes -> Nested Sets, Closure Table. Frequent changes -> Adjacency List + CTE. PostgreSQL -> ltree.',
    explanation:
      'Hierarchical data (organizational structures, categories, comments) is a common task. Each method has trade-offs. For small trees, an Adjacency List is sufficient. For large and deep ones -- specialized solutions or graph databases.',
  },
  {
    id: 'sd-db-045',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'middle',
    type: 'quiz',
    question: 'What is a deadlock in a database?',
    options: [
      'An overflow of the transaction buffer',
      'A mutual lock where two transactions wait for each other',
      'A long transaction that locks a table',
      'A database connection error',
    ],
    correctIndex: 1,
    explanation:
      'A deadlock is a situation where transaction A holds a lock on resource X and waits for resource Y, while transaction B holds a lock on Y and waits for X. Neither can proceed. The DBMS automatically detects the deadlock and aborts one transaction (the victim). Prevention: always lock resources in the same order, minimize lock duration, use optimistic locking where possible. Monitoring: pg_stat_activity, deadlock logs.',
  },
  {
    id: 'sd-db-046',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'senior',
    type: 'open',
    question: 'How do you perform an online schema migration for a table with billions of rows without downtime?',
    sampleAnswer:
      'Online schema migration for large tables: 1) pt-online-schema-change (Percona, MySQL) -- creates a new table with the new schema, copies data in chunks, uses triggers to synchronize new changes, and at the end performs an atomic swap. 2) gh-ost (GitHub, MySQL) -- similar, but without triggers (it uses the binlog). 3) pgroll (PostgreSQL) -- the expand/contract pattern with versioned views. 4) Manual approach: add a nullable column (fast), backfill data in batches, make it NOT NULL, drop the old column. 5) Blue-green tables -- create a new table, dual-write during the migration, switch reads. Principles: backward compatible changes (adding a nullable column), forward compatible (the application works with both schemas), incremental rollout, monitoring replication lag during the migration. Expand-contract: first expand the schema (add column), then contract (drop old column).',
    explanation:
      'Schema migrations at scale are one of the hardest tasks. ALTER TABLE with a lock on a billion rows = hours of downtime. Facebook, GitHub, and Stripe have published approaches to zero-downtime migrations. The key is backward/forward compatibility and incrementality.',
  },
  {
    id: 'sd-db-047',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'senior',
    type: 'quiz',
    question: 'What is a vector database and what is it used for?',
    options: [
      'A database for storing images',
      'A database for storing and searching over vector representations (embeddings)',
      'A database for time series',
      'A graph database',
    ],
    correctIndex: 1,
    explanation:
      'A vector database is a specialized DB for storing and performing similarity search over vector representations (embeddings). Embeddings are numeric representations of data (text, images, audio), where semantically similar objects have similar vectors. Applications: semantic search, recommendations, RAG (Retrieval-Augmented Generation) for LLMs, deduplication. Examples: Pinecone, Weaviate, Milvus, Qdrant, pgvector (PostgreSQL extension). Algorithms: ANN (Approximate Nearest Neighbor), HNSW, IVF.',
  },
  {
    id: 'sd-db-048',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'junior',
    type: 'open',
    question: 'What is a database transaction and which operations does it include?',
    sampleAnswer:
      'A transaction is a logical unit of work with the DB that either executes fully or not at all. The main operations: BEGIN/START TRANSACTION -- the start of a transaction. COMMIT -- committing the changes (saving). ROLLBACK -- rolling back the changes (canceling). SAVEPOINT -- a save point within a transaction for a partial rollback. Example: a money transfer -- decrease balance A, increase balance B. If the second operation fails, the first is rolled back. Without a transaction, the money could "disappear." The properties of transactions are ACID: Atomicity, Consistency, Isolation, Durability.',
    explanation:
      'Transactions are a fundamental DBMS concept. They guarantee data integrity during failures and concurrent access. Important: long transactions hold locks, which reduces performance. A transaction should be as short as possible.',
  },
  {
    id: 'sd-db-049',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'middle',
    type: 'quiz',
    question: 'Which PostgreSQL mechanism allows storing and querying JSON data with indexing?',
    options: [
      'A TEXT column with a JSON string',
      'The JSONB data type with GIN indexes',
      'The XML data type',
      'The ARRAY data type',
    ],
    correctIndex: 1,
    explanation:
      'JSONB is a binary format for storing JSON in PostgreSQL. Unlike JSON (textual), JSONB: is optimized for reading (it is not parsed every time), supports indexes (GIN for fast search by keys and values), supports operators (containment @>, existence ?). Applications: schema-flexible attributes, settings, metadata. It does not replace the relational model for structured data with frequent JOINs.',
  },
  {
    id: 'sd-db-050',
    block: 'sd',
    topic: 'databases',
    topicLabel: 'Databases (SD)',
    difficulty: 'senior',
    type: 'open',
    question: 'Describe a strategy for choosing a database for a startup that plans rapid growth. Which factors are critical?',
    sampleAnswer:
      'A DB selection strategy for a growing startup: 1) Start simple -- PostgreSQL covers 90% of cases. Do not choose NoSQL "for the future" without understanding the data patterns. 2) Managed services -- RDS, Cloud SQL, Atlas. Do not spend engineering time on operations. 3) Data schema -- if the structure is unknown and changes quickly, MongoDB can speed up iterations. If the data is structured -- SQL. 4) Transactions -- if ACID guarantees are needed -> a relational DB. 5) Scaling -- read replicas are the first step. Plan sharding in advance (UUID instead of auto-increment). 6) Vendor lock-in -- consider the cost of migration. PostgreSQL-compatible options (CockroachDB, Aurora) are simpler than proprietary DynamoDB. 7) The team -- use what the team knows. Expertise matters more than theoretical advantages. 8) Cost -- free tiers for the start, a clear cost-growth model. Recommendation: PostgreSQL (RDS) + Redis (ElastiCache) covers most startups up to serious scale.',
    explanation:
      'Premature optimization of the database choice is a common mistake. Stack Overflow ran on SQL Server, Instagram started on PostgreSQL. Choosing an exotic database creates an operational burden. It is better to start with a proven solution and optimize as you grow and understand the real patterns.',
  },
];
